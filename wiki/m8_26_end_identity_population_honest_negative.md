# M8.26: END_IDENTITY_UNPRINTED Population — HONEST NEGATIVE (No Zero-False End-Identity Gate)

Status: **SAFE HONEST-NEGATIVE.** The corpus-wide `END_IDENTITY_UNPRINTED`
blocker class (25 bores) is **investigated and closed as correctly-abstaining**.
**No zero-false terminal/AP-HH end-identity gate exists** for this population;
**zero bores move**; all 25 stay `END_IDENTITY_UNPRINTED`, each with exact named
missing evidence. Census frozen `25/13/5/5/4/3/1/2 = 58`; proof-only; no
placement, no stroke, no engine change; log8/log32/log42 + the M8.20 group
review untouched.

Probe: `truelinev2/proof/run_end_identity_population_probe.py` (G1–G6 PASS)
Tests: `truelinev2/tests/test_end_identity_population_probe.py` (6, offline)
Verification: M8.26 construct→refute→synthesize adversarial workflow
(**0 gate proposals of 5 candidate classes; 0 survived the skeptic panel**).

## 1. The gate hypothesis, and why it is refuted by the evidence

The M8.25 lever was: *a new adversarially-proven end-identity gate reading a
printed **terminal/AP-HH** layer the current `bind_end_structure_note` grammar
excludes* — without inferring identity from run callouts/equations/footage, and
without trusting nearby AP/HH text whose frame/layer ownership is unproven.

The read-only probe enumerated the 25 bores live and dumped the complete
end-station printed evidence per referenced sheet. Drift-guarded findings:

- **G3 — zero AP-id terminals at any of the 25 ends** (`ending_callout_ap_ids=[]`,
  `starting_callout_ap_ids=[]`). The AP-HH terminal form the hypothesis required
  is simply **absent** at these bore ends; no AP position-chain resolves
  (`ap_ownership_bound=[]`), so frame/layer ownership is never PROVEN for any
  candidate.
- **G4 — exactly ONE bore has a printed end NOTE the grammar binds: log27**
  (`PLACE 24"X34"X24" NEXTLINK HH PROP. SPLICE POINT`). Its class
  (`NEXTLINK HH` / `SPLICE`) sits **outside** the dialect `class_keywords`, its
  symbol layer (`NEXTLINK`) **shares red/blue fills** with `installer_hh` and
  `terminal_port_hh` (no class-fill discriminator), it carries **no AP id**, and
  there is **no drawn axis tick** at 13+55 to corroborate position — so it is
  class-ambiguous AND unlocatable. (log27 already PLACES its redline via the
  M8.5/M8.8 station-increment opt-ins; only the stroke-geometry lane abstains.)
- **G5 — zero clean gate candidates** (no unique frame/layer-owned AP at an end
  tick). The only ending-callout structure keyword in the whole population
  (log3/log4 `PORT TERMINAL TAIL`) is a dialect **CONDUIT** class (`BORE - PORT`),
  not a handhole, and arrives inside a grammar-excluded RUN CALLOUT.

## 2. Population taxonomy (machine-derived; 25 bores)

| # | end-station situation | bores |
|---|---|---|
| 15 | **pure printed void** — no classifiable end-structure note/AP/tick (3 of these — log5/log15/log30 — have a bare ladder tick whose nearest symbol is a FLOWER POT or 200+pt away: wrong-class / unowned) | log5, log11, log15, log19, log29, log30, log31, log41, log43, log44, log45, log48, log52, log55, log69 |
| 8 | **frame-reset `STA <end>=0+00`** — the end is the start-of-next-frame origin, not a terminal (M8.6/M8.24: `=0+00` is frame-local and printed-unidentified) | log36, log54, log63, log66 (pure reset) + log3, log4, log39, log49 (reset + callout) |
| 5 | **run callout / continuation only** — the run literally continues past the end (log39→17+23, log53→26+38) or tails into a conduit class (log3/log4 `PORT TERMINAL TAIL`); footage/closure is not an identity | log3, log4, log39, log49, log53 |
| 1 | **printed end NOTE, unmodeled class** — log27 NEXTLINK HH / SPLICE (no fill discriminator, no AP, no end tick) | log27 |

(Rows overlap: log3/log4/log39/log49 are both reset-equation and callout ends.)

log43/log44 are additionally **SOURCE-quality** abstains (M8.25): log43's drawn
axis stops at 45+33 (45+33→59+19 is a 1386' printed void); log44's 325' span
matches no print-18 run.

## 3. Adversarial confirmation

A 5-class construct panel (A NEXTLINK note · B PORT-TERMINAL-TAIL callout · C
reset-equation end · D drawn-tick-only · E text-void) each **steelmanned** a
zero-false gate; **all five returned `gate_proposed=false`**. With nothing to
refute, **0 of 0 gates survived** the skeptic panel. The panel's live PlanPdf
inspection added corroboration: log3/log4's real PORT HH structures are **rival
AP-137/AP-138** (both position-bound → ≥2 → abstain, even ignoring that
`PORT TERMINAL TAIL` is a conduit); log5/log15/log30's nearest end-tick symbol
is a FLOWER POT or 200+pt away, and log30's nearest cluster even **disagrees
across its own two sheets**. The conclusion matches the engine's own gated
result exactly: binding any end here would attach a wrong or nonexistent
structure and violate ZERO-FALSE.

## 4. Disposition + the single forward lever

All 25 stay `END_IDENTITY_UNPRINTED` (correct, zero-false). Per the ALL-REDLINES
standard, abstention is the safe interim state and the **per-bore named missing
evidence is the engineering target, not a manual fallback**. The full per-bore
missing-evidence list is in the probe report
(`data/outputs/end_identity_population_probe/end_identity_population_probe.json`).
Forward routes by class:

- **Pure-void + reset-equation ends (23 of 25):** unprovable from plan text —
  these are **KMZ/geo corroboration or SOURCE_REVIEW** lanes, not a
  redline-engine lever. (log43/log44 specifically need an owner bore_log17
  source re-read.)
- **The single redline-engine lever — log27:** disambiguate the `NEXTLINK`
  symbol layer's class-fill (a distinct color, a printed class id_token, or a
  structure-id token) AND obtain a drawn axis station tick at 13+55 (sheet 16,
  currently absent). Only if **both** land does log27 become a single-bore gate
  candidate — and it clears **at most 1 of 25**, while not changing any redline
  (log27 already places via opt-ins). Cheapest first move: a layer-fill/color
  audit of `NEXTLINK` vs `installer_hh` vs `terminal_port_hh` on sheet 16.

## 5. Boundary

Proof-only; census frozen `25/13/5/5/4/3/1/2 = 58`; log8/log32/log42 +
the M8.20 group review re-proven untouched; no stroke/PNG/segment, no AUTO, no
tolerance/budget change, no contract change, no family/daily-bundle relation as
placement proof. Outcome: the `END_IDENTITY_UNPRINTED` class is **closed as
correctly-abstaining** — no placeable member under printed plan evidence, each
with its exact named missing evidence, the AP-HH-terminal gate hypothesis
adversarially refuted.
