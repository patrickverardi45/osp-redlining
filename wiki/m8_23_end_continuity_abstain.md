# M8.23: log42 END Continuity — Corroborated but NON-PROMOTING (formal ABSTAIN)

Status: **SAFE ABSTAIN — log42 is NOT a REVIEW candidate.** The asked-for
non-scale END-continuity → REVIEW promotion was REFUTED by a 5-lens
adversarial panel (mis-targeted blocker). log42 stays
`STRUCTURE_IDENTITY_BINDING_REQUIRED`; census frozen `25/13/5/5/4/3/1/2 = 58`;
proof-only; zero strokes.

Probe: `truelinev2/proof/run_end_continuity_abstain_probe.py` (G1–G7 PASS)
Tests: `truelinev2/tests/test_end_continuity_abstain_probe.py` (6)

## 1. Why the REVIEW promotion was refused (the real blocker)

The END-scale "6% disagreement" the milestone was built to fix is a
**probe-only artifact**. In the SHIPPED engine `extract_group_claims([log42])`
returns **0 far survivors** (the M8.18 uniqueness gate), so the M8.19
cross-sheet scale-join is never reached — far_scale is never computed. The 6%
story exists only inside M8.22's probe, which feeds the join a
`DIRECTIONAL_FORWARD_OF_PRINTED_ORIGIN` survivor the codebase explicitly bars
from Law-1/M8.18/M8.19.

log42's **actual** shipped-lane abstain is **START-structure-identity**:
`resolve_bore(log42).named_missing` names the START origin (11 sheet-2
structures on the shared alignment, 0 corroborate; the 0+00 origin can't be
uniquely bound) — it does **not** mention scale. A law that proves the END
join changes nothing about the START abstain, so promoting log42 to REVIEW
would ask a human to confirm a redline whose **origin identity is unknown** —
a false REVIEW candidate (zero-false violation). Continuity is therefore NOT
claimed proven: the only non-scale crossing witness is the printed boundary
equation (which alone never sufficed in `cross_sheet_join_verdict`), and
closure `270+17=287` is printed arithmetic, never made the sole load-bearing
gate.

## 2. What IS banked (the one sound gate the panel validated)

The END terminus is `terminal_port_hh` by **printed class**, settling the
FLOWER POT red herring permanently:
- `bind_end_structure_note(287)` → BOUND, label `…TERMINAL 6 PORT HH AP-105 …
  TERMINAL TAIL`; `resolve_structure_position("AP-105", terminal_port_hh)` →
  BOUND @(84.6,419.4); the run callout is `E/W PORT TERMINAL TAIL`.
- FLOWER POT@(84.8,422.3) is on a **different CAD layer** (`FLOWER POT` vs
  `NEXTLINK`), with **no** PORT-HH/AP identity at 2+87. Excluded by printed
  class — never by which anchor numerically fits. The "FLOWER POT proves
  scale at 3.4%" is the WRONG class.

The 17-ft end segment carries **no usable scale**: a 2.9-pt anchor move
(NEXTLINK 1.3669 vs FLOWER POT 1.5039) swings end-scale 10% on the same draw.
Over-fire guard: log32's 36-ft end segment also trips any naive sub-floor
predicate yet stays blocked on START — sub-floor is **necessary-not-
sufficient**, and a continuity-via-sub-floor law would be a generalization
hazard that still wouldn't address log42's real blocker.

## 3. Named missing & next step

The **START origin structure identity** at the callout-frame 0+00
(`NEXTLINK@819,351`, `DESIGN_PATH_AMBIGUOUS` / identity ABSTAINED): a strand
discriminator that **binds the origin identity** (not just its position, as
M8.22's probe-local directional filter did), and/or the owner `bore_log13`
block-semantics re-read. END continuity is downstream and irrelevant to
placement until the origin identity binds through a **lane-accepted** path.

## 4. Boundary

Proof-only; census frozen; log8/log32 + the M8.20 group review untouched
(re-proven); no stroke/PNG/segment, no AUTO, no tolerance/budget change, no
REVIEW promotion. Outcome: SAFE ABSTAIN with the exact missing evidence named.
