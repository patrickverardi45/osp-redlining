# START HERE — TrueLine v2 Canonical Bootstrap

> Single source of current working truth. Read THIS file first, in full — it is small on purpose.
> **continued 97 (2026-06-30) — Track B: read-only cold-package CODE gate G-b′ (plan-view ROUTE-LAYER ISOLATION) SHIPPED (read-only observer; NO AUTO/placement/renderer/`_cap_review`/status change; `select_dialect` + named detectors UNTOUCHED).** Backend `feat/truelinev2` **`fcc6278`(continued-96 START_HERE bump)→`2f8a26a`** (`feat: read-only plan-view route-layer isolation (cold-package G-b′; no AUTO/placement)`, 2 files/+683, pushed `origin/feat/truelinev2`, synced 0/0). **`2f8a26a` IS the new latest CAPABILITY/code + docs HEAD; backend `origin/main` `068a279` UNTOUCHED; full v2 2041→2058 pass / 4 skip (+17); cold-package matrix 11/11 + deterministic 50/58 + `DETERMINISTIC_AUTO=49` UNCHANGED; web/staging UNTOUCHED; P5 PAUSED; Hector v1-parity PARKED.** Capability chain now **G-a `78730d5` → G-b `788e645` → G-a′ `cf2a914` → G-b′ `2f8a26a`**. **G-b′ (`2f8a26a`):** NEW `extract/plan_view_route_isolator.py` (read-only; name-free; imports ONLY stdlib + the read-only G-a/G-b observers + `PlanPdf`; no contracts/match/render/api/store/placement/`_cap_review`): `observe_route_isolation(line_items, start_xy, end_xy, …)` classifies each drawing ROUTE_CANDIDATE vs closed GRID/BOX (per-drawing cycle: edges≥nodes & nodes≥3) vs PAGE_FRAME (bbox covers ≥0.8 of BOTH page dims), drops word-attached short leader/annotation-tick segments, then REUSES G-b's `observe_run_between_anchors` VERBATIM on (a) the route-isolated set and (b) the full set, COMPARING them to flag a span that exists only through grid/border artifacts → diagnostics `ROUTE_LINEWORK_ISOLATED` / `NO_ROUTE_LINEWORK` / `GRID_OR_BORDER_ONLY` / `MULTIPLE_ROUTE_CANDIDATES` / `ROUTE_ENDPOINT_NOT_TIGHT` / `ROUTE_LAYER_AMBIGUOUS` / `UNMEASURABLE`. `ROUTE_LINEWORK_ISOLATED` ADDITIONALLY requires a SIMPLE anchor-to-anchor path (no dangling stub / no tail past an anchor — closes the adversarially-found gap where G-b's CONNECTED admits a degree-2-anchor stub). `class_verified` ALWAYS False → REVIEW evidence only, never a stroke/placement/AUTO. + path wrapper `observe_plan_view_route_for_path` (G-a→G-b′). 17 name-free adversarial tests (inline geometry for the core; real `fitz` PDFs for the path driver); a focused adversarial review confirmed the false-positive-ISOLATED property HOLDS (no box/grid/frame/leader fakes CONNECTED). **Validation on gitignored `public-cold-011` (no files committed; no names in code/docs; no generalization claimed):** page 16, stations 177+01/178+56 (label centers ~43pt apart); 485 drawings → 8 boxes excluded + 400 leader/tick dropped → 118 isolated route segments; G-b′ IMPROVES the diagnosis from raw grid-polluted `MULTIPLE_PLAUSIBLE_RUNS` (reach_tol 50) to the more precise `ROUTE_LAYER_AMBIGUOUS`; STILL refuses honestly (no redline drawn). **STILL MISSING:** label anchors are still ~16–41pt OFF the route (`start_gap≈41`); the route region still forks (laterals in un-layered linework); no clean `ROUTE_LINEWORK_ISOLATED` on cold-011 yet; **NEXT GATE G-b″** = feed the isolated route segments back into anchor resolution (G-a′-on-isolated-route) for a source-supported route anchor, then a route-vs-lateral discriminator, then G-e cold REVIEW stroke. NO placement/promotion; **G4/AUTO REMAINS BLOCKED.** See [[hot]]/[[current-sprint]]/[[log]] continued 97 + [[cold-package-evaluation-harness]].
> **continued 96 (2026-06-30) — Track B: TWO more read-only cold-package CODE gates SHIPPED — G-b (plan-view drawn-run VERIFICATION) + G-a′ (plan-view label→structure/route ANCHOR RESOLVER) (read-only observers; NO AUTO/placement/renderer/`_cap_review`/status change; `select_dialect` + named detectors UNTOUCHED).** Backend `feat/truelinev2` **`78730d5`(G-a, continued-95)→`faea7c6`(continued-95 START_HERE bump)→`788e645`(G-b)→`cf2a914`(G-a′)**, all pushed `origin/feat/truelinev2`. **`cf2a914` IS the new latest CAPABILITY/code HEAD; backend `origin/main` `068a279` UNTOUCHED; full v2 2019→2030(G-b)→2041(G-a′) pass / 4 skip; cold-package matrix 11/11 + deterministic 50/58 + `DETERMINISTIC_AUTO=49` UNCHANGED; web/staging UNTOUCHED; P5 PAUSED; Hector v1-parity PARKED.** Capability chain now **G-a `78730d5` → G-b `788e645` → G-a′ `cf2a914`**. **G-b (`788e645`):** NEW `extract/plan_view_run_observer.py` (read-only; name-free; imports only `PlanPdf` + the G-a locator; no contracts/match/render/api/store/placement/`_cap_review`; no axis): `observe_run_between_anchors` classifies drawn linework between two located anchors → `PLAN_VIEW_RUN_CONNECTED` vs `NO_DRAWN_RUN`/`BROKEN_RUN`/`MULTIPLE_PLAUSIBLE_RUNS`/`FORKED_RUN`/`ENDPOINT_NOT_TIGHT`/`UNMEASURABLE` (bbox prefilter + O(n) spatial-hash weld; never draws; `class_verified` always False); `observe_plan_view_run_for_path` composes G-a→G-b. 11 name-free adversarial tests. Cold-011: REFUSES at every tol (`NO_DRAWN_RUN`@12→`ENDPOINT_NOT_TIGHT`@30→`MULTIPLE_PLAUSIBLE_RUNS`@50) — labels 16-40pt off the route + no CAD layer isolation. **G-a′ (`cf2a914`):** NEW `extract/plan_view_anchor_resolver.py` (read-only; name-free; reuses the proven `leader_symbol_trace` chain; same import guards): upgrade a located LABEL to a SOURCE-BACKED anchor in strict priority leader-traced symbol→leader tip→unique nearby symbol→unique route TERMINUS → `ANCHOR_RESOLVED_TO_SYMBOL`/`_LEADER_TIP`/`_ROUTE_ENDPOINT` else refuse `LABEL_ONLY_NO_ANCHOR`/`AMBIGUOUS_ANCHOR`/`NO_SUPPORTED_ANCHOR`/`UNMEASURABLE`; NEVER snaps to the nearest passing line (route termini only); `class_verified` always False. 11 name-free tests. Cold-011: both labels still locate but anchors → `AMBIGUOUS_ANCHOR` (`AMBIGUOUS_LEADER`: each word framed by 3 grid boxes) → no stronger anchor, G-b can't improve. **Architecture doc `truelinev2/docs/COLD_PACKAGE_REDLINE_ARCHITECTURE.md` updated (G-b + G-a′ marked DONE; G-b′ route-layer isolation promoted to the gating step).** Proof both gates: prior battery (96 pass G-a′/+85 G-b) all green; locks UNCHANGED; forbidden-seam diff EMPTY; new files name-free; cold-011 validations gitignored, NOT committed, no generalization claimed. **The gates give endpoint POSITIONS + ANCHOR TRUTH + a RUN-VERIFICATION primitive — still NOT a drawn redline.** **STILL MISSING / NEXT GATE: G-b′ route-layer isolation** (separate the route from grid/border/annotation so labels aren't framed by grid boxes — the honest blocker proven by G-a′'s refusal on cold-011), then G-e cold REVIEW stroke; plus HDD point binder (B1), OSP structure binders (B3), OCR (scanned), GENERIC CLASS VERIFICATION. NO placement/promotion; **G4/AUTO REMAINS BLOCKED.** See [[hot]]/[[current-sprint]]/[[log]] continued 96 + [[cold-package-evaluation-harness]].
> **continued 95 (2026-06-30) — Track B: senior-architect cold-package redline ARCHITECTURE + first read-only code gate G-a (plan-view printed-station 2-D LOCATOR) SHIPPED (read-only observer; NO AUTO/placement/renderer/`_cap_review`/status change; `select_dialect` + named detectors UNTOUCHED).** Backend `feat/truelinev2` **`b3f4589`(docs continued-94)→`78730d5`** (`feat: read-only plan-view printed-station 2-D locator (cold-package G-a; no AUTO/placement)`, 3 files/+357, pushed `origin/feat/truelinev2`)→**`<continued-95 bump>`** (START_HERE). **`78730d5` IS the new latest CAPABILITY/code commit (first code since `4422cf4`); backend `origin/main` `068a279` UNTOUCHED; full v2 2010→2019 pass/4 skip (+9 new); cold-package matrix 11/11 + deterministic 50/58 + `DETERMINISTIC_AUTO=49` UNCHANGED; web/staging UNTOUCHED; P5 PAUSED; Hector v1-parity PARKED.** ROOT-CAUSE call: the harvest proof case `public-cold-011` bound BOTH endpoints via `PRINTED_STA_CALLOUT` but failed geometry with `NO_STATION_AXIS` — NOT a weak linear-axis fit but a PLAN-VIEW MODEL MISMATCH (a meandering OSP/fiber route has no linear `station=a*x+b` axis; 3 non-collinear station tokens). So the first gate is NOT better axis fitting (that only helps profile/pipeline sheets, not the product domain) — it is the missing primitive: resolve a source-bound station to its 2-D position from the printed LABEL, no axis. NEW `extract/printed_station_locator.py` (read-only; name-free; imports only `PlanPdf` + station parsing; touches NO contracts/match/render/api/store/placement/`_cap_review`): `locate_printed_station` (identity match; strict refusal `NO_PRINTED_STATION_LABEL`/`AMBIGUOUS_STATION_LABEL`; never snaps/invents) + `observe_plan_view_endpoints` (both source-bound endpoints→2-D anchors + label separation; `class_verified` ALWAYS False → never unblocks AUTO). + `truelinev2/docs/COLD_PACKAGE_REDLINE_ARCHITECTURE.md` (name-free 6-stage roadmap: recognition→identity-binding→2-D-position→drawn-run-verify→cold-REVIEW→AUTO; per-family needs; gates G-a→G-z) + 9 name-free synth tests (incl. a non-collinear layout where a linear axis provably can't fit). Proof: full v2 **2019 pass/4 skip**; prior package_validation/leader_trace/2-D/branch tests **76 pass**; locks UNCHANGED; forbidden-seam diff EMPTY. Local GITIGNORED cold-011 validation: `PLAN_VIEW_ENDPOINTS_LOCATED` — both endpoints resolved to 2-D positions (177+01→~(107,270), 178+56→~(150,270), sep 43.47) where the axis observer abstained. **The locator gives endpoint POSITIONS ONLY — not a verified bore path or a redline stroke.** **STILL MISSING:** G-b (verify a drawn run connects the two anchors — branch/tightness in 2-D); HDD entry/exit point-station binder; OSP structure/vault/handhole binders; cold REVIEW stroke emission; OCR/raster ingestion (scanned/no-text); GENERIC CLASS VERIFICATION. NO placement/promotion; NO generalization claimed; **G4/AUTO REMAINS BLOCKED.** See [[hot]]/[[current-sprint]]/[[log]] continued 95 + [[cold-package-evaluation-harness]].
> **continued 94 (2026-06-30) — Track B: PUBLIC COLD-CORPUS HARVEST + dialect-discovery (read-only; docs/data only; NO engine/observer/dialect/AUTO/placement/renderer/`_cap_review` change; `select_dialect` + named detectors UNTOUCHED).** Backend `feat/truelinev2` **`294b3c2`(docs continued-93)→`bf4f216`** (`docs: public cold-corpus harvest checkpoint (read-only; name-free)`)→**`<this bump>`** (START_HERE continued-94), pushed `origin/feat/truelinev2`. **Latest CAPABILITY commit remains `4422cf4`; backend `origin/main` `068a279` UNTOUCHED; full v2 2010 pass/4 skip + cold-package matrix 11/11 + deterministic 50/58 + `DETERMINISTIC_AUTO=49` UNCHANGED (NOT re-run — docs/data only); web/staging UNTOUCHED; P5 PAUSED; Hector v1-parity PARKED.** Drove a broad public-plan harvest through the read-only `harness/package_validation` intake path (4 parallel research agents → verified direct-PDF URLs → download to GITIGNORED `data/outputs/truelinev2/cold_packages/public-cold-NNN/` → UNMODIFIED `select_dialect` prefilter → classify; cold candidates with a clean source-visible bore span packaged name-free + validated). **35 cumulative candidates downloaded (ALL gitignored, NONE committed): 10 RECOGNIZED by existing dialects (`DIRECTIONAL BORE` + `STA a+bb TO STA c+dd` — both grammars COMMON in real public plans), 14 COLD/non-recognized, 11 UNUSABLE (no text layer / no station evidence → needs OCR/raster ingestion).** **3 fully validated: `public-cold-001` (recognized→`INELIGIBLE_RECOGNIZED_WORK_CORPUS` control), `public-cold-002` (eligible-fresh; observer RAN, endpoints UNBOUND, `RIVAL_RUNS`, ABSTAINED), `public-cold-011` (eligible-fresh; FIRST real-plan endpoint BIND — both termini bound via `PRINTED_STA_CALLOUT` from a printed `Sta. X to Y` handhole span; geometry `NO_STATION_AXIS`; only blocker `CLASS_VERIFICATION_MISSING`).** **Grammar families (name-free):** recognized DIRECTIONAL-BORE + STA-TO-STA; cold pipeline-HDD plan-profile (largest cold family; entry/exit POINTS not structures → unbound), cold county/municipal buried-fiber handhole-span (binds via PRINTED_STA_CALLOUT), cold OSP-fiber-permit plan-view (VAULT/HANDHOLE + STA-BCF), cold municipal water-main-HDD (length-based), cold airport/utility-duct offset-station; scanned/no-text (OCR-needed). **NEXT GATES (read-only, NO AUTO, each owner-approved to implement):** (1) generic station-axis fitting across diverse sheets (cold-011 bound but `NO_STATION_AXIS`), (2) HDD entry/exit point-station binder (largest cold family), (3) OCR/raster ingestion later. Name-free committed summary `truelinev2/docs/PUBLIC_COLD_CORPUS_HARVEST.md`; named source inventory + per-candidate URLs ONLY in the GITIGNORED `data/outputs/truelinev2/cold_packages/_DIALECT_DISCOVERY_REPORT.md` (+ per-package `_harvest.json`). NO generalization claimed; NO placement/promotion; **G4/AUTO REMAINS BLOCKED.** See [[hot]]/[[current-sprint]]/[[log]] continued 94 + [[cold-package-evaluation-harness]].
> **continued 93 (2026-06-30) — DOCS-ONLY save/bootstrap bump: the continued-92 read-only cold-package intake path is now an ACTIONABLE real/fresh package collection workflow (docs/wiki only; NO engine/observer/fixture/test/AUTO/`_cap_review`/placement/status/renderer/staging/web change).** Backend `feat/truelinev2` **`5c10e9a`(docs continued-92)→`600b370`** (`docs: cold-package validation intake contract + owner collection checklist (docs-only; no code/AUTO change)`, 1 file/+307, pushed `origin/feat/truelinev2`, synced). **Latest CAPABILITY commit remains `4422cf4` (continued-92 read-only eligibility+validation intake path); backend `origin/main` `068a279` UNTOUCHED; cold-package matrix 11/11 + deterministic 50/58 + `DETERMINISTIC_AUTO=49` UNCHANGED; full v2 2010 passed / 4 skipped (unchanged — docs-only, full suite not re-run this turn); no new production flag; web/staging UNTOUCHED; P5 PAUSED; Hector v1-parity PARKED.** NEW `truelinev2/docs/COLD_PACKAGE_VALIDATION_INTAKE.md` (name-free; verified line-by-line against the implementation; the contract stays test-locked by `tests/test_package_validation.py`) turns `harness/package_validation.py` into an actionable workflow: exact real/fresh package folder structure; exact `package.json` contract; accepted upload kinds (PLAN_PDF + BORE_LOG REQUIRED, GIS_ROUTE/PHOTO optional); required per-bore fields `bore_label`/`sheet`/`start_ft`/`end_ft`; advisory `provenance_class` + the full SOURCE-EVIDENCE-OVERRIDES eligibility ladder; every eligibility verdict + AUTO blocker; the read-only run command (repo-root venv `venv/Scripts/python.exe` + `PYTHONPATH`); a name-free dummy manifest; a plain-English owner checklist. **Safe gitignored drop-zone `data/outputs/truelinev2/cold_packages/<package-id>/`** (`data/` ignored `.gitignore:30` → real customer files NEVER committed); REQUIRED `uploads/plan.pdf` + `uploads/bore-log.xlsx`, OPTIONAL route KMZ/KML + photos; the manifest must remain NAME-FREE (real names live ONLY inside the uncommitted uploaded files). **HONESTY:** `CROSS_SHEET_UNRESOLVED` is documented as DEFINED in the blocker vocabulary but NOT currently emitted; eligibility ≠ AUTO (`class_verified` ALWAYS False + `CLASS_VERIFICATION_MISSING` ALWAYS present); NO placement / promotion / generalization claim. **Guards run:** doc name-token scan CLEAN; `test_package_validation.py` (14) + cold-package name-free guard (3) = **17 passed**; tracked-file diff EMPTY before the docs commit (docs-only, no code/runtime/staging/web behavior changed). **STILL MISSING:** Patrick must provide the FIRST real/fresh non-recognized package (plan PDF + bore-log `.xlsx` + per-bore sheet/start_ft/end_ft); GENERIC CLASS VERIFICATION / class-table strategy; cross-sheet continuation remains a caveat. **G4 AUTO REMAINS BLOCKED until Patrick explicitly approves a later AUTO gate; do NOT start another engineering gate.** See [[hot]]/[[current-sprint]]/[[log]] continued 93 + [[cold-package-evaluation-harness]].
> **continued 92 (2026-06-30) — Track B: read-only COLD-PACKAGE ELIGIBILITY + VALIDATION INTAKE PATH SHIPPED (the bridge from synthetic proof to a real/fresh package; read-only/observer; NO AUTO, NO `_cap_review`/renderer/placement/status change).** Backend `feat/truelinev2` **`b8e4aaa`(docs continued-91)→`4422cf4`** (`product: read-only cold-package eligibility + validation intake path (no AUTO; bridges synthetic proof to a real package)`, 3 files/+558, pushed `origin/feat/truelinev2`, synced 0/0). **Backend `origin/main` `068a279` UNTOUCHED; cold-package matrix 11/11 + deterministic 50/58 + `DETERMINISTIC_AUTO=49` UNCHANGED; full v2 1996→2010 passed / 4 skipped; no new production flag; P5 PAUSED; web/staging UNTOUCHED.** The Track B observer stack (terminus evidence + branch-uniqueness + station-x tightness + 2-D endpoint tightness + leader-traced provenance) was proven only against synthetic harnesses; this builds the bridge to REAL/FRESH cold-package validation. NEW `harness/package_validation.py` (name-free, read-only; imports the observer stack read-only; NO placement/store/job/promotion): `load_package_manifest` (`package.json`: advisory `provenance_class` + uploads + `bores[{bore_label,sheet,start_ft,end_ft}]`); `validate_package` with **SOURCE-EVIDENCE-OVERRIDES-DECLARED** precedence — `select_dialect(plan)` not None OR recognized-registry membership → `INELIGIBLE_RECOGNIZED_WORK_CORPUS` even if declared FRESH; declared `ACTIVE_FIELD` (name-free for the active-field/Hector-reserved corpus) → `INELIGIBLE_ACTIVE_FIELD_CORPUS`; declared `SYNTHETIC` or under a synthetic root → `SYNTHETIC_TEST_ONLY` (`real_validation=False`, exercises the report path but NEVER counted as real generalization); declared FRESH + non-recognized + complete → `ELIGIBLE_FRESH_NONRECOGNIZED`. `run_observer_stack` = `extract_termini` → `observe_package_terminus_geometry` per bore. `PackageValidationReport`: eligibility/reason, corpus_class, recognized, real_validation, source files present/missing, per-candidate observer diagnostics (terminus source-bound, branch, station-x, 2-D, provenance ladder, `class_verified=False`), `auto_blockers` (`CLASS_VERIFICATION_MISSING`/`REAL_FRESH_PACKAGE_VALIDATION_PENDING`/`INSUFFICIENT_TERMINUS_EVIDENCE`/`NO_DRAWN_COORDINATE_EVIDENCE`/`AMBIGUOUS_EVIDENCE`/`CROSS_SHEET_UNRESOLVED`), **`placement_performed=False`** + explicit "no placement/promotion performed" note; harness-only CLI prints the JSON report. **Eligibility statuses:** `ELIGIBLE_FRESH_NONRECOGNIZED` / `SYNTHETIC_TEST_ONLY` / `INELIGIBLE_RECOGNIZED_WORK_CORPUS` / `INELIGIBLE_ACTIVE_FIELD_CORPUS` / `INCOMPLETE_UPLOAD` / `UNKNOWN_PROVENANCE_CLASS` / `PACKAGE_NOT_FOUND` / `PACKAGE_UNREADABLE`. Proof (`tests/test_package_validation.py`, 14 tests): eligible-fresh branch runs the observer; synthetic (declared or via root) flows through but `real_validation=False`; recognized-via-registry rejected even if declared FRESH; declared ACTIVE_FIELD rejected; incomplete → INCOMPLETE_UPLOAD + named missing inputs; ambiguous (fork) + missing-coordinate non-promoting; not-found/unreadable; read-only; no eligible real/fresh package among the synthetic fixtures. `package_validation.py` added to the name-free guard `HARNESS_MODULES`. **STILL MISSING:** an actual real/fresh non-recognized package to validate; GENERIC CLASS VERIFICATION / class-table strategy; cross-sheet continuation remains a caveat. **NO eligible real/fresh package currently exists → validated on synthetic manifests ONLY, NO real-package generalization claimed.** **G4 AUTO REMAINS BLOCKED until Patrick explicitly approves a later AUTO gate.** See [[hot]]/[[current-sprint]]/[[log]] continued 92 + [[cold-package-evaluation-harness]].
> **continued 91 (2026-06-30) — Track B: read-only LEADER-TRACED structure-coordinate PROVENANCE for cold packages SHIPPED (read-only/observer; NO AUTO, NO `_cap_review`/renderer/placement/status change; class verification STILL MISSING).** Backend `feat/truelinev2` **`5d7bc84`(docs continued-90)→`df555a8`** (`product: read-only leader-traced structure-coordinate provenance for cold packages (no AUTO; class verification still missing)`, 4 files/+565/−9, pushed `origin/feat/truelinev2`, synced 0/0). **Backend `origin/main` `068a279` UNTOUCHED; cold-package matrix 11/11 + deterministic 50/58 + `DETERMINISTIC_AUTO=49` UNCHANGED; full v2 1973→1996 passed / 4 skipped; no new production flag; P5 PAUSED; web/staging UNTOUCHED.** continued-90's cold-lane `TERMINUS_DRAWN_COORDINATE` was geometry-only (a compact drawn shape at the projected station-x). `df555a8` UPGRADES the PROVENANCE when the source supports it: NEW `extract/leader_symbol_trace.py` (name-free, **pure-stdlib** geometry, uniqueness-mandatory) runs the proven `structure_position` chain (word → label box → leader → symbol) **LAYER-AGNOSTICALLY** — ZERO CAD layer names, ZERO fill/class check — so it works in the cold lane with no dialect layer table. `extract/terminus_coordinate_observer.derive_terminus_coordinate` now tries leader-trace FIRST (→ **`LEADER_TRACED_SYMBOL`**, the strongest cold-lane provenance), refuses on AMBIGUOUS leader/symbol (→ `AMBIGUOUS_DRAWN_COORDINATE`, never downgraded), else falls back to the weaker **`COMPACT_SYMBOL_AT_STATION`**; sub-statuses `LABEL_ONLY_NO_SYMBOL`/`AMBIGUOUS_LEADER`/`AMBIGUOUS_SYMBOL`/`NO_LEADER_EVIDENCE`. `TerminusCoordinate` gains the provenance ladder + `leader_trace` + **`class_verified` (ALWAYS False in the cold lane)**. The prior `derive_terminus_drawn_coordinate` (compact) is UNCHANGED; core/adapter gained optional words/line_items/label-tokens (empty → prior behavior preserved); the symbol-at-tip gate excludes elongated leader lines (aspect bound) so a leader can't count as its own symbol; the label/text centroid is NEVER a coordinate. `harness/synth.py` adds `_label_box`/`_leader` + leader-traced/label-only/ambiguous-leader/ambiguous-symbol builders (name-free, NOT in `_CATALOG`). Proof (`tests/test_leader_symbol_trace.py`, 23 tests): leader-traced → `LEADER_TRACED_SYMBOL` (stronger than the same symbol without a frame → `COMPACT_SYMBOL_AT_STATION`); label-only → NO_DRAWN_COORDINATE; ambiguous leader/symbol → non-promoting; y-misaligned → still 2-D rejected; fork → still branch-rejected; prior 17 branch + 19 2-D tests preserved; read-only + pure-stdlib + name-free. **STILL MISSING:** real/fresh-package validation; **GENERIC CLASS VERIFICATION** (cold-lane has no CAD layer/class table — `structure_position.resolve_structure_position` is the named-dialect analog; a generic class-table/class-inference strategy if one is possible); cross-sheet continuation remains a caveat. **NO eligible fresh non-recognized package exists in the repo → validated on the adversarial harness ONLY, NO real-package generalization claimed.** **G4 AUTO REMAINS BLOCKED until Patrick explicitly approves a later AUTO gate.** See [[hot]]/[[current-sprint]]/[[log]] continued 91 + [[cold-package-evaluation-harness]].
> **continued 90 (2026-06-30) — Track B: read-only TERMINUS DRAWN-COORDINATE + 2-D endpoint-tightness observer SHIPPED (the continued-89 residual closed against the adversarial harness; read-only/observer; NO AUTO, NO `_cap_review`/renderer/placement/status change).** Backend `feat/truelinev2` **`ff3b5a7`(docs continued-89)→`c9d9bc1`** (`product: read-only terminus drawn-coordinate + 2-D endpoint-tightness observer (no AUTO)`, 4 files/+620, pushed `origin/feat/truelinev2`, synced 0/0). **Backend `origin/main` `068a279` UNTOUCHED; cold-package matrix 11/11 + deterministic 50/58 + `DETERMINISTIC_AUTO=49` UNCHANGED; full v2 1954→1973 passed / 4 skipped; no new production flag; P5 PAUSED; web/staging UNTOUCHED.** continued-89's residual: a source-bound terminus carries a STATION not a drawn coordinate, so a run reaching the right station-x at the WRONG y read TIGHT. `c9d9bc1` closes it AGAINST THE ADVERSARIAL HARNESS. NEW `extract/terminus_coordinate_observer.py` (name-free, pure geometry, imports only read-only extract/ingest readers) exposes **`TERMINUS_DRAWN_COORDINATE`** (a COMPACT drawn shape at the bound station via `PlanPdf.vector_segments`; uniqueness-mandatory → DRAWN_COORDINATE_BOUND / NO_DRAWN_COORDINATE / AMBIGUOUS_DRAWN_COORDINATE / NOT_SOURCE_BOUND; never guessed, never the offset label-text anchor) + **`ENDPOINT_TO_TERMINUS_2D_TIGHTNESS`** (2-D run-terminal→coordinate gap; UNMEASURABLE when no coordinate), composing the PRESERVED **`BRANCH_PATH_UNIQUENESS`** + **`ENDPOINT_TO_RUN_COORDINATE_TIGHTNESS`**. It EXCLUDES detected symbol segments from the run graph so a symbol at an endpoint can't weld into a false fork. Additive `spanning_run_terminals` helper in `run_geometry_observer.py` (the spanning run's terminal (x,y); does NOT change `observe_run_geometry` verdicts — 17 prior tests byte-green). `harness/synth.py` adds `plan_symbol_clean_with_notes` + `plan_symbol_y_misaligned_with_notes` + `_structure_symbol` (name-free, NOT in `_CATALOG`). Proof (`tests/test_terminus_coordinate_observer.py`, 19 tests; 3-critic adversarial verify = SOUND): clean drawn-symbol → 2-D TIGHT/verified; y-misaligned → station-x TIGHT (fooled) BUT 2-D LOOSE → rejected via `ENDPOINT_TO_TERMINUS_2D_TIGHTNESS` (the new INDEPENDENT catch); missing coordinate → UNMEASURABLE, NOT promoted + NOT rejected; fork → branch rejects; NOT_SOURCE_BOUND honest; read-only + no-placement-import + name-free. **NO eligible fresh non-recognized package exists in the repo (only the recognized work corpus + synthetic) → validated on the adversarial harness ONLY, NO real-package generalization claimed; `observe_package_terminus_geometry` is the read-only validation PATH for future packages.** **STILL MISSING:** real/fresh-package validation; CLASS-VERIFIED or LEADER-TRACED structure-coordinate extraction for cold packages (cold-lane binding is GEOMETRY-ONLY — `structure_position.resolve_structure_position` is the class-verified analog but needs a CAD layer table + class fills the cold lane lacks); cross-sheet continuation remains a caveat. **G4 AUTO REMAINS BLOCKED until Patrick explicitly approves a later AUTO gate.** See [[hot]]/[[current-sprint]]/[[log]] continued 90 + [[cold-package-evaluation-harness]].
> **continued 89 (2026-06-30) — Track B: the G4-blocking missing observer signals now EXIST and are PROVEN against the adversarial harness (read-only/observer; NO AUTO, NO `_cap_review`/renderer/placement/status change).** Backend `feat/truelinev2` **`38815b2`→`b5cba2f`(docs-only START_HERE continued-88 bump)→`7c8a659`** (`product: read-only branch-uniqueness + endpoint-tightness geometry observer (no AUTO)`, pushed `origin/feat/truelinev2`, synced 0/0). **Backend `origin/main` `068a279` UNTOUCHED; cold-package matrix 11/11 + deterministic 50/58 + `DETERMINISTIC_AUTO=49` UNCHANGED; full v2 1937→1954 passed / 4 skipped; no new production flag; P5 PAUSED; web/staging UNTOUCHED.** The continued-88 G4 central finding — the wrong-branch FORK is indistinguishable from the clean positive (no branch-uniqueness signal), so a known-negative passed the design-only oracle and G4 AUTO was NOT safely implementable from current data — is now CLOSED **against the adversarial harness** by `7c8a659`, the recommended next gate. **(1) Exposure (smallest production touch, observational):** `extract/generic_geometry.py` RETAINS the raw near-band drawn segments the generic lane already computes but discarded (union-find welding + x-sorted polyline collapse a fork into one opaque run; only a rival COUNT survived) + a read-only `band_segments_for(sheet)` accessor — never read by placement/render; the generic dialect is never in the deterministic 50/58 path. **(2) Observer** `extract/run_geometry_observer.py` (name-free, pure geometry, imports NO contracts/match/render/api/store): **`BRANCH_PATH_UNIQUENESS`** from the reconstructed segment graph (degree≥3 junction → FORKED; ≥2 spanning components → RIVAL_RUNS; loop → FORKED; none spans → NO_SPANNING_RUN; single simple chain → UNIQUE) + **`ENDPOINT_TO_RUN_COORDINATE_TIGHTNESS`** = run-terminal-to-bound-endpoint gap in the PROVEN station-x axis (`axis.x_at`; >15 → LOOSE; unbound endpoint → UNMEASURABLE, never guessed). **(3) Adversarial proof** (`tests/test_run_geometry_observer.py`, 17 tests; 3-critic SOUND verify): wrong-branch fork `g4geo-06` FLIPS from the recorded current-data oracle PASS (`oracle_would_pass=True`, the documented G4 false positive) → observer-REJECT via `BRANCH_PATH_UNIQUENESS`; clean positive `g4geo-01` still passes (UNIQUE+TIGHT); a NEW `plan_offset_run_with_notes` (synth.py, NOT in `_CATALOG` → cold matrix untouched) is caught ONLY by `ENDPOINT_TO_RUN_COORDINATE_TIGHTNESS` with branch UNIQUE — the two signals are INDEPENDENT; parallel/broken/overshoot/undershoot/baseline rejected; the multi-sheet negative stays rejected by the EXISTING cross-sheet signal (observer is single-sheet); the augmented gate passes ONLY the true positive. Existing `g4_geometry_fixtures.py` / `test_g4_geometry_validation.py` UNTOUCHED — the historical documented gap is PRESERVED; the observer closes it SEPARATELY. **STILL MISSING (reported, not closed):** real/fresh diverse-package validation has NOT happened; STRUCTURE-COORDINATE EXTRACTION (bind a terminus to the structure symbol's drawn x,y) is still missing for true 2-D endpoint tightness — current tightness is station-x ONLY (an off-band structure could still read tight); cross-sheet continuation remains a caveat. **G4 AUTO REMAINS BLOCKED until Patrick explicitly approves the next gate** (real-package validation + structure-coordinate extraction). See [[hot]]/[[current-sprint]]/[[log]] continued 89 + [[cold-package-evaluation-harness]].
> **continued 88 (2026-06-30) — Track B cold-package source-evidence arc: G3 terminus DISPLAY (route + web panel, browser-proven) → terminus fixtures 3→10 → G4 AUTO DESIGN doc + PREFLIGHT fixtures → all THREE source-bound terminus binders (structure-label + station-callout + matchline-boundary) → G4 GEOMETRY-vs-ENDPOINT validation harness. ALL read-only/observer; NO AUTO, NO `_cap_review`/renderer/placement change.** Backend `feat/truelinev2` **`6f2a8ef`→`3e19fd4`→`31ec7fa`→`2524740`→`ab3bad7`→`558af39`→`04be2ce`→`38815b2`** (all pushed `origin/feat/truelinev2`); web `main` **`811532f`→`48ddab9`→`1a2f822`** (pushed). **Backend `origin/main` `068a279` UNTOUCHED. Cold-package matrix 11/11 UNCHANGED; deterministic 50/58 + `DETERMINISTIC_AUTO=49` UNCHANGED; full v2 1891→1937 passed / 4 skipped; no new production flag; P5 PAUSED.** **G3 (`3e19fd4` route + `48ddab9` panel):** read-only `contracts/terminus_report.py` (`terminus_evidence_report`) + default-OFF `GET /v2/product/jobs/{job}/terminus-evidence` (separate read path, NEVER the placement orchestrator — the observer-only forbidden-import guard holds) + web `ProductTerminusEvidence.tsx` (green source-proven vs amber from-bore-log-row, plain-English blockers, never implies AUTO). UI **browser-proven** via an isolated route-stub page (real panel + real backend data; the earlier "Connecting…" stall = Next-16 cross-origin dev block on `127.0.0.1` vs `localhost`, NOT a panel bug). **Terminus fixtures 3→10 (`31ec7fa`):** symmetric one-bound, ambiguous, bare-callout, offset-note (no over-bind), multi-sheet, depth/BOC carried-but-inert, route-geometry-no-termini. **G4 (`2524740` doc + `ab3bad7` preflight):** `truelinev2/docs/G4_TERMINUS_AUTO_DESIGN.md` (smallest-safe = a SEPARATE default-OFF post-placement gate emitting a DISTINCT `SOURCE_BACKED_TERMINUS_AUTO`, NEVER an `_cap_review` edit; the real cap is `_place_generic` building REVIEW + the candidate→tier map needing `AUTO_SELECT`) + 7 preflight fixtures (lone positive + 6 negatives) with a DESIGN-ONLY test oracle. **THREE source-bound binders built (all cold-lane-guarded — `select_dialect` None asserted; the callout/matchline grammars avoid `STA a TO STA b`/`DIRECTIONAL BORE`):** `558af39` `extract/callout_anchor.py` (PRINTED_STA_CALLOUT, exact span, refuses bare/proximity/ambiguous, callout-vs-structure CONFLICT) + `04be2ce` `extract/matchline_anchor.py` (MATCHLINE_BOUNDARY_STATION, reuses `matchline_join.see_sheet_crossings` BILATERAL CONFIRMED tier, refuses unilateral/sheet-mismatch/proximity). **G4 GEOMETRY VALIDATION (`38815b2`):** 8 adversarial fixtures (both endpoints source-bound, geometry varies) + a design-only oracle. **CENTRAL FINDING (test-asserted):** 6/7 negatives ARE caught by current signals (rivals→MULTIPLE_PLAUSIBLE_RUNS; broken/baseline→ABSTAIN; overshoot→DRAWN_EXTENT_EXCEEDS_BORE_SPAN; undershoot→PARTIAL/LOW; multi-sheet→PARTIAL_CROSS_SHEET_REVIEW) **but the WRONG-BRANCH FORK is indistinguishable from the clean positive** (no branch-uniqueness signal) → a known-negative passes the oracle → **G4 AUTO is NOT safely implementable from current data.** **NEXT GATE (recommended): read-only branch-uniqueness / endpoint-to-run-coordinate observer in the generic lane (no AUTO/`_cap_review`/placement change), THEN real/fresh-package validation.** Hector v1-parity PARKED (v1-style redline info; depth+BOC; clickable stations; station-click details). Web staging for Hector intentionally NOT redeployed during G3/G4 (may still serve `811532f`). See [[hot]]/[[current-sprint]]/[[log]] continued 88 + [[cold-package-evaluation-harness]].
> **continued 87 (2026-06-29) — Track A Hector handoff COMPLETE (web `fix/hector-blockers` merged to web `main` `811532f`; Cloudflare Access One-time-PIN enabled; staging live for Hector) → Track B cold-package (non-recognized) ENGINE arc shipped read-only-first.** Backend `feat/truelinev2` **`a90c7be`→`9284fa6`→`5cb621d`→`d3f77af`→`6358cda`→`6f2a8ef`** (all pushed `origin/feat/truelinev2`); web `main` now **`811532f`** (merged + pushed). **Backend `origin/main` `068a279` UNTOUCHED (nothing merged to backend main). Deterministic 50/58 + `DETERMINISTIC_AUTO=49` UNCHANGED (frontier-lock 14/14, byte-identical); full v2 suite 1880→1891 passed / 4 skipped; no new production flag; NO `_cap_review`/renderer/named-dialect change; P5 PAUSED.** **TRACK A:** owner accepted the continued-86 staged build; surgical `--ff-only` merge web `fix/hector-blockers` (`8918e9a`→`be3c8b6`→`0eb357b`→`811532f`) → web `main` `763019c`→`811532f` (exact smoke-tested SHA). Public Vercel re-verified safe/disconnected (product API off; landing/intake "not connected"; `/map` 404). Cloudflare Access blocker = login-METHOD misconfig (account-member "Sign in with Cloudflare" only → Hector couldn't get a code) — a Zero-Trust CONFIG issue not code (no CF API token here → owner action); owner enabled One-time PIN (email code) + reached OTP → Hector access live (allow policy `morning-river-d67d`, emails-only, not broadened). A transient `404` the owner saw mid-edit root-caused read-only to a browser-cached/transient response (infra healthy: edge 302, local 200/200). `B-FR-HECTOR-READINESS-1` → RESOLVED. Hector link `https://staging.fieldroute.io/` (OTP). **TRACK B (read-only first, the non-recognized engine — deferred since c59):** `9284fa6` cold-package evaluation harness (`truelinev2/harness/`: drives ARBITRARY packages through the REAL product path with a configured-but-EMPTY registry so the recognized shortcut can't fire; scores ABSTAIN/REVIEW/AUTO on path+named-blocker; correct-abstain=PASS; name-free) → `5cb621d` expanded 4→11 fixtures + confidence band/correction → `d3f77af` **engine #1 over-placement guard** (generic lane: only-candidate-is-full-sheet-baseline → ABSTAIN `NO_DRAWN_RUN_OVER_SPAN` not a LOW placement on the survey baseline; 50/58 byte-identical) → `6358cda` **engine #2 split coarse abstain** (`detect_blocker()` → `NO_STATION_AXIS`/`INSUFFICIENT_AXIS_QUALITY`/`NO_WELDABLE_RUN`; diagnostic, no placement change) → `6f2a8ef` **terminus extractor G1+G2 OBSERVER-ONLY** (design accepted via 5-agent archaeology+critique → Option B = REVIEW-only enrichment first, AUTO deferred; `extract/terminus_evidence.py` source-backed per-endpoint model + `extract/terminus_extractor.py` reusing `structure_anchor.bind_end_structure_note`/`PlanPdf.lines`/bore-log to bind a printed endpoint else a named missing-evidence blocker, never inferring; 3 name-free seed fixtures + plain-notes/screenshots authoring guide, no Excel; **NOT wired into `run_product_redline`** — test-asserted; cold-package matrix stays 11/11). **NEXT GATE (owner): approve G3 (REVIEW-only terminus evidence display, no status change) OR direct otherwise. DEFERRED: G4 AUTO promotion (needs `_cap_review` rework + per-source-type confidence table + blocker truth table + both-endpoints-source-bound). Hector v1-parity feedback PARKED (v1-style redline info; depth+BOC; clickable stations; station-click details). Pre-existing `brenham`/`NEXTLINK` contract/extract name-leaks still flagged, untouched.** See [[hot]]/[[current-sprint]]/[[log]] continued 87 + [[fieldroute-cloudflare-staging]] + [[truelinev2-product]].
> **continued 86 (2026-06-29) — Owner behind-Access smoke FAILED continued-85's "resolved": found a sheet-resolution bug + v1-parity gaps. THREE fixes shipped + pushed (NOT merged); `B-FR-HECTOR-READINESS-1` REOPENED/OPEN.** Backend `feat/truelinev2` `971dec8`→`51096ba`→`e3bc716`→**`a90c7be`** (pushed `origin/feat/truelinev2`); web `fix/hector-blockers` `8918e9a`→`be3c8b6`→`0eb357b`→**`811532f`** (pushed `origin/fix/hector-blockers`); **`origin/main` `068a279` + web `main` `763019c` UNTOUCHED (NOT merged); deterministic 50/58 + `DETERMINISTIC_AUTO=49` UNCHANGED (ZERO engine/render change); no new production flag; P5 PAUSED.** **(1) Sheet-resolution fix (`51096ba`/`be3c8b6`) — OWNER-CONFIRMED working:** the mark-route fallback conflated the construction-sheet LABEL (`7 OF 30`) with the PDF page INDEX (`7 of 43`), opening PDF page 7 (a `TYP-6` typical-details sheet) instead of the plan sheet at PDF page 20. NEW name-free `truelinev2/ingest/sheet_label_index.py` (title-block resolver: per-page `N OF M` with modal-total filter → CONSTRUCTION_PLAN; `TYP-n` → TYPICAL_DETAILS; else OTHER; `resolve(ref)→pdf_page`); `get_plan_page_metadata` exposes per-page `construction_sheet_number`/`plan_sheet_label`/`sheet_type`/`is_plan_sheet`; web `ProductSourceAnchorCapture` resolves refs→PDF page, defaults/submits the resolved page, shows the evidence, flags non-plan pages; manifest `source_sheets`/artifact `sheet` carry the construction label (closeout no longer mislabels). `page_number` stays a PDF page index everywhere. Resolver lives in `ingest/` (the render-adapter import guard forbids `extract/`). Derived offset = config `sheet_offset` 13. Owner re-smoke: sheet 8 OF 30 → PDF p21 ✓. **(2) KMZ/Google-Earth fidelity (`e3bc716`/`0eb357b`):** `gis_route.load_job_route_kmz` now RE-SERVES the uploaded KMZ FAITHFULLY (a `.kmz` returned verbatim, a `.kml` packed) instead of the flat rebuild that emitted generic "Point" pins — preserves the customer's OWN folders/styles/names/descriptions (proven on the real Brenham design KMZ: 14 folders + 49 styles + 1115 descriptions, zero "Point"); redline stays pixel-only (never added). **(3) Web UX (`0eb357b`):** `PlanPageViewer` "Enlarge to mark" fullscreen zoom(1–5×)+scroll-pan modal (click-to-mark resolution-independent); redline proof clearly labeled "Placed redline proof — drawn from your N marked points" + next-action; "Print / save review" promoted to an obvious button; GE copy updated. **(4) Per-job OPERATOR-ENTERED pricing (`a90c7be`/`811532f`) — owner-selected honest model:** NEW `truelinev2/contracts/job_pricing.py` (blank rates required, NO fake/default dollars; footage = SERVER quantity via NEW public `billing_summary.job_effective_footage`; totals = footage×rate + Σexceptions, server-computed; provenance `OPERATOR_ENTERED_UNVERIFIED` + disclaimer; DISTINCT from the configured-rate billing model); GET/POST `/v2/product/jobs/{job}/operator-pricing`; `closeout_pdf` §12 "Operator-Entered Pricing (UNVERIFIED)"; web `ProductOperatorPricing.tsx` in the Export/Closeout review. v1 archaeology (Workflow) confirmed v1 had NO structure-preserving KMZ re-exporter (re-serving is better) + v1 pricing was operator-typed/client-only (the unsafe pattern v2 replaced) + v1 photo/EXIF was honest but needs a v2 photo-upload+EXIF path (gated, NOT built). **Full v2 suite 1880 passed / 4 skipped** (+ new `test_sheet_label_index`/`test_job_pricing` + gis/closeout/route-set/naming-guard updates). Gated staging REDEPLOYED (backend `a90c7be`, web `811532f` staging-base; same-origin cloudflared `47f42c57…`; local 200/200, **edge 302**); deployed proofs: sheet 7→PDF page 20 on real Brenham, KMZ folders/styles preserved, pricing GET=blank/POST=computed/negative→400, closeout PDF §12. **B-FR-HECTOR-READINESS-1 stays OPEN — do NOT merge `fix/hector-blockers`, do NOT add Hector to Access, do NOT declare ready until owner's behind-Access smoke passes.** **NEXT GATE (owner): behind-Access smoke of all fixes (pricing card / GE folders / Enlarge-to-mark / redline-proof / Print / sheet-resolution regression) → if good, merge `fix/hector-blockers` + add Hector to the `morning-river-d67d` Access policy.** Future: configured-rate-sheet authoritative billing; EXIF photo + true-georef overlays (gated). See [[hot]]/[[current-sprint]]/[[log]] continued 86 + [[bugs/current-bugs]] B-FR-HECTOR-READINESS-1.
> **continued 85 (2026-06-29) — PR#4 guided 6-step stepper SHIPPED → entire web PR stack MERGED to `main` (dependency order, NOT PR-number order) → gated staging REDEPLOYED from merged `main` (both audits GREEN) → owner walkthrough found 4 Hector send-blockers → HOTFIX `8918e9a` pushed (NOT merged).** **PR#4 = the approved guided 6-step stepper** (Project/Upload/Route map/Bore logs/Redline proof/Export; body shows ONE step; done/current/upcoming/locked rail; silent project auto-init — no "Set up workspace"/slug/disabled button; non-recognized abstain reframed CALM + raw codes behind Technical details + the EXISTING "mark the route" capture surfaced as the next step; photos "stored for reference only"; route-only GE KMZ): web `feat/guided-stepper-onboarding` **`3ff6ac5`** off PR#3 `c20ee70`; tsc/lint/`next build` clean; brand-new-customer audit (fresh tenant, empty store, NO recognized registry → genuine non-recognized path) + recognized multi-bore audit both GREEN end-to-end (PDF `%PDF-`/ZIP/route-KMZ real). **MERGE (GitHub numbers ≠ team labels): GitHub PR #1=audit `86bfa97`, #2=source-package `f77b727`, #4=owner-mode `c20ee70` (opened last), #3=stepper `3ff6ac5`** → correct merge order is by DEPENDENCY **#1→#2→#4→#3** (NOT ascending). `gh` unavailable → merged via the stored git credential (owner PAT, GitHub REST); PR#1/#2 were DRAFTS (had to `markPullRequestReadyForReview`); no branch protection on `main`; merge-commits + explicit base-retarget (no branch deletes). Merge commits `6115dd6`→`72df7f7`→`74c8d9b`→**`763019c` (final `main`)**, 0 open PRs. **Vercel auto-deploys `main` → PRODUCTION CHANGED** (final prod build = success); the public `trueline-web-experience.vercel.app` is DISCONNECTED/safe (product API off → "Workspace not connected", no data/upload, internal+legacy routes 404). **Gated staging REDEPLOYED from merged `main`** (web staging-base `763019c`, then hotfix `8918e9a`; backend `feat/truelinev2` **`971dec8` UNCHANGED**; same-origin path-split cloudflared `47f42c57…`; **Access 302** on `/`,`/intake`,`/v2`; curated 4 jobs [recognized-multi/demo-general-upload(-ambiguous)/completed-redline-showcase; no ODOT]; supervisor self-heal Ready); both audits GREEN on the staging build (driven local-base — the agent can't browser-drive through the Access edge; behind-Access smoke = owner's). **HECTOR HOTFIX `8918e9a` on `fix/hector-blockers` (off `main` `763019c`), pushed, NOT merged — 4 send-blockers, NO engine work:** (A CRITICAL) the manual mark-route fallback opened the COVER/title page → now defaults the plan page to the bore-log row's own `sheetRefs` (+ recognized `renderSheets`), shows "Suggested sheet(s)" chips + an obvious selector + pre-fills the station identity; verified it opens on **sheet 7 not the cover**, closeout UNBLOCKED end-to-end (`ProductSourceAnchorCapture`); (B) the Bore logs step lists ALL uploaded bore-log files, one reviewed-bore-log PER file (rbl-main/rbl-2…), file-linked rows + a "Plan sheet(s)" column + a Primary marker (`ProductReviewedBoreLogGate`); (C) the upload type radio → 4 clear buttons/cards Plan PDF·KMZ/KML route·Bore log·Photos (`ProductUploadPanel`); (D) Route map step → **"Route context (route-only)"**, PDF redline is the proof, NO overlays yet (kept playback+GE) (`stepperSteps`/`ProductRouteMap`). Future map/photo direction recorded in web `docs/FUTURE_MAP_PHOTO_REQUIREMENTS.md` (EXIF geotag photos, source-backed redline/station overlays, clickable v1 station info boxes, GE redlines only with true georeferencing). Re-audit GREEN (cold path with TWO bore logs → mark on suggested sheet 7 → render → export; recognized path no regression). **Flags: no new production flag; backend `origin/main` `068a279` untouched; deterministic 50/58 + `DETERMINISTIC_AUTO=49` UNCHANGED (zero engine/render change); P5 PAUSED.** B-FR-HECTOR-READINESS-1 blockers RESOLVED (pending owner behind-Access confirmation). **NEXT GATE (owner): behind-Access smoke on `staging.fieldroute.io` → if good, merge `fix/hector-blockers` to `main` + add Hector's email to the `morning-river-d67d` Access policy.** See [[hot]]/[[current-sprint]]/[[log]] continued 85 + [[bugs/current-bugs]] B-FR-HECTOR-READINESS-1 + [[fieldroute-cloudflare-staging]].
> **continued 84 (2026-06-29) — Owner-Mode simplification (A/B/C) DONE + reconciled into the parallel PR stack as PR#3; backend C1 PUSHED; Hector-readiness audit = NOT Hector-ready; UX-restructure plan PENDING owner approval (no code).** Owner: the site still feels like a dev board / one giant scroll — get the new-customer flow understandable BEFORE returning to the real engine problem (non-recognized files). Shipped A/B/C this session: **A** owner-workflow declutter (one status surface; jargon/codes/ids behind Advanced; drop numbered/lock sidebar + env footer), **B** hero-map route-progression playback (draw-in + Play/scrub on the REAL KMZ map, route-only; reused the dash trick, NOT the deleted mock map), **C** Google-Earth route KMZ export + source-backed street names (verbatim from KMZ `<description>`, never invented) + C2 georeferencing design doc. **Backend `feat/truelinev2` `79d4424`→`971dec8` PUSHED** to `origin/feat/truelinev2` (repo remote = `osp-redlining.git`): additive `GET …/gis-route/download` + `gis_route` street labels + `truelinev2/docs/REDLINE_GEOREFERENCING_DESIGN.md`; **136 product-API/gis/kmz/naming + 26 frontier-lock tests pass**; deterministic **50/58 + DETERMINISTIC_AUTO=49 UNCHANGED**; no new prod flag; `origin/main` `068a279` untouched. **Web: a SEPARATE Claude shipped PR#1 (`86bfa97` audit) → PR#2 (`f77b727` `feat/fieldroute-simple-source-package-review`: consolidate onto `/intake`, delete legacy mock app incl. `components/map/*`+`lib/geometry.ts`, brand/jargon sweep), stacked + open + green.** My web A/B/C (local `main` `1dad09b`+`5b44810`) were RECONCILED: cherry-picked onto a branch off PR#2 → **PR#3 `feat/owner-mode-simplification-ge-export` `c20ee70`** (`fb4f31b` A + `c20ee70` B+C), PUSHED + **PR#3 OPENED stacked on PR#2** (Vercel preview green); local `main` **RESET back to `origin/main` `429df79`** (NOT pushed — never bypass the stack). 5 overlap files auto-merged; only Sidebar conflicted (env-footer → REMOVED per owner, also satisfies audit FR-AUDIT-011). Part B inlined the draw-in trick so PR#2 deleting the mock hero-map didn't break it. **Hector-readiness audit (PR#3 local-base, fresh `new-customer-audit` tenant, real KMZ/PDF/xlsx/photos, recognized + UNrecognized): VERDICT = NOT Hector-ready** (`B-FR-HECTOR-READINESS-1`). Good: uploads, extraction-first bore rows, map+streets, redline step-through previews, GE route export. Blockers: (1) **non-recognized plan = scary dead-end with raw codes `(UPLOADED_CORPUS_NOT_RECOGNIZED)`/`(ENGINE_ABSTAINED)` + no next step** (the path Hector's real plan hits); (2) **new-customer entry maze** (Set up workspace→Create project, random slug, disabled button, 3 cards); (3) **giant single-scroll 8-section page**; + photos copy overstate, recognized path shows corpus `payload.xlsx`, Access-gated badge, landing jargon. Owner correction → **UX-restructure plan PRODUCED (awaiting approval, no code):** guided **6-step STEPPER** (Project/Upload/Map/Bore logs/Redline proof/Export), one screen per step, progressive disclosure, non-recognized fallback = surface the EXISTING human "mark the route" capture as the next step (NO engine work), photos "stored reference only", GE route-only; borrow Fieldwire/Ocius-X IA (function, not assets). **NEXT GATE (owner): approve the UX plan → implement as PR#4 stacked on #3 (or fold into #3) → re-run the Hector audit → merge #1→#2→#3(→#4) → deploy gated staging → re-audit on `staging.fieldroute.io` → then send Hector.** Guardrails held: no Phase 4, no engine work, no fake georef/streets/coords, no deleted-route resurrection, ODOT out of featured flow, Brenham seed-only, gated staging NOT redeployed (still pre-PR build), supervisor running, nothing deployed. See [[hot]]/[[current-sprint]]/[[log]] continued 84 + [[owner-mode-simplification-and-kmz-export]] + [[fieldroute-council-v1-mirror]].
> **continued 83 (2026-06-28) — COMMITTED + PUSHED (both repos): Brenham-primary owner walkthrough — Phase 1 honest labels + Phase 2 de-ODOT + Phase 3 (read-only bore-log extraction + multi-bore recognized step-through) + UX-parity (clean route map + v1-style bore info cards).** Owner direction: stop featuring ODOT; make the REAL Brenham package the primary owner upload→redline→map→closeout walkthrough; keep the product GENERIC (Brenham = seed/test/walkthrough DATA only, never hardcoded in reusable code/routes/schemas/env/labels). Backend `feat/truelinev2` `f0c9ffe`→`bef66ed`(read-only bore-log extraction)→`1ed89cb`(multi-bore step-through + de-ODOT)→**`79d4424`**(date/crew/boc info-card fields); web `main` `81dd7ba`→`e6d772a`(FieldRoute branding + drop ODOT jobs)→`e557c5a`(readiness + extraction-first gate + step-through)→`de880da`(step-through placed-gate fix)→**`429df79`**(clean map + bore info card); all PUSHED (ahead/behind 0/0). `origin/main` (backend) UNTOUCHED `068a279`; engine/renderer/fixtures/anchors + deterministic **50/58 byte-identical**; NO new production flag; P5 PAUSED; Vercel HELD; full v2 **1856 passed / 4 skipped** (+8). **Gap analysis (9-agent Workflow):** located the Brenham assets (KMZ 1116 features; plan 43 sheets; 58 `bore_log*.xlsx` all parse); the COLD uploaded REVIEW lane mostly ABSTAINS on Brenham (its 50 proven redlines live in the RECOGNIZED path) ⇒ recognized multi-bore is primary, the existing name-free general-upload demos carry the REVIEW/correction lane. **Phase 3A:** name-free `truelinev2/extract/borelog_rows.py` (`.xlsx`→UNTRUSTED `TABLE_IMPORT` rows, read-only via `load_borelog`, no fabricated confidence/geometry, NOT fed to placement) + default-OFF `POST …/reviewed-bore-logs/{rbl}/extract` + web **extraction-first** gate (manual entry under Advanced). **Phase 3B:** `recognized_corpus_handoff` multi-log job-local bundle (all engine-ready rbls; read-only committed renders, sha256-verified, 50/58 byte-identical) + per-bore `recognized_logs` in evaluate; web `ProductBoreStepThrough` (bore list → click → redline on correct plan sheet → next/prev → "pixel-only on sheet N — not georeferenced" badge + v1-style bore info card: file/log id/stations/span/print/sheets/date/crew/depth/boc/review/extraction/geometry/artifacts, "not available" for missing); seed `seed_recognized_multi.py` (5 logs); **`recognized-multi` = primary recognized job**; featured CAPPED to 5, shape scales to N. **UX-parity:** map **"Route only" default** (576-point KMZ source layer hidden) + "Show source points" toggle; `borelog_rows` additively reads optional date/crew/boc (name-free); `ReviewedRowView` exposes raw fields via the EXISTING reviewed-bore-log read (NO new endpoint). Two gated-staging redeploys, both browser-smoked (export PDF 29.4MB/ZIP 6.7MB; 0 console errors); edge **302** (Access); store curated-4 (no ODOT); self-heal Ready. Smoke-found+fixed: build needs `NEXT_PUBLIC_TL2_PRODUCT_API=1`; web `rbl-main` single-bore assumption (**B-FR-MULTIBORE-RBLMAIN-1**, closeout/gate show 1 of N; multi-bore truth = Project files + step-through). Honest by-design: map redline overlay route-only (not georeferenced); no fake coords/streets/confidence. Next (owner-gated, NOT started): re-walk; Phase 4 (source-backed street/route from KMZ `<description>` + committed Playwright smoke); broader visual redesign. See [[hot]]/[[current-sprint]]/[[log]] continued 83 + [[brenham-primary-owner-walkthrough]].
> **continued 82 (2026-06-27) — COMMITTED + PUSHED (both repos): FieldRoute "Claude Council" → v1-parity seam fixes + full "demo" scrub + neutral-alias ID masking + legacy-route redirect + bore-log made SOURCE/REVIEW (not data-entry) + generic-lane MEDIUM cap.** A 9-role multi-agent **Workflow** produced 7 council deliverables in **`truelinev2/docs/council/00-06`** (master plan, council report, public web research corpus [21 fetched public HDD/fiber/permit sources], v1 mirror plan, engine behavior spec, owner smoke script, live audit findings). **Verdict (live-confirmed by an isolated-stack browser audit): the v2 engine + backend are honest and solid; FieldRoute failed only at the user-facing SEAMS.** Backend `feat/truelinev2` `9ee706a`→**`f0c9ffe`** (pushed; `7f89107` council docs, `e6c9bb3` generic Lane-B capped at **MEDIUM never HIGH** [`_GENERIC_MAX_CONF=0.70`] + regression test [C2/C6], `f0c9ffe` owner-doc slug scrub; full v2 **1848 passed / 4 skipped**; deterministic **50/58 UNCHANGED**; `origin/main` `068a279`). Web `main` `e418ef1`(continued-81, NOW PUSHED)→`32a2450`(demo scrub + v1-mirror seam fixes: front door Home/New project/Projects, guided→closeout link, loop-breakers [W2 engineReady-null, W4 twin "Create project" → "Set up workspace"], closeout honesty [D1 no phantom Photos, D2 honest sign-off, D4 Print demoted, D7 Final review checklist], W1 mis-file guard + "stored not auto-read", deleted dead billing-leak components, ?section= deep-link, NEW stale-assemble-error fix)→`5520cc5`(**ID masking**: `jobLabels.ts` jobTitle/jobAlias/resolveJobId/rawToAlias + `middleware.ts` redirect raw `demo-*` `?job=`→neutral alias `project-clean-placement`; Diagnostics "Project key"/Technical-details "Internal reference"/URLs all show aliases; export+seed filenames cleaned — store ids unchanged)→`f5a0ce4`(legacy contract-preview/mock routes `/closeout`,`/packet`,`/map`,`/plans`,`/feed`,`/evidence`,`/redlines`,`/projects/*`,`/settings` middleware-redirected to Projects)→**`81dd7ba`**(bore-log section **SOURCE/REVIEW-oriented**: manual row entry demoted to a collapsed "Advanced manual review (temporary fallback)"; seeded jobs show reviewed rows READ-ONLY + generate WITHOUT manual entry; a blank upload shows an HONEST "extraction-pending" state — automatic bore-row extraction from uploads NOT implemented, **no fake rows/extraction**; section renamed "Bore log"), all PUSHED (web `origin/main` synced 0/0). **Verified:** tsc + `next build` clean (middleware compiled); isolated local-base browser smoke of every flow + **0 visible "demo" on every product surface** (Diagnostics/Technical-details expanded, server HTML, all URLs); legacy routes redirect to Projects; bore-log seeded=read-only-ready / blank=honest-blocker; recognized/clean/ambiguous/export-gate flows all work. **Staging REDEPLOYED** each step (same-origin `https://staging.fieldroute.io` build, source store re-seeded clean filenames, supervisor `-Restart` → backend 200 / web 200 / **edge 302/Access**). No new production flag; engine/renderer/fixtures/anchors/`origin/main`/deploy untouched; P5 PAUSED; Vercel HELD. **Paused for Patrick's behind-Access owner smoke** (`05_owner_smoke_script.md`). Tier-6 polish NOT started (owner gate). Deferred owner-approval: real bore-log OCR/xlsx extraction (the honest "extraction-pending" is the placeholder), embedded closeout photos, role-gated Approve (with auth), tiled basemap. LESSON: HTTP 200 is not proof — drive the real browser flow + LOOK. See [[hot]]/[[current-sprint]]/[[log]] continued 82 + [[fieldroute-cloudflare-staging]] + [[generic-geometry-review-lane]].
> **continued 81 (2026-06-26) — COMMITTED LOCALLY (NOT pushed): v1-MIRROR single-page job workspace (web-only UX restructure; NO new backend).** Owner mission: the v2 app worked but felt like a developer console split across disconnected pages — mirror v1's ONE-job-ONE-page workflow. Web `main` `a2a0aaa`→**`e418ef1`** (`product: v1-style single-page job workspace`, 8 files, +744/−706); **LOCAL commit NOT pushed** (awaiting owner OK). **Backend UNTOUCHED `9ee706a`** (zero new backend capability — composed entirely from existing reads); `origin/main` `068a279`. The workspace (`/intake?workspace=1&job=`) is now ONE coherent scrollable page: a **job header** (readable name + stage + files summary + redline/closeout status + a SINGLE derived primary next action; raw id muted) then the sections IN ORDER — Project files (cards: uploaded/missing/what-for/next-action) → Map/route → Bore logs (readable rows table + ready/needs-review chip; segment-groups + engine-readiness gate behind a disclosure) → Redline (Generate + verdict + placed PNG + pointer) → Review & correct (the ONLY Accept/Reject + the auto-gated Correct) → Closeout review (NEW v1 review-before-print) → Export & print — each as `<section id="ws-*">`. The **sidebar is same-page anchors** with an IntersectionObserver scroll-spy (sidebar + body share state only via the URL). **One owner per action** (no duplicate buttons): Redline=Generate, Review=Accept/Correct, Closeout=Assemble, Export=Download/Print. **Closeout review** composes existing reads (project summary, uploaded files, redline evidence images, bore-log rows, quantities — billing QUANTITIES only/no dollars, package contents incl/omitted, honest KMZ note, disclaimer). **Print/Save** = `window.print()` + an `@media print` block in globals.css scoped to `#closeout-print` (sidebar/topbar/banner/other sections hidden); the server closeout PDF stays the authoritative deliverable. Dev plumbing (raw slots/provenance/codes/sha/internal statuses) collapsed behind Technical details/Diagnostics. `ProductWorkflowPanel` slimmed to Generate-only; `ProductReviewCandidates` got a `hideGenerate` prop so the GUIDED demo (which owns its own Generate) is unbroken. **ALL 3 FLOWS BROWSER-PROVEN single-page** (local-base Playwright, 0 console errors, reload-stable): clean → Generate → HIGH → Accept → Assemble → ZIP/PDF 200; ambiguous → Generate → LOW 45% → mark route → Render → SUPERSEDED → Assemble → ZIP/PDF 200; recognized → Generate → placed → Assemble → ZIP/PDF 200. Print emulation verified (closeout-print visible; sidebar/others hidden). Guided demo (`/intake` chooser + `/showcase` 2 imgs + `/`) intact. tsc + `next build` clean. STAGING REDEPLOYED (staging-base web `e418ef1`, backend `9ee706a`; cloudflared up; Access **302** ×3; durability task re-enabled; 6 curated jobs, 3 targets pristine). **NEXT: push web `main` `e418ef1` on owner OK; then owner behind-Access walkthrough of the single page.** See [[fieldroute-cloudflare-staging]].
> **continued 80 (2026-06-26) — COMMITTED LOCALLY (NOT pushed): make FieldRoute work end-to-end like v1 (upload→redlines→review/correct→closeout/export) + DURABLE staging.** Owner mission: stop side quests, make the uploaded-project workflow actually usable. Backend `feat/truelinev2` `a3c27fa`→**`9ee706a`** (`product: persist human-corrected REVIEW + unify the export gate`); web `main` `5b4a8d8`→**`a2a0aaa`** (`product: corrected REVIEW flows to closeout (no dead-end) + cleaner workspace`). **BOTH commits LOCAL ONLY — NOT pushed** (awaiting owner go-ahead); `origin/main` UNTOUCHED `068a279`; engine/renderer/fixtures/anchors + deterministic 50/58 UNTOUCHED. Full v2 **1847 passed / 4 skipped**. **(1) CORRECTION DEAD-END FIXED (the core gap):** a LOW/wrong uploaded-REVIEW candidate corrected via the human-confirmed source-anchor render now SUPERSEDES the engine candidate (new terminal `REVIEW_SUPERSEDED` in `review_acceptance.py`; set ONLY after a source-anchor render SUCCEEDS, so a still-pending REVIEW never slips through). `product_workflow._review_gate` treats SUPERSEDED like ABSTAINED → the corrected redline ASSEMBLES + EXPORTS instead of dead-ending on "accept the engine candidate first"; `run_product_redline` echoes superseded (no re-accept). Web: the Review card reflects the corrected redline (shows the corrected stroke, hides the dead accept/correct controls, "Placement corrected" banner → Closeout/Exports), and `ProductSourceAnchorCapture` fires `onChanged` so slots refresh + Redlines/Closeout offer Assemble with no reload. **(2) EXPORT GATE UNIFIED:** the ZIP/PDF download routes share `export_gate` (un-accepted/rejected REVIEW → 409; recognized/AUTO/accepted/superseded/abstained pass). **(3) DURABLE STAGING (req H — the recurring 1033/dead-process failure):** gitignored ops harness `data\outputs\truelinev2\staging_smoke\ops\` — `staging-supervisor.ps1` (mutex-guarded, enforces EXACTLY ONE backend:8100 + web:3000 (pinned `-H 127.0.0.1`) + cloudflared tunnel, curl health, logs, restarts the dead) + `register-staging-task.ps1` + `run-supervisor-once.cmd`; a 5-minute **Scheduled Task "FieldRoute Staging Supervisor"** (`schtasks`, current user) self-heals + a **Startup-folder VBS** (`%APPDATA%\…\Startup\FieldRoute-Staging.vbs`) gives instant login recovery (At-Logon trigger needs elevation → the .vbs covers it non-elevated; `register-staging-task.ps1` is ready for an elevated owner run). PROVEN: killed web → task healed it in 3s, survives the task's exit. **(4) STORE CLEANED to 6 curated jobs** (3 DOD-flow: `demo-general-upload` clean-HIGH / `demo-general-upload-ambiguous` LOW+correct / `recognized-log9` deterministic + 3 guided-demo) — `job-xxxxxx` name-leak/stale clutter pruned; `clean_demo_store.py` KEEP fixed. **(5) UI product-shaped:** friendly job titles (raw id muted), "Project workspace" header (was "internal/not part of the guided demo"), uncertain REVIEW in Redlines points to the Review section, raw codes behind "Technical details". **ALL 3 FLOWS BROWSER-PROVEN** (local-base Playwright, 0 console errors, reload-stable): clean→HIGH→Accept→Assemble→ZIP 30KB+PDF 1.3MB; ambiguous→LOW 45%→mark route→Render→SUPERSEDED→Assemble→ZIP 28KB+PDF 1.3MB; recognized→DETERMINISTIC (2 PNGs)→Assemble→ZIP 1.86MB+PDF 6MB; guided `/intake`+`/showcase`+`/` intact. **STAGING REDEPLOYED + LIVE** (staging-base web `a2a0aaa`, backend `9ee706a`, cloudflared up; Access **302** ×4; durability task enabled). **NEXT: push both branches on owner OK; then owner behind-Access walkthrough of the 3 jobs.** LESSON: HTTP 200 is not proof — LOOK at the rendered stroke + drive the real browser flow. See [[fieldroute-cloudflare-staging]] + [[generic-geometry-review-lane]].
> **continued 79 / 79b (2026-06-25) — COMMITTED + pushed (BOTH repos): HONEST generic placement (coverage/ambiguity-graded confidence + "Correct redline placement" step) + accept→assemble workflow state-sync fix; staging REDEPLOYED ×2.** Owner placement-correctness mandate ("stop UX polish — make uploaded-project placement actually work; don't draw the wrong route / wrong page / wrong bore segment / overstate confidence"). Backend `feat/truelinev2` `96c0e10`/`02d5b78`→`9a54118` (`product: honest generic placement — coverage/ambiguity-graded confidence + correction signal`)→`5e83879` (ambiguous demo builder)→**`a4854a9`** (`product: workflow/redline echoes accepted REVIEW state`); web `main` `66636eb`→`dad8d36` (`product: surface honest placement confidence + a "Correct redline placement" step`)→**`5b4a8d8`** (`product: recognize an accepted REVIEW across Redlines/Closeout`). **(79 — honest placement):** the generic REVIEW lane drew the WRONG route + OVERSTATED confidence on real plans — reproduced on the REAL ODOT plan with named dialects FORCED off (the continued-78 fix was validated ONLY on a synthetic plan rigged to pass, masking the real failure): the name-free shape detector fragments ALL plan linework into **130–150 "runs"** and locked onto the partial **RED right-of-way (ROW) line** (53–68% cover) at **MEDIUM 0.58–0.68** (visually proven — drawn on ROW, not the bore); the `cover>=0.5` prefilter hid the 130+ rivals from the competition count. ROOT CAUSE is **semantic not geometric** (the bore is a CAD-layer / "DIRECTIONAL BORE" annotation; the "BORE" note is 140–480pt off the drawn line → NO reliable tight anchor). Fix (honesty-first, `_score_bore_run`/`_place_generic`/`_confidence`): coverage dominates (0.40); red 0.20→0.05; rivals counted over ALL span-overlapping runs (3+ → hard LOW; partial <85% → LOW/low-MEDIUM; sparse tick rows penalized); HIGH only on near-full coverage + tight extent + ZERO rivals; emits `GENERIC_PARTIAL_SPAN`/`GENERIC_MULTIPLE_PLAUSIBLE_RUNS`/`GENERIC_CORRECTION_RECOMMENDED` + runner-up alternatives. Real ODOT 118'/71' → **LOW + correction** (were MEDIUM); synthetic clean demo → **HIGH 0.85** clipped to the bore. **Correction path BUILT (web):** a LOW/correction candidate mounts the EXISTING human-confirmed `ProductSourceAnchorCapture` ("Correct redline placement" → `HUMAN_CONFIRMED_CONTROL_POINTS` dashed REVIEW). **(79b — workflow state-sync):** owner found a dead-end — accept a REVIEW candidate in **Review** → **Redlines** showed only "Generate" (no Assemble) + **Closeout** said "assemble in Redlines" → stranded. Backend already assembled off the persisted accept (`assemble_closeout_package`, any panel); fix = `run_product_redline` echoes `review_accepted`/`requires_acceptance=false` once accepted, and (web) **Closeout now OWNS Assemble** + **Redlines rehydrates** from persisted `slots.redlineManifest`/`exportPackage` ("A redline is already placed" + Assemble; survives a full reload). Engine/renderer/fixtures/anchors/`origin/main` `068a279` UNTOUCHED; deterministic 50/58 + named ODOT/Brenham byte-identical; no new prod flag. Full v2 **1845 passed / 4 skipped** (+5 tests). STAGING REDEPLOYED (backend `a4854a9`, web `5b4a8d8`); demo jobs `demo-general-upload` (HIGH+correct) + NEW `demo-general-upload-ambiguous` (LOW+correction) reseeded pristine; Access **302** verified; recognized-log9 → `RECOGNIZED_DETERMINISTIC` intact; browser-smoked ALL paths (Review-accept → Redlines & Closeout both Assemble incl. after reload → Assemble → Exports ZIP/PDF; fresh same-panel flow) **0 console errors**. Bugs `B-FR-GENERIC-PLACEMENT-OVERSTATE-1` + `B-FR-ACCEPT-ASSEMBLE-SYNC-1` → FIXED. **LESSON: a synthetic test rigged to pass proves nothing — reproduce on a REAL plan and LOOK at the stroke vs the source bore; HTTP 200 is not proof.** See [[hot]]/[[current-sprint]]/[[log]] continued 79 + [[generic-geometry-review-lane]].
> **continued 78 (2026-06-25) — COMMITTED + pushed (backend): BORE-AWARE generic placement (owner staging-smoke fix) + REDEPLOYED.** Backend `feat/truelinev2` `30c76aa`→**`96c0e10`** (`product: bore-aware generic placement — select the bore run, clip to the span, honest confidence`). The owner's staging smoke of the continued-77 generic lane caught a WRONG placement: it drew the FULL drawn run (a 200ft stroke for a 150ft bore, floating), picked runs by MIDPOINT (a survey baseline can win), and reported a fake 93%. Diagnosed visually (LOOKED at the rendered PNG, not just HTTP 200). Fixed: new `_place_generic` (adapter) does BORE-AWARE selection — score each drawn run for BEING the bore (extent matches the bore-log span + brackets the endpoints + red/proposed color + covers; a full-sheet survey baseline is heavily penalized) — CLIPS the stroke to the bore span (`StationAxis.x_at` inverse + `GenericGeometryDialect.clip_centerline_to_x`) so the redline spans EXACTLY the bore not the full run, and confidence is EARNED by placement quality (cap **0.85** — the lane INFERS which line is the bore, never near-certain/AUTO; a full-sheet alignment pick is capped **LOW** `GENERIC_PLACED_ON_ALIGNMENT_LINE_UNVERIFIED`). The generic lane no longer routes through `run_match`/`decide_by_extent`. Realistic demo plan (grid + full-sheet baseline + 2 utilities + a tight red bore) + diagnostic proof images (`truelinev2/proof/run_generic_placement_diagnostic.py`: source / ticks+axis / candidate runs+selection+clipped stroke / rendered crop) + regression tests (selects bore run over baselines / clips to span / baseline-only→LOW+unverified). Engine/renderer/fixtures/anchors/`origin/main` `068a279` UNTOUCHED; deterministic 50/58 UNCHANGED; no new prod flag. Backend full v2 **1840 passed / 4 skipped**. Browser-smoked the corrected UI (local-base): HIGH **85%** + evidence reasons ("drawn run length matches the bore-log span (100%)" / "brackets the bore start/end" / "drawn red") + clipped stroke ON the bore run + full Generate→Accept→Assemble→ZIP/PDF, **0 console errors**. STAGING REDEPLOYED: backend `96c0e10`, web `66636eb` (staging-base rebuilt after the local-base smoke), `demo-general-upload` pristine; Access **302** verified; recognized-log9 → `RECOGNIZED_DETERMINISTIC` intact. See [[hot]]/[[current-sprint]]/[[log]] continued 78 + [[generic-geometry-review-lane]].
> **continued 77 (2026-06-24/25) — COMMITTED + pushed (BOTH repos): GENERAL uploaded-project REVIEW placement — the name-free generic-geometry fallback (abstain-by-default CLOSED) + first staging deploy.** Backend `feat/truelinev2` `641f52a`→`57e698d` (`product: general uploaded-project REVIEW placement (name-free generic-geometry fallback)`)→**`30c76aa`** (`product: fit the generic station axis from the densest tick row + self-contained demo seed`); web `main` `7c04ef1`→**`66636eb`** (`product: surface generic-placement REVIEW confidence in the workspace`). A general uploaded plan that matches NO named (Brenham/ODOT) dialect no longer ABSTAINS by default — NEW `truelinev2/extract/generic_geometry.py` (`GenericGeometryDialect`, name-free, **NOT** in the global `_DIALECTS`) reconstructs a plausible drawn run from the plan's OWN generic vector geometry + station-label axis (color-agnostic shape gates + union-find dash welding + red-bucket boost + legend/title-block suppression + densest-tick-row axis fit) and produces a REVIEW candidate drawn on the plan, capped REVIEW, with graded confidence + reasons + warnings. Adapter `uploaded_corpus_engine_handoff` extended — the generic fallback fires ONLY when the named dialects return None/ABSTAIN, so the deterministic 50/58 path is PROVABLY byte-identical. Additive per-log `placement_candidate` manifest block (optional → recognized manifests byte-identical); `export_bundle` `status/placement_candidates.json` + `closeout_pdf` "REVIEW REQUIRED" banner carry candidate state; `review_acceptance` carries confidence. Engine/renderer/fixtures/anchors/`origin/main` `068a279` UNTOUCHED; 50/58 UNCHANGED; no new prod flag. Backend full v2 **1837 passed / 4 skipped** (1828 baseline + 9 new generic-lane tests). Proven end-to-end on the real ODOT plan (named dialects FORCED off) + browser-smoked (local-base): generate→generic REVIEW + confidence→accept→closeout→ZIP/PDF, 0 console errors. STAGING DEPLOYED: backend `30c76aa`, web `66636eb`, NEW demo job **`demo-general-upload`** (tenant `staging-smoke`; self-contained synthetic non-recognized plan + Brenham-flat bore-log via gitignored `data/outputs/.../staging_smoke/seed_general_upload.py` reusing committed `truelinev2/proof/seed_general_upload_demo.py`); Access **302** verified. NOTE: continued-78 corrected this lane's placement — read it first. See [[hot]]/[[current-sprint]]/[[log]] continued 77 + [[generic-geometry-review-lane]].
> **continued 76 (2026-06-24) — COMMITTED + pushed (web only): Phase 11 UX — workspace section nav moved into the MAIN SIDEBAR.** Web `main` `9dfae85`→**`7c04ef1`** (`product: move workspace section nav into the main sidebar`, 4 files). Owner feedback: the Phase-11 section nav in a body card felt like a page-inside-a-page. Now the 9 workflow sections render in the **main left `Sidebar`** ONLY at `/intake?workspace=1`; the body is a compact Job header + the single selected section FULL-WIDTH. State is URL-driven (**`?workspace=1&job=<id>&section=<key>`**) — new `src/lib/workspaceSections.ts` is the shared source for sidebar+body (no drift); Sidebar `useSearchParams` is Suspense-wrapped, sections are LOCKED until a `?job=` is selected, and the public nav (Home/Redline Showcase/Intake) is unchanged everywhere else. Gating-correctness fix: Closeout/Exports reads gate on the **`export_package` slot (set by Assemble)**, not the `redline_manifest` slot (Generate) → a PLACED-but-not-assembled job makes NO doomed `/closeout` 404. Backend UNCHANGED (`c5852d7`; `origin/main` `068a279`; frontier 50/58). Browser smoke 0 console errors (deep-link + full flow); guided demo unchanged (0 workspace links on `/`,`/showcase`,`/intake`). tsc/eslint/build clean. See [[hot]]/[[current-sprint]]/[[log]] continued 76.
> **continued 75 (2026-06-24) — COMMITTED + pushed (BOTH repos): Phase 11 — internal multi-page product WORKSPACE.** Backend `feat/truelinev2` `d0ed096`→**`c5852d7`** (`product: GIS route geometry read for the workspace map`); web `main` `3f9c597`→**`9dfae85`** (`product: internal multi-page workspace`). The typed-only `/intake?workspace=1` workbench becomes a left-rail section workflow over the selected job (Job Summary / Uploads / Map-Route / Bore Logs / Redlines / Review / Closeout / Exports / Billing), reusing the proven Phase 9/10 components. ONLY new backend capability = dialect-FREE `truelinev2/contracts/gis_route.py` + `GET /v2/product/jobs/{job}/gis-route` (stdlib `zipfile`+`xml.etree`, NO new dep) → real WGS84 LineString/Point/Polygon + bbox from the uploaded GIS_ROUTE, honest NAMED empty states; Map = self-contained SVG (NO Leaflet/tiles/keys/geocoding — a tiled basemap is a deferred owner-approved follow-up; redline geometry stays pixel-only on the PDF pages). Billing section = quantities only (no dollars). recognized-log9 seed now uploads the real Brenham design KMZ under the generic label `recognized_route.kmz` (1116 features) so the Map shows real route context. Engine/renderer/fixtures/anchors/`origin/main` UNTOUCHED; 50/58 UNCHANGED. Backend full v2 **1828 passed / 4 skipped** (+7 gis-route). See [[hot]]/[[current-sprint]]/[[log]] continued 75.
> **continued 74 (2026-06-24) — COMMITTED + pushed (BOTH repos): Phase 10b — server-rendered CLOSEOUT PDF packet.** Backend `feat/truelinev2` `7190dc0`→**`d0ed096`** (`product: server-rendered closeout PDF packet (trusted-data-only)`); web `main` `9099b8a`→**`3f9c597`** (`product: download closeout PDF button`). New `truelinev2/contracts/closeout_pdf.py` renders a customer-readable closeout packet PDF with **PyMuPDF (`fitz`, already a v2 dep — NOT reportlab, which is not installed)** from EXISTING trusted output, EMBEDDING the sha256-verified `FINAL_REDLINE_PNG` bytes; route `GET /v2/product/jobs/{job}/export-package/pdf` (409 SPECIFIC not-ready without a validated bundle, never a fake packet). Brings forward v1's closeout-packet STRUCTURE WITHOUT v1's client-side billing/rate math: dollars appear ONLY when `billing_summary.status` is server-COMPUTED/FINAL from configured cost rules, else "Billing summary omitted — no server cost rules configured"; KMZ honest BLOCKED (pixel-only). Web "Download closeout PDF" button beside the ZIP. Engine/renderer/fixtures/anchors/`origin/main` UNTOUCHED; 50/58 UNCHANGED. Backend full v2 **1821 passed / 4 skipped**; browser smoke → real 6.08 MB `%PDF` (3 pages, 2 embedded redline image XObjects, all markers). See [[hot]]/[[current-sprint]]/[[log]] continued 74.
> **continued 73 (2026-06-23/24) — OPERATIONAL ONLY (NO code/commit): staging-runtime triage + v1 closeout/billing audit.** Three same-session staging incidents fixed operationally (capability HEAD stayed `7190dc0`; `origin/main` `068a279`; 50/58 UNCHANGED): (1) **unstyled raw HTML** = TWO `next start` bound to :3000 split across IP stacks; cloudflared dialed the STALE IPv4 process whose HTML referenced a deleted CSS-chunk hash (HTTP 500) → fix = kill both, run EXACTLY ONE `next start -H 127.0.0.1`. (2) **workspace closeout button "missing"** = the owner viewed a STALE pre-Phase-9 page (the tunnel had served the old build for hours); the current build was proven correct end-to-end (no code change), fix = hard refresh. (3) **Cloudflare Error 1033** = a host sleep reaped ALL THREE processes (cloudflared + web + backend) → fix = full-stack restart (no rebuild; the file-backed store survived). Plus a v1 closeout/billing UX + data-model AUDIT (reference only) that recommended the continued-74/75 lanes. Detail in [[fieldroute-cloudflare-staging]] memory. See [[hot]]/[[current-sprint]]/[[log]] continued 73.
> **continued 72 (2026-06-23) — COMMITTED + pushed (BOTH repos): Phase 10 — production workflow completion (downloadable export + 50/50 coverage + gate fix).** Backend `feat/truelinev2` `0241f40`→**`7190dc0`** (`product: downloadable export zip + recognized coverage 50/50 + stale-abstain gate fix`, pushed `0241f40..7190dc0`); web `main` `f274152`→**`9099b8a`** (`product: export-zip download button + workflow step strip`, pushed). **(1) Downloadable closeout export ZIP** — new `truelinev2/contracts/export_bundle.py` + `GET /v2/product/jobs/{job}/export-package/download` streams a real `.zip` assembled ONLY from trusted output (redline manifest + sha256-VERIFIED `FINAL_REDLINE_PNG` bytes + closeout/export/KMZ status JSON + reviewed-bore-log metadata + README); a pixel-only KMZ is reported honestly inside (no `.kmz` member, never faked). Web "Download closeout package (.zip)" button + `downloadExportBundleBlob`. **(2) Recognized coverage widened 37→50** — `recognized_corpus_handoff._deterministic_artifacts` falls back (after the UNCHANGED sweep-dir 37, byte-identical) to the committed all-50 union manifest for the 13 ALREADY_DRAWN logs, each PNG **sha256-verified** before serving; multi-sheet-aware (log65's single cross-sheet PNG → both sheets 9+10); the gitignored registry now maps all 50 drawn logs. **(3) Stale-abstain gate fix** — the REVIEW-acceptance gate IGNORES `ABSTAINED` records, so a recognized deterministic package is no longer blocked at closeout by a stale abstain from a prior attempt (`job-jsy03x` fixed — now serves log9 → READY). v1 audited (7-agent workflow) as reference: brought forward the closeout section-model + honest-disclaimer language; REFUSED v1's snap-to-route / proportional-fallback KMZ shortcuts (redline KMZ stays honestly `BLOCKED[UNSUPPORTED_PIXEL_ONLY]`); rendered-PDF binary DEFERRED (inline PNG view + the zip-with-PNGs satisfy the minimum). **Engine/renderer/fixtures/anchors/`origin/main` UNTOUCHED; deterministic frontier 50/58 drawn UNCHANGED.** Backend full suite **1819 passed / 2 skipped** (+7); web `tsc`/build/lint(my files clean)/smoke green; browser smoke green (recognized → real PNGs → closeout READY → real **downloadable zip** validated; 7-step workflow strip; guided demo chooser clean). Proof harnesses `truelinev2/proof/run_phase9_*` + the audit plan stay UNTRACKED/gitignored. See [[hot]] continued 72.
> **continued 71 (2026-06-23) — COMMITTED + pushed (BOTH repos): Phase 9 — connect the product workflow to the PROVEN redline capability (Phase 8C disconnect CLOSED).** Backend `4141b15`→**`0241f40`** (`product: connect uploaded jobs to the proven redline capability`, pushed); web `ba0ae02`→**`f274152`** (`product: wire the workspace to the 3-path redline workflow + closeout/export`, pushed). Phase 8C proved the workspace `Generate` was wired ONLY to the weak default-mode uploaded-corpus adapter, so Patrick's uploaded `bore_log9` (a DRAWN deterministic log) ABSTAINED instead of serving its real render. Fix: new `truelinev2/contracts/product_workflow.py` 3-path orchestrator (`POST /v2/product/jobs/{job}/workflow/redline` + `/workflow/closeout`) chooses IN ORDER: **(A) recognized deterministic** (exact sha256 → serve the EXISTING committed render, `DETERMINISTIC_AUTO`, render `c19b565`) → **(B) uploaded REVIEW** (engine candidate, accept/reject, never faked AUTO) → **(C) ABSTAIN** (SPECIFIC recognition+engine reasons, never a bare `ENGINE_ABSTAINED`). Real binary KMZ writer + structural validator added (product redline KMZ honestly `BLOCKED` pixel-only; proven Google-Earth-valid on REAL design-KMZ coords). Recognized-corpus registry expanded to 37 drawn logs. Web `ProductWorkflowPanel` shows the 3 paths DISTINCTLY + closeout/export. Engine/fixtures/`origin/main` UNTOUCHED; frontier 50/58 UNCHANGED. Backend suite 1812→(c72)1819 passed. See [[hot]] continued 71.
> **continued 70 (2026-06-23) — COMMITTED + pushed (web only): Phase 8B — restore the PRODUCTIVE internal upload workflow.** Web `main` `368d6c9`→**`ba0ae02`** (`product: restore productive internal upload workflow`, 2 files/+60/−15; pushed). Phase 8A investigation confirmed the backend uploaded→REVIEW pipeline ALREADY exists and is generic + name-free — the engine runs on a job's OWN uploaded plan + bore-log (NOT seeded/fixture/recognized-corpus); dialect chosen by PATTERN (ODOT "DIRECTIONAL BORE" / Brenham "DIR. BORE"/"STA…TO STA"); the 7C "dead-end" was ONLY that the gate + REVIEW card were hidden. 8B restores them in the typed-only `?workspace=1`: upload inventory → reviewed-bore-log gate → engine REVIEW candidate (Generate → real `FINAL_REDLINE_PNG` → Accept/Reject). Honest framing: the engine parses the uploaded bore-log FILE; typed reviewed rows are a human SIGN-OFF gate (NOT the geometry source — human-corrections-drive-geometry is a future lane); UNSUPPORTED plans → honest ABSTAIN (`NO_PLAN_DIALECT_RECOGNIZED`/`ENGINE_ABSTAINED`…) with friendlier plain-English copy, accept BLOCKED. **Hector demo safety UNCHANGED** (no visible workspace link; `/` 3 cards; `/intake` 2 REVIEW choices; guided paths untouched — all re-verified). **Backend ENGINE UNTOUCHED** (`dcb1d29`; `origin/main` `068a279`; frontier **50/58**; no fake AUTO/parsing/PNGs). Proof: a gitignored HTTP smoke drives the REAL API upload→gate→generate→accept — supported = real REVIEW PNG (sha-verified, 50,389 B) accepted `ENGINE_GENERATED_HUMAN_ACCEPTED_REVIEW`; unsupported = ABSTAIN + accept 409 (**12/12**). Backend contract tests **27 passed** (engine untouched); web `tsc`/build/lint clean; browser smoke green (workspace productive + guided regression, 0 console errors). Generic-named sample staged (gitignored `samples/supported-plan.pdf` + `supported-bore-log.xlsx`) for Patrick's manual upload. NOT a public/Hector workflow yet — typed-only internal. See [[hot]]/[[current-sprint]]/[[log]] continued 70.
> **continued 69 (2026-06-23) — COMMITTED + pushed (web only): Phase 7C demo-safety — hide the internal upload workspace from the guided demo.** Web `main` `5f67703`→**`368d6c9`** (`product: hide internal upload workspace from guided demo`, 2 files/+38/−95; pushed). The owner's authenticated smoke found the internal upload workspace was still reachable from the visible demo UI and is a storage-only dead-end (old/random `job-xxxxxx`, 59-file uploads, raw filenames, "nothing happens"). Fix: **the `/intake` chooser now shows ONLY the two guided REVIEW workflows (no workspace card)** + the **landing drops its "Upload Intake / Job Workspace" card** → **ZERO visible link to the workspace anywhere** (sidebar/topbar/main; verified `workspace=1` link count = 0). The raw workspace remains ONLY at the **typed `?workspace=1`**, now **storage-only** (no reviewed-bore-log gate, no redline card, no source-anchor) with a strong banner ("Internal upload workspace · Not part of the guided demo · Uploads are stored for intake only; automatic parsing and redline generation are not enabled from this workspace · Use the prepared REVIEW workflows"). **Staging store pruned to EXACTLY the 3 demo jobs** (`completed-redline-showcase` / `demo-review-acceptance` / `demo-cross-sheet-review`) via a gitignored `clean_demo_store.py` (removed `job-dya4b6`/`gferbz`/`gh9f29`/`wg9p49`). **Backend engine UNCHANGED** (`dcb1d29`; `origin/main` `068a279`; frontier **50/58**; no fake AUTO/parsing/PNGs). Validated: web `tsc`/build/lint clean + browser smoke — chooser (2 options only), guided REVIEW (Generate→PNG→Accept), guided cross-sheet (Generate→2 PNGs+caveats), workspace (typed-only, storage-only banner), landing (3 cards, no workspace link), showcase (2 PNGs), **0 console errors**. Same-origin staging rebuilt + `cloudflared` `47f42c57…` live; Access **302** verified. Hector's email STILL must be added to the `morning-river-d67d` Access policy. See [[hot]]/[[current-sprint]]/[[log]] continued 69.
> **continued 68 (2026-06-23) — COMMITTED + pushed (web only): Phase 7B guided-intake demo polish.** Web `main` `91bcc29`→**`5f67703`** (`product: simplify guided intake demo paths`, 2 files/+186/−49; pushed). `/intake` no longer opens the raw workbench by default — it switches on the URL: a clean **"Choose a demo workflow" chooser** (no job list / upload inventory), **`?job=<demo>`** → a MINIMAL guided view with ONLY that demo's one engine redline card, **`?workspace=1`** → the internal upload workspace (explicitly labeled "Internal upload workspace" + "automatic parsing is not enabled in this demo path", off the guided path). Guided paths HIDE the global job list / upload inventory / reviewed-bore-log gate / create-job / raw uploads / dev source-anchor UI and show NO raw filenames. **Backend engine UNCHANGED** (capability HEAD `dcb1d29`; `origin/main` `068a279`; frontier **50/58**; no fake AUTO; no fake parsing). `ProductIntake.tsx` is now FULLY lint-clean (the prior `:74` boot-effect baseline resolved with the same idiom). Validated: web `tsc`/build/lint clean + browser smoke (chooser / guided REVIEW / guided cross-sheet 2-PNG+caveats / internal workspace / landing / showcase, **0 console errors**). Same-origin staging rebuilt + `cloudflared` `47f42c57…` live; Access **302** verified. Hector's email STILL must be added to the `morning-river-d67d` Access policy. See [[hot]]/[[current-sprint]]/[[log]] continued 68.
> **continued 67 (2026-06-23) — COMMITTED + pushed (web only): Phase 7 staging demo-shell hardening.** Web `main` `90935be`→**`91bcc29`** (`product: harden staging demo shell and showcase flow`, 7 files/+392/−150; pushed). **Backend engine UNCHANGED** — capability HEAD stays **`dcb1d29`**, `origin/main` `068a279`, deterministic frontier **50/58** UNCHANGED; no fake AUTO, no fake data. `/` is now a **demo-safe STATIC landing** (NO SSR fetch to the Access-gated API → returns 200 behind Access; the continued-66 `/` SSR-500 = [[bugs/current-bugs]] B-FR-DASHBOARD-SSR-1 is RESOLVED). New **`/showcase`** route renders the REPRESENTATIVE deterministic finished-redline PNGs (log8 s18+s22), honestly labeled "representative" with the full-package facts (50 drawn / 83 `FINAL_REDLINE_PNG` / `c19b565` / 50/58) — **the full 83-PNG gallery is NOT built yet**. `/intake` shows **exactly one redline card per job** (recognized→automatic handoff; otherwise→engine REVIEW acceptance) with `?job=` deep-linking from the landing cards: **`demo-review-acceptance`** = simple REVIEW acceptance, **`demo-cross-sheet-review`** = cross-sheet REVIEW (NEW clean-seeded job; 2 PNGs + matchline caveats; stays REVIEW). Demo nav trimmed; mock topbar / dev / manual-source-anchor UI HIDDEN in normal staging. Seed convention fix: staging reviewed-bore-log id `rbl-1`→`rbl-main` (the gate UI's canonical id). Validated: web `tsc` clean + production build + contract-parity + live-product-read + lint clean (only the pre-existing `ProductIntake.tsx:74` baseline) + product-visible name-token scan zero. Gated staging rebuilt same-origin + `cloudflared` `47f42c57…` live; Access **302** verified. Hector's email STILL must be added to the `morning-river-d67d` Access policy. See [[hot]]/[[current-sprint]]/[[log]] continued 67.
> **continued 66 (2026-06-23) — gated staging demo LIVE (operational only; NO code / NO commit / NO capability change):** the committed Phase 6 lane was verified end-to-end behind Cloudflare Access, and a two-part `/intake` demo was staged for an owner/Hector walkthrough. Capability HEAD stays **`dcb1d29`** (git `72abcd0`); web `90935be`; `origin/main` `068a279`; deterministic frontier `50/58` UNCHANGED. Demo: **`https://staging.fieldroute.io/intake`** (tenant `staging-smoke`, behind Cloudflare Access — **302 verified** to `morning-river-d67d.cloudflareaccess.com`): (1) **deterministic Brenham SHOWCASE** = job `completed-redline-showcase` → "Automatic redline handoff" card → committed-render replay (`DETERMINISTIC_RECOGNIZED_CORPUS`, log8, 2 `FINAL_REDLINE_PNG`); (2) **ODOT REVIEW acceptance** live workflow = job `demo-review-acceptance` (pristine) → "Engine REVIEW redline candidate" card → Generate→Accept (`ENGINE_GENERATED_HUMAN_ACCEPTED_REVIEW`). Stack: backend `:8100` (+`TL2_RECOGNIZED_CORPUS_REGISTRY`), web prod build `:3000` (same-origin), `cloudflared` `47f42c57…`. Changed = gitignored data + a process restart only (backend tracked changes NONE; no commit needed). Caveat (owner-deferred, do NOT fix yet): `/` dashboard SSR-500 behind same-origin Access ([[bugs/current-bugs]] B-FR-DASHBOARD-SSR-1) → use `/intake` only. Next = owner/Hector `/intake` walkthrough (add Hector's email to the `morning-river-d67d` Access policy). See [[hot]]/[[current-sprint]]/[[log]] continued 66.
> **continued 65 (2026-06-23) — COMMITTED + pushed (BOTH repos): Phase 6 — the REVIEW acceptance lane.** REVIEW is now a FIRST-CLASS product output: the uploaded-corpus ENGINE generates a source-supported REVIEW redline candidate from a job's own plan + reviewed bore-log, and a human ACCEPTS or REJECTS the engine candidate WITHOUT drawing geometry. Backend `feat/truelinev2` capability HEAD `f4f6529`→**`dcb1d29`** (`product: add REVIEW acceptance lane for engine candidates`, 5 files/+883/−4; pushed `70eaa30..dcb1d29`); web `main` `39c7958`→**`90935be`** (`product: add REVIEW candidate acceptance UI`, 4 files/+564; pushed). New contract `truelinev2/contracts/review_acceptance.py` (state machine `REVIEW_CANDIDATE→REVIEW_ACCEPTED/REVIEW_REJECTED`, plus `ABSTAINED`) + 5 default-OFF `/v2/product/jobs/{job}/review-candidates[...]` routes + a web `ProductReviewCandidates` card. Accepted REVIEW carries job-local provenance **`ENGINE_GENERATED_HUMAN_ACCEPTED_REVIEW`** (+ `no_manual_geometry`) in the acceptance record; the published manifest stays schema-valid (per-log `OWNER_CONFIRMED_HUMAN_ADJUSTABLE`, **frozen provenance enum UNCHANGED**), **never `DETERMINISTIC_AUTO`**. Real ODOT proof (generate→accept→valid `FINAL_REDLINE_PNG`): **71′** 1 artifact, **118′** sheets 10+11 (2 artifacts), **88′** sheets 11+12 (2 artifacts); 118′/88′ keep `CROSS_SHEET_CONTINUATION_REVIEW` + `MATCHLINE_CONTINUATION_UNVERIFIED`. **`NO_PER_BORE_TERMINI` still blocks AUTO**; frontier `50/58` UNCHANGED (uploaded-corpus bundles job-local, not summed in). Verify: backend full v2 **1796 passed / 4 skipped** (+ targeted 151/1: 14 contract + 7 API + naming guard + adapter + frontier lock `50/58`,`DETERMINISTIC_AUTO=49`); web `tsc --noEmit` clean + node checks + new/changed files lint-clean. The ODOT proof harness + `data/outputs/**` are NOT committed. No second corpus / no staging/demo started. Next (owner-gated): gated staging smoke of the REVIEW acceptance lane, OR product packaging/summary for sales/pilot positioning. See [[hot]]/[[current-sprint]]/[[log]] continued 65.
> **continued 64 (2026-06-22) — COMMITTED + pushed:** ODOT **Phase 5** (matchline continuity validation) is COMMITTED — backend `feat/truelinev2` capability HEAD `996e058`→**`f4f6529`** (`product: validate matchline continuity for cross-sheet REVIEW legs`, 2 files/+201/−17; pushed `30b01fa..f4f6529`). The adapter now validates matchline continuity from PRINTED evidence ONLY (reuses `extract/matchline_join.see_sheet_crossings` + `parse_station`, no parser rewrite) — read-only metadata, never geometry, never AUTO: 118′ + 88′ print mutual "MATCHLINE: SEE SHEET N" adjacency but NO boundary station inside the bore span → `MATCHLINE_SHEET_ADJACENCY_CONFIRMED` + `MATCHLINE_CONTINUATION_UNVERIFIED`; the CONFIRMED tier (`MATCHLINE_CONTINUATION_CONFIRMED_REVIEW`) is implemented + tested but NOT triggered on this corpus. Rendered artifacts BYTE-IDENTICAL to Phase 4 (metadata-only); geometry unchanged; REVIEW only, **no AUTO** (`NO_PER_BORE_TERMINI` still blocks AUTO); frontier `50/58` UNCHANGED (uploaded-corpus bundles job-local, not summed in). Verify: backend **42 passed / 1 skipped** (13 adapter incl. 6 matchline + no-AUTO guard; 3 naming guard; 10 ODOT; frontier lock `50/58`,`DETERMINISTIC_AUTO=49`); tree clean; ahead/behind 0/0. **Next Tulsa-31-applicable lane = per-sheet coverage completeness metrics** (CONFIRMED-matchline + AUTO both need a second corpus; none yet). No second corpus, no staging/web/demo. See [[hot]]/[[current-sprint]]/[[log]] continued 64.
> **continued 63 (2026-06-22):** ODOT **Phase 4** (cross-sheet REVIEW assembly) is COMMITTED — backend `feat/truelinev2` **`996e058`** (`product: render cross-sheet REVIEW legs for uploaded-corpus bores`, pushed). Two-sheet bores now render a REVIEW leg PER referenced sheet (each from that sheet's OWN drawn geometry; NO connecting segment across the matchline): **118′ → sheets 10+11 (2 artifacts); 88′ → sheets 11+12 (2 artifacts)**, both FULL coverage. The winner-sheet PNGs stayed BYTE-IDENTICAL to Phase 2; the new sheet-11 legs are real red dashed REVIEW strokes at the matchline. `CROSS_SHEET_CONTINUATION_REVIEW` remains truthful (legs not assembled into one validated route); **`NO_PER_BORE_TERMINI` still blocks AUTO** (no AUTO promotion). Frontier `50/58` UNCHANGED (uploaded-corpus bundles job-local). No second corpus / no staging/web/demo. Next product-engine lane = **(1) validated matchline continuity (printed matchline station equations)** OR **(2) per-sheet coverage completeness metrics**. (continued-62: Phase 3 caveat `26db2c7`; continued-60: Phase 1 adapter `f3621c6`; **Victoria is NOT the next target**.) The c56 snapshot below is retained as prior context.
> Snapshot below is current as of **2026-06-22 (continued 56 — M2 human-confirmed source-anchor RENDER lane shipped (Slices 1–3, BOTH repos): upload PLAN_PDF → review bore-log to `engine_ready` → display the real plan page → click ordered route control points → validate a `HUMAN_CONFIRMED_CONTROL_POINTS` source anchor → RENDER a dashed REVIEW redline PNG → publish a real `mock_example:false` bundle (`bundle_origin=HUMAN_CONFIRMED_SOURCE_ANCHOR`) → set the job's `redline_manifest`/`artifact_bundle` slots → view the artifact inline. Backend `feat/truelinev2` HEAD `585c187` (M2 Slice 1 `47c9c2b` / Slice 2 `e715731` / Slice 3 `585c187`, pushed → `origin/feat/truelinev2`); web `trueline-web-experience` `main` HEAD `e9d838d` (Slice 2 `40b2866` / Slice 3 `e9d838d`, pushed → web `origin/main`); **`origin/main` (backend) UNTOUCHED `068a279`**; NO deterministic engine/render/fixture/anchor/coordinate/frontier-accounting change; render `c19b565`; deterministic frontier 50/58 (the human-confirmed render lane is SEPARATE — never summed in); **P5 auth still PAUSED**; Vercel/deploy HELD — active direction = harden the real workflow, then deploy only when owner authorizes)**. For the absolute-latest
> state, read ONLY the top ~35 lines of `C:/Nova/knowledge/TrueLine-Wiki/wiki/hot.md` — never the whole file.
> **Do NOT load history/archive files** (`log.md`, `current-sprint.md`, full `hot.md`) unless explicitly
> asked or investigating a specific historical decision.

## Product goal
Deterministic auto-placement of **ALL** redlines from the source files. Manual operator placement is NOT
the product. **Product gate = actual DRAWN red strokes on the PDFs** — classification / "placed for
review" buckets are not progress until they become drawn strokes.
- **ALL-REDLINES standard** (non-negotiable): place every redline from source; abstention is only an
  interim safety state + a *named* missing-source target, never a manual fallback or "done".
- **DO-NOT-WIDEN** (coexists): never place a wrong redline. Drive abstentions to zero by EXTRACTING the
  missing source relationship — not by guessing, and not by asking a human to decide from vibes.
- Identity-only bridges, NEVER invented coordinates. Every drawn stroke is red (canonical red-stroke law).

## Repo / branch state
- Backend repo: `C:/Nova/projects/TrueLine/TrueLine_Beta`  ·  Branch: `feat/truelinev2`  ·  capability HEAD **`a4854a9`** (continued-79b workflow/redline accepted-state echo; pushed `origin/feat/truelinev2`, ahead/behind 0/0 — proof harnesses `truelinev2/proof/run_phase9_*.py` + `run_review_acceptance_odot.py` + `run_generic_*probe.py` + `run_generic_real_plan_diagnostic.py` + `seed_general_upload_local_smoke.py` + the gitignored recognized-corpus registry + the `data/outputs/truelinev2/staging_smoke/` seed scripts + `redline-bundle.tgz` stay UNTRACKED by design)  ·  `origin/main` **`068a279`** (untouched). (continued-79/79b `96c0e10`→`9a54118` [honest generic placement: coverage/ambiguity-graded confidence + correction signal] →`5e83879` [ambiguous demo builder] →`a4854a9` [workflow/redline echoes accepted REVIEW state — unblocks closeout assembly]; continued-78 `30c76aa`→`96c0e10` = bore-aware placement fix [select bore run / clip to span / honest confidence]; continued-77 `641f52a`→`57e698d`→`30c76aa` = name-free generic-geometry REVIEW lane [abstain-fix] + densest-row + self-contained demo seed; all pushed. Full v2 suite **1845 passed / 4 skipped**.) (continued-75 Phase 11 `d0ed096`→`c5852d7` = dialect-free `gis_route.py` + `GET …/gis-route` for the workspace map; continued-74 Phase 10b `7190dc0`→`d0ed096` = `closeout_pdf.py` server-rendered PDF packet via fitz + `GET …/export-package/pdf`; continued-76 was web-only [sidebar nav], NO backend change. Full v2 suite 1828 passed / 4 skipped.) (continued-72 Phase 10 `0241f40`→`7190dc0` = downloadable export zip + recognized coverage 37→50 + stale-abstain gate fix; continued-71 Phase 9 `4141b15`→`0241f40` = 3-path product-workflow orchestrator connecting uploads to the proven render + binary KMZ writer; both pushed. (continued-70: web-only Phase 8B — restore productive internal upload workflow; NO backend capability change [investigation confirmed the uploaded→REVIEW pipeline already exists + is generic]; continued-69: web-only Phase 7C demo-safety — hide internal upload workspace; NO backend capability change; continued-68: web-only Phase 7B guided-intake demo polish — NO backend capability change; continued-67: web-only Phase 7 demo-shell hardening — NO backend capability change, capability HEAD stays `dcb1d29`; continued-65: `70eaa30`→`dcb1d29`, `product: add REVIEW acceptance lane for engine candidates` — Phase 6 REVIEW acceptance lane COMMITTED; continued-64: `30b01fa`→`f4f6529`, `product: validate matchline continuity for cross-sheet REVIEW legs` — Phase 5 matchline continuity COMMITTED; continued-63: `041ce5a`→`996e058`, `product: render cross-sheet REVIEW legs…`; continued-62: `f657ddf`→`26db2c7`, cross-sheet caveat; continued-60: `1759a0b`→`f3621c6`, Phase 1 adapter; continued-59: `c3715d4`→`9fa92b8`, recognized-corpus harness.)
- Web repo (SEPARATE): `C:/Nova/projects/trueline-web-experience`  ·  Branch: `main`  ·  HEAD **`5b4a8d8`** = web `origin/main` (synced, ahead/behind 0/0; continued-79b `dad8d36`→`5b4a8d8` = `product: recognize an accepted REVIEW across Redlines/Closeout (no more dead-end)`; continued-79 `66636eb`→`dad8d36` = `product: surface honest placement confidence + a "Correct redline placement" step`; continued-77 `7c04ef1`→`66636eb` = `product: surface generic-placement REVIEW confidence in the workspace` [continued-78 was backend-only, NO web change]; continued-76 `9dfae85`→`7c04ef1` = `product: move workspace section nav into the main sidebar`; continued-75 `3f9c597`→`9dfae85` = `product: internal multi-page workspace`; continued-74 `9099b8a`→`3f9c597` = `product: download closeout PDF button`; continued-72 `f274152`→`9099b8a` = `product: export-zip download button + workflow step strip`; continued-71 `ba0ae02`→`f274152` = `product: wire the workspace to the 3-path redline workflow + closeout/export`; continued-70 `368d6c9`→`ba0ae02` = `product: restore productive internal upload workflow`; continued-69 `5f67703`→`368d6c9` = `product: hide internal upload workspace from guided demo`; continued-68 `91bcc29`→`5f67703` = `product: simplify guided intake demo paths`; continued-67 `90935be`→`91bcc29` = `product: harden staging demo shell and showcase flow`)  ·  LIVE at `https://trueline-web-experience.vercel.app/` (continued-54 Vercel deploy; continued-55–70 NOT on Vercel — Vercel HELD). Gated FieldRoute staging (`https://staging.fieldroute.io/`, Cloudflare Access) runs a LOCAL PRODUCTION `next build`+`next start` same-origin, NOT Vercel, NOT `next dev` — the demo entry is **`/`** (demo-safe landing); **`/intake` is a guided "Choose a demo workflow" chooser of the 2 REVIEW workflows ONLY**, with the raw workspace reachable ONLY via the typed `?workspace=1` (no visible link), now **PRODUCTIVE for supported packages** (continued-70: upload → reviewed-bore-log gate → engine REVIEW candidate; unsupported → honest ABSTAIN).
- Product lives in `truelinev2/` (clean-room, zero old-app imports). v2 suite last FULL run: **1686 passed / 4 skipped** (continued-53). Continued-56 verified by TARGETED runs only (full suite not re-run): backend **154 passed / 1 skipped** (M2 render + product API + source-anchor + the deterministic frontier-lock tests, which passed UNCHANGED) + naming guard (operator `NAME_TOKENS`) 3 ✓ + AST no-engine-import guard; web `check-live-product-read.mjs` (incl. 6 new Slice 3 checks) ✓ + `tsc --noEmit` clean. Callout-sweep e2e **31 passed / 1 skipped**.
- Isolated track: monolith / Render UNTOUCHED; Vercel HELD by owner (no env change / no deploy); nothing merged or deployed.

## HEAD / remote state (verify with `git` before trusting this snapshot)
- **continued 63 (2026-06-22) — ODOT Phase 4 cross-sheet REVIEW assembly COMMITTED + pushed:** backend `041ce5a`→**`996e058`** (`product: render cross-sheet REVIEW legs for uploaded-corpus bores`, 2 files/+137/−55; pushed `041ce5a..996e058`). Multi-sheet bores now render a REVIEW leg per referenced sheet (winner sheet keeps its render BYTE-FOR-BYTE; each extra leg from that sheet's OWN drawn geometry; NO cross-sheet connecting segment). Real proof: 118′ → sheets 10+11 (2 artifacts; s10 byte-identical `d3238305…`), 88′ → sheets 11+12 (2 artifacts; s12 byte-identical `f66216da…`), 71′ single-sheet unchanged (1 artifact). Caveats `CROSS_SHEET_CONTINUATION_REVIEW` (+ new `PARTIAL_CROSS_SHEET_REVIEW` when a referenced sheet has no leg); REVIEW only — `NO_PER_BORE_TERMINI` still blocks AUTO. Validation: backend **149 passed / 1 skipped** (adapter incl. cross-sheet/partial tests + product API + naming guard + frontier lock `50/58`,`DETERMINISTIC_AUTO=49` + recognized-corpus + M2 render + ODOT units). `origin/main` untouched `068a279`.
- **continued 62 (2026-06-22) — ODOT Phase 3 AUTO-gap foundation COMMITTED + pushed:** backend `f657ddf`→**`26db2c7`** (`product: add cross-sheet continuation REVIEW caveat`, 2 files/+63/−11; pushed `f657ddf..26db2c7`). Adapter now emits `CROSS_SHEET_CONTINUATION_REVIEW` (multi-sheet bore rendered on one winning sheet) — truth annotation only (no coordinate/render change, never AUTO; engine/match untouched). A read-only per-bore-termini scout + strict AUTO gate confirmed NO ODOT row reaches AUTO (71′ `NO_PER_BORE_TERMINI`; 118′/88′ `NO_PER_BORE_TERMINI` + `CROSS_SHEET_CONTINUATION_REVIEW`). Validation: backend **148 passed / 1 skipped** (adapter incl. cross-sheet test + product API + naming guard + frontier lock `50/58`,`DETERMINISTIC_AUTO=49` + recognized-corpus + M2 render + ODOT units). `origin/main` untouched `068a279`.
- **continued 61 (2026-06-22) — ODOT Phase 2 (proof only, NO tracked code):** ran the committed adapter `f3621c6` AS-IS on all three Tulsa-31 bores via a gitignored proof. **71′ (sheet 11), 118′ (sheet 10 of [10,11]), 88′ (sheet 12 of [11,12]) each rendered a real job-local `FINAL_REDLINE_PNG` REVIEW stroke** (`bundle_origin=UPLOADED_CORPUS_ENGINE`; sizes 50,429 / 56,799 / 39,252 B). All REVIEW (`DRAWN_EXTENT_COVERS_SPAN_NOT_TIGHT` + `DRAWN_VECTOR_EXTENT`/`LOCATION_ONLY`/`DRAWN_EXTENT_EXCEEDS_BORE_SPAN`); 118′/88′ additionally place on a single winning sheet (cross-sheet continuation NOT assembled). Frontier `50/58` UNCHANGED; capability HEAD stays `f3621c6`. Next lane = AUTO/generalization (Phase 3).
- **continued 60 (2026-06-22) — ODOT Phase 1 uploaded-corpus ENGINE adapter COMMITTED + pushed:** backend `1759a0b`→**`f3621c6`** (`product: add uploaded-corpus engine adapter phase 1`, 6 files/+589/−2; pushed `1759a0b..f3621c6`). FIRST real uploaded-corpus engine adapter: runs the shipped engine on a job's OWN uploaded plan + engine-ready reviewed bore-log → renders the redline from the plan's DRAWN geometry → job-local `FINAL_REDLINE_PNG` bundle (`bundle_origin=UPLOADED_CORPUS_ENGINE`), or the engine's named blocker. Proven on the real ODOT plan + 71′ bore log: REVIEW (`DRAWN_EXTENT_COVERS_SPAN_NOT_TIGHT`), real PNG published. SEPARATE job-local `1/1` — NEVER summed into `50/58`. Validation: backend **147 passed / 1 skipped** (adapter + product API paths + naming guard + frontier lock `50/58`,`DETERMINISTIC_AUTO=49` + recognized-corpus + M2 render + ODOT units). `origin/main` untouched `068a279`.
- **continued 59 (2026-06-22) — continued-57/58 staging work COMMITTED + pushed:** backend `c3715d4`→**`9fa92b8`** (`product: add recognized-corpus handoff harness`, 7 files/+567/−2; pushed `c3715d4..9fa92b8`) + web `e9d838d`→**`39c7958`** (`staging: harden intake recognized-corpus flow`, 9 files/+411/−128; pushed `e9d838d..39c7958`). The recognized-corpus bundle is a SEPARATE job-local `1/1` (`bundle_origin=DETERMINISTIC_RECOGNIZED_CORPUS`), NEVER summed into the deterministic `50/58`. Validation: backend **132 passed / 1 skipped** (recognized-corpus + product API + naming guard + frontier lock `50/58`, `DETERMINISTIC_AUTO=49`); web `tsc --noEmit` clean + contract parity + 71 live-product-read checks + production `next build` OK. `origin/main` untouched `068a279`. (continued-57/58 were the previously-uncommitted staging layers — see [[hot]].)
- Last RENDER commit: **`c19b565`** (log3 wired + DRAWN, 49→50/58 — UNCHANGED; deterministic frontier). Latest capability HEAD = **`dcb1d29`** (was `f4f6529`)
  (continued-56 M2 Slice 3; `feat: render validated source anchors into a human-confirmed redline bundle`, 6 files/+610, pushed → `origin/feat/truelinev2` = `585c187`; tree clean; ahead/behind 0/0). **Continued-56 M2 backend arc** on the continued-55 capability `b74d7de`: `47c9c2b` (M2 Slice 1 — source-anchor record + API) → `e715731` (M2 Slice 2 — plan-page metadata + in-memory raster routes) → `585c187` (M2 Slice 3 — render validated source anchors → human-confirmed bundle). The M2 human-confirmed render lane is SEPARATE from the deterministic 50/58 frontier (NO engine/render/fixture/anchor/coordinate/frontier-accounting change; the only contract change is an additive OPTIONAL manifest field `bundle_origin`). **Continued-55 backend arc** on the continued-53 capability `89cf5b5`: `8f6cbbd` (c53 docs) → `d813208` (c54 docs) → `c7ed45b` (staging runbook) → `f743374` (Slice A) → `c8b5ab3` (Slice B) → `b74d7de` (Slice C). NO engine/render-truth change. Prior: continued-52 Slice 4 routes `eea8378` (docs `f63d5df`); continued-51 Slice 3 routes `baf36e0` (docs `a7c9289`); continued-50 Slice 2 routes `bd55273` (docs `a809ce8`); continued-49 Slice 1 routes `986ae74` (docs `4c438ed`); continued-48 export_package `c5185d5` (docs `0c7eecc`); continued-47 billing `42c6bab` (docs `6b4ae1f`); continued-46 closeout `43a8501` (docs `f7e0772`); continued-45 kmz_export `2f70e22`; continued-44 handoff `2013879`; continued-43 gate `6048ef1`; continued-42 Slice 1 `2193a0e`; continued-41 `a4bf2a5` (docs/audit). Continued-41 engine-repo arc (7 commits on the continued-40 save `021561c`):
  `b9fd9cf` (staging URL) → `09fc469` (artifact-hosting plan) → `e886de2` (hosting IMPL recorded) → `377536d` (Cedar Ridge→Brenham relabel) → `5211102` (mock-shell→proof-viewer) → `2a559bd` (artifact-fetch env-debug) → `a4bf2a5` (v1 salvage audit + v2 pipeline contract). NO engine/render-truth change.
  Web (SEPARATE `trueline-web-experience` repo): `main` `9d73dd2`→**`e9d838d`** (continued-56 M2 arc: `40b2866` Slice 2 source-anchor capture UI → `e9d838d` Slice 3 render action + inline artifact); web `origin/main` = `e9d838d` (synced, ahead/behind 0/0); default branch = `main`; LIVE at `https://trueline-web-experience.vercel.app/` (continued-54 deploy; continued-55/56 NOT deployed — Vercel HELD). Prior arc (continued-55): `d9465b0`→`4d33eff`→`383b178`→`b11e432`→`8378803`→`9d73dd2`.
  Prior saves: continued-40 `a4b8590`/`021561c` (staging standup); continued-39 `16295d4` (P4 plan + Fable clean main); continued-38 `bdbc3b1` (Phase 2J + Fable UI preserve/retire + repo-arch + remote-init P1).
  ARCHIVE (recovery): branch/tag `archive-v2-continued-35-superseded-scratch` = **`d8508b9`** (superseded `backend/tl_core/**` + 14 proof slices). `origin/main`: **`068a279`** (untouched).

## Latest — continued 67 (2026-06-23): Phase 7 staging demo-shell hardening COMMITTED + pushed (web only) — demo-safe `/`, `/showcase`, per-job card gating, deep-linked REVIEW + cross-sheet; backend engine UNCHANGED; 50/58 UNCHANGED
**continued 67 — Phase 7 hardens the gated staging environment into a self-guiding product demo for the capabilities that already exist (NO engine work, NO new capability).** Web `main` `90935be`→**`91bcc29`** (`product: harden staging demo shell and showcase flow`, 7 files/+392/−150, pushed). **Backend tracked code UNTOUCHED; capability HEAD stays `dcb1d29`; `origin/main` `068a279`; render `c19b565`; deterministic frontier `50/58` UNCHANGED; no fake AUTO; no fake data; P5 auth PAUSED.**
- **Demo-safe `/` (RESOLVES the continued-66 SSR-500):** `/` is rewritten as a STATIC landing page that performs ZERO server-side fetch, so it returns 200 even behind Cloudflare Access (the old `/` was an async server component awaiting the Access-gated API → login HTML → 500). [[bugs/current-bugs]] **B-FR-DASHBOARD-SSR-1 → RESOLVED.** Clean FieldRoute landing: 4 plain-English cards + a "How to read this demo" REVIEW-vs-AUTO explainer ("the app does not guess when source evidence is missing").
- **`/showcase` (NEW route):** a client gallery rendering the recognized-corpus showcase job's REAL deterministic finished-redline PNGs (log8 s18+s22), CLEARLY labeled "representative finished output" with the honest full-package facts (50 drawn / 83 `FINAL_REDLINE_PNG` / render `c19b565` / frontier 50/58). **The full 83-PNG gallery is NOT built yet** (representative-but-honest; deferred — needs a static bundle export).
- **`/intake` per-job card gating:** exactly ONE redline card per job — recognized corpus → "Automatic redline handoff"; otherwise → "Engine REVIEW redline candidate". Irrelevant cards, the dev/manual-source-anchor UI, and the obsolete mock topbar chrome (fake project switcher / search / "(mock)" avatar) are HIDDEN in normal staging; nav trimmed to Home / Redline Showcase / Intake.
- **Deep-linked demo cards:** the landing cards `?job=`-deep-link `/intake` to auto-select the right job — **`demo-review-acceptance`** = simple REVIEW acceptance; **`demo-cross-sheet-review`** = cross-sheet REVIEW (NEW clean-seeded job; Generate → 2 PNGs on both referenced sheets + `CROSS_SHEET_CONTINUATION_REVIEW`/`MATCHLINE_*` caveats + "Why REVIEW not AUTO"; stays REVIEW, never AUTO).
- **Seed convention fix (gitignored data only):** the staging reviewed-bore-log id was aligned `rbl-1`→`rbl-main` (the canonical id `ProductReviewedBoreLogGate` reads), so the gate card shows `engine_ready: true` instead of a misleading "Create reviewed bore-log" prompt. The demo store holds exactly 3 clean jobs (`completed-redline-showcase` published; `demo-review-acceptance` + `demo-cross-sheet-review` pristine).
- **Verify:** web `tsc --noEmit` clean + production build + contract-parity + live-product-read node checks + lint clean on all changed/new files (the ONLY remaining lint error is the PRE-EXISTING `ProductIntake.tsx:74` `loadProjectAndJobs` boot effect, untouched); product-visible name-token scan ZERO; backend tracked status clean. Browser smoke (localhost) + live API smoke (16/16) green; gated staging rebuilt same-origin + `cloudflared` `47f42c57…` live; Access **302** verified on `/`, `/intake`, `api-staging…/v2/product/jobs`.

**Next gate (owner):** add Hector's email to the `morning-river-d67d` Cloudflare Access policy (he is NOT yet added — nothing is viewable otherwise), then the `/` → showcase → REVIEW → cross-sheet walkthrough. The authenticated behind-Access browser smoke is the owner's (the agent has no Access identity). Detail: [[hot]] / [[current-sprint]] / [[log]] continued 67.

### Prior — continued 65 (2026-06-23): Phase 6 REVIEW acceptance lane COMMITTED (both repos) — REVIEW is a first-class engine-generated, human-accepted output; never AUTO; 50/58 UNCHANGED
**continued 65 — REVIEW becomes a first-class product output.** The uploaded-corpus ENGINE generates a source-supported REVIEW redline candidate from a job's own plan + reviewed bore-log; a human ACCEPTS or REJECTS the engine candidate WITHOUT drawing geometry. Backend `feat/truelinev2` `f4f6529`→**`dcb1d29`** (`product: add REVIEW acceptance lane for engine candidates`, 5 files/+883/−4, pushed `70eaa30..dcb1d29`); web `main` `39c7958`→**`90935be`** (`product: add REVIEW candidate acceptance UI`, 4 files/+564, pushed). **`origin/main` UNTOUCHED `068a279`; render `c19b565`; deterministic frontier `50/58` UNCHANGED; P5 auth PAUSED; Vercel HELD.**
- **Acceptance lane (new contract `truelinev2/contracts/review_acceptance.py`):** a job-local state machine `REVIEW_CANDIDATE → REVIEW_ACCEPTED / REVIEW_REJECTED` (+ `ABSTAINED`), keyed per engine-ready reviewed bore-log. `generate` runs the engine via the EXISTING uploaded-corpus adapter (REVIEW → renders a real `FINAL_REDLINE_PNG` + records the candidate; engine ABSTAIN → records its named blocker; AUTO → renders deterministically with no acceptance gate; missing inputs → blockers, no record). `accept` confers provenance `ENGINE_GENERATED_HUMAN_ACCEPTED_REVIEW`; `reject` needs a reason and is terminal. Idempotent generate preserves a prior decision.
- **5 default-OFF `/v2/product` routes** (generate / list / get / accept / reject) behind `TL2_PRODUCT_PIPELINE_API_OPTIN`; the accepted `FINAL_REDLINE_PNG` retrieves via the EXISTING job-artifact routes. Web `ProductReviewCandidates` card: Generate → show the redline PNG(s) + why-REVIEW-not-AUTO + caveats → Accept / Reject (with reason). No manual point-clicking UI.
- **No schema change (frozen provenance enum UNCHANGED):** the rich provenance + `no_manual_geometry` live in the acceptance RECORD; the published manifest stays within the 6-value enum (per-log `OWNER_CONFIRMED_HUMAN_ADJUSTABLE`, **never `DETERMINISTIC_AUTO`**). Engine/renderer/publisher reused read-only; no fixture/anchor/coordinate change; no manual source-anchor path introduced.
- **Real ODOT proof (generate → accept → valid `FINAL_REDLINE_PNG`):** **71′** sheet 11 (1 artifact); **118′** sheets 10+11 (2 artifacts); **88′** sheets 11+12 (2 artifacts). All three: accepted provenance `ENGINE_GENERATED_HUMAN_ACCEPTED_REVIEW`, `no_manual_geometry`, PNG bytes load, bundle job-local `1/1`. 118′/88′ keep `CROSS_SHEET_CONTINUATION_REVIEW` + `MATCHLINE_CONTINUATION_UNVERIFIED` + `MATCHLINE_SHEET_ADJACENCY_CONFIRMED`. **`NO_PER_BORE_TERMINI` still blocks AUTO.** (Proof harness `truelinev2/proof/run_review_acceptance_odot.py` + `data/outputs/**` are LOCAL ONLY — NOT committed.)
- **Verify:** backend full v2 **1796 passed / 4 skipped** (+ targeted 151/1: 14 review-acceptance contract + 7 product-API + naming guard now covering the new module + uploaded-corpus adapter + frontier lock `50/58`,`DETERMINISTIC_AUTO=49`); schema diff empty (no provenance enum change); web `tsc --noEmit` clean + node checks (incl. 11 new) + new/changed files lint-clean (the one remaining lint error is PRE-EXISTING in `ProductIntake` `loadProjectAndJobs`, untouched). Both repos pushed, ahead/behind 0/0; `origin/main` untouched.

**Next lane (owner-gated; NOT started):** EITHER a gated staging smoke of the REVIEW acceptance lane ONLY (needs the held staging wiring — product API is unauthenticated while P5 is paused), OR product packaging / summary for sales/pilot positioning. No second corpus; AUTO still needs a corpus with per-bore termini / printed boundary-station equations. Detail: [[hot]] / [[current-sprint]] / [[log]] continued 65.

### Prior — continued 64 (2026-06-22): ODOT Phase 5 matchline-continuity validation COMMITTED — 118′/88′ adjacency confirmed but station-level continuity UNVERIFIED; artifacts byte-identical; AUTO still blocked; 50/58 UNCHANGED
**continued 64 — the uploaded-corpus adapter now validates, from PRINTED matchline evidence ONLY, whether the cross-sheet REVIEW legs of a multi-sheet bore are continuous across the matchline.** Backend `feat/truelinev2` `996e058`→**`f4f6529`** (`product: validate matchline continuity for cross-sheet REVIEW legs`, 2 files/+201/−17, pushed `30b01fa..f4f6529`). **`origin/main` UNTOUCHED `068a279`; render `c19b565`; deterministic frontier `50/58` UNCHANGED; P5 auth PAUSED; Vercel HELD.**
- **Matchline-continuity validation (truthfulness only, read-only):** new helper `_matchline_continuity` (+ `_sheet_sees` / `_shared_boundary_station`) reuses the existing `extract/matchline_join.see_sheet_crossings` primitive for printed boundary-STATION equations (the strict CONFIRMED tier) and a SEE-SHEET adjacency check for the no-station case. No parser rewrite; no invented station/coordinate. The verdict is threaded through `_run_engine` → `evaluate` (candidate `matchline_continuity` + `matchline_evidence`) and into the job-local manifest warnings as metadata caveats ONLY.
- **Three caveats:** `MATCHLINE_CONTINUATION_CONFIRMED_REVIEW` (both sheets print the same boundary station inside the bore span), `MATCHLINE_CONTINUATION_UNVERIFIED` (station-level join not proven), `MATCHLINE_SHEET_ADJACENCY_CONFIRMED` (mutual SEE-SHEET printed, no boundary station).
- **Real corpus (Tulsa-31):** 118′ (sheets 10+11) + 88′ (sheets 11+12) print mutual matchline SEE-SHEET cross-references → **adjacency confirmed**, but NO boundary station inside the bore span → station-level continuity stays **`MATCHLINE_CONTINUATION_UNVERIFIED`** (+ `MATCHLINE_SHEET_ADJACENCY_CONFIRMED`). The CONFIRMED tier is implemented + tested but NOT triggered on this corpus.
- **Boundaries:** rendered artifacts BYTE-IDENTICAL to Phase 4 (metadata-only change); geometry unchanged; tier stays REVIEW; **NO AUTO promotion** (`NO_PER_BORE_TERMINI` still blocks AUTO). Engine/match/renderer untouched; frontier `50/58` UNCHANGED (uploaded-corpus bundles job-local, never summed in). No second corpus; no staging/web/demo. Verify: backend **42 passed / 1 skipped** (13 adapter incl. 6 new matchline-continuity tests + the no-AUTO guard; 3 naming guard; 10 ODOT; frontier lock `50/58`,`DETERMINISTIC_AUTO=49`); git tree clean; pushed ahead/behind 0/0.

**Next product-engine lane (Tulsa-31-applicable; separately authorized; NOT started): per-sheet coverage completeness metrics** — quantify how much of each sheet's span portion the rendered REVIEW legs cover. CONFIRMED-matchline continuity and AUTO both need a corpus with the missing evidence (printed boundary-station equations / per-bore termini) — Tulsa-31 has neither and there is no second corpus yet. Detail: [[hot]] / [[current-sprint]] / [[log]] continued 64.

### Prior — continued 63 (2026-06-22): ODOT Phase 4 cross-sheet REVIEW assembly COMMITTED — 118′/88′ render both referenced sheets; winner PNGs byte-identical; AUTO still blocked; 50/58 UNCHANGED
**continued 63 — multi-sheet ODOT bores now render a REVIEW leg per referenced sheet (full cross-sheet coverage), preserving the winner-sheet render byte-for-byte.** Backend `feat/truelinev2` `041ce5a`→**`996e058`** (`product: render cross-sheet REVIEW legs for uploaded-corpus bores`, 2 files/+137/−55, pushed). **`origin/main` UNTOUCHED `068a279`; render `c19b565`; deterministic frontier `50/58` UNCHANGED; P5 auth PAUSED; Vercel HELD.**
- **Cross-sheet REVIEW assembly:** a new adapter helper `_extra_sheet_legs` reuses the existing ODOT `dialect.extract_callouts` to find source-supported drawn extents on referenced sheets OTHER than the winner's; each leg is built ONLY from its own sheet's drawn geometry — NO connecting segment across the matchline. One log (one bore) → one `FINAL_REDLINE_PNG` per rendered sheet; `source_sheets` = all rendered sheets.
- **Real proof:** 118′ → sheets **10+11** (2 artifacts; s10 byte-identical to Phase 2 `d3238305…`, new s11 leg 26,238 B), 88′ → sheets **11+12** (2 artifacts; s12 byte-identical `f66216da…`, new s11 leg 48,702 B); **71′ single-sheet unchanged (1 artifact)**. New sheet-11 legs are real red dashed REVIEW strokes at the matchline (verified inline). Both bores = FULL coverage (not partial).
- **Truthful caveats:** `CROSS_SHEET_CONTINUATION_REVIEW` (legs rendered per-sheet, NOT assembled into one validated route) + new `PARTIAL_CROSS_SHEET_REVIEW` (some referenced sheet has no leg — implemented + tested, not triggered on Tulsa-31). Tier stays REVIEW; provenance `OWNER_CONFIRMED_HUMAN_ADJUSTABLE`. **`NO_PER_BORE_TERMINI` still blocks AUTO — no AUTO promotion.**
- **Boundaries:** engine/match/renderer unchanged; frontier `50/58` unchanged (bundles job-local, never summed in); name-free; no second corpus; no staging/web/demo. Verify: backend **149 passed / 1 skipped**; git tree clean.

**Next product-engine lane (separately authorized; NOT started):** either **(1) validated matchline continuity** — use the printed matchline station-equations to CONFIRM the two legs are one continuous bore across the matchline (upgrade the caveat from "not assembled" to "continuation confirmed"; still REVIEW); or **(2) per-sheet coverage completeness metrics** — quantify how much of each sheet's span portion the legs cover. AUTO still needs a corpus with per-bore termini (absent in Tulsa-31; no second corpus yet). Detail: [[hot]] / [[current-sprint]] / [[log]] continued 63.

### Prior — continued 62 (2026-06-22): ODOT Phase 3 AUTO-gap foundation COMMITTED — CROSS_SHEET_CONTINUATION_REVIEW caveat; scout confirms NO ODOT row reaches AUTO; 50/58 UNCHANGED
**continued 62 — Phase 3 makes the blocker model truthful and confirms (fresh evidence) Tulsa-31 cannot reach AUTO.** Backend `feat/truelinev2` `f657ddf`→**`26db2c7`** (`product: add cross-sheet continuation REVIEW caveat`, 2 files/+63/−11, pushed). **`origin/main` UNTOUCHED `068a279`; render `c19b565`; deterministic frontier `50/58` UNCHANGED; P5 auth PAUSED; Vercel HELD.**
- **`CROSS_SHEET_CONTINUATION_REVIEW` caveat ADDED** (adapter-level): emitted when a bore references multiple plan sheets but the engine places/renders on a single winning sheet (cross-sheet continuation not assembled into one per-bore route). Surfaced in the candidate report (`evaluate`) + manifest warnings (+ a `referenced_sheets` field). Truth annotation ONLY — no coordinate/render change, never claims AUTO; engine/match layer untouched. Tested: 118′/88′ get it, 71′ does not.
- **Per-bore-termini scout (read-only, gitignored) + strict AUTO gate** — searched the real plan for DB markers / directional-bore notes / pit symbols / station brackets / matchline hints; AUTO only on a tight, unique, source-bound run that spans the bore length. The first-pass gate falsely passed 71′ (the M4 dash-phase trap) — caught + fixed with a run-length check.
- **NO ODOT row reaches AUTO:** 71′ → `NO_PER_BORE_TERMINI`; 118′ + 88′ → `NO_PER_BORE_TERMINI` + `CROSS_SHEET_CONTINUATION_REVIEW`. The plan draws ONE continuous dashed alignment (~40ft dashes / ~9ft gaps); DB markers are dash-level (~49ft spacing, 44–61 per sheet-group) so the DB token near each bore end is NOT a unique per-bore terminus; pit tokens exist but none bracket a span. **Tulsa-31 source lacks per-bore termini / a tight unique run.**
- **All three stay REVIEW** (Phase 2 renders unchanged); no new AUTO render; no fake AUTO. Verify: backend **148 passed / 1 skipped**; name-free; git tree clean.

**Next product-engine lane (separately authorized; NOT started): cross-sheet route assembly for REVIEW coverage (NOT AUTO promotion)** — assemble the matchline-split legs of a two-sheet bore (118′/88′) into one per-bore REVIEW route so the render covers BOTH sheets instead of a single winning sheet. Improves REVIEW coverage; does NOT unlock AUTO (AUTO still needs per-bore termini / a whole-run bore log, absent in Tulsa-31). **No second corpus yet.** Detail: [[hot]] / [[current-sprint]] / [[log]] continued 62.

### Prior — continued 61 (2026-06-22): ODOT Phase 2 — committed adapter run AS-IS on 71′/118′/88′; all three produced real job-local FINAL_REDLINE_PNG REVIEW renders; NO tracked code change; 50/58 UNCHANGED
**continued 61 — Phase 2 is a PROOF session: the committed Phase 1 adapter (`f3621c6`) was run AS-IS on all three Tulsa-31 bores; each produced a real job-local REVIEW redline. No tracked code changed.** Backend capability HEAD stays **`f3621c6`** (+ a continued-61 docs commit). **`origin/main` UNTOUCHED `068a279`; render `c19b565`; deterministic frontier `50/58` UNCHANGED; P5 auth PAUSED; Vercel HELD.**
- **All three bores RUNNABLE → REVIEW → real `FINAL_REDLINE_PNG`** (`bundle_origin=UPLOADED_CORPUS_ENGINE`, job-local, NOT summed into 50/58): 71′ sheet 11 (50,429 B), 118′ sheet 10 of [10,11] (56,799 B), 88′ sheet 12 of [11,12] (39,252 B). Each reason `DRAWN_EXTENT_COVERS_SPAN_NOT_TIGHT`; caveats `DRAWN_VECTOR_EXTENT`/`LOCATION_ONLY`/`DRAWN_EXTENT_EXCEEDS_BORE_SPAN`.
- **Two-sheet bores under-cover:** 118′ + 88′ reference two sheets but render only a SINGLE winning-sheet REVIEW marker (both sit at the MATCHLINE). The cross-sheet continuation is NOT assembled into a per-bore route.
- **AUTO stays blocked** by the banked M4 reality: the plan draws one continuous dashed alignment with NO per-bore termini; plus, for two-sheet bores, no cross-sheet route assembly. AUTO requires source evidence for per-bore termini / a tight unique span.
- **No tracked code changed in Phase 2** (adapter used as-is); the only artifacts are a gitignored proof harness + renders under `data/outputs/`. Verify: backend **132 passed / 1 skipped** (adapter + product API + naming guard + frontier lock `50/58`,`DETERMINISTIC_AUTO=49` + ODOT units); git tree clean.

**Next lane (started): `UPLOADED_CORPUS_ENGINE_ADAPTER_ODOT_PHASE_3_AUTO_GAP_FOUNDATION`** — make the blocker model truthful (add a `CROSS_SHEET_CONTINUATION_REVIEW` caveat for multi-sheet bores rendered on one sheet) + a read-only per-bore-termini scout (entry/exit pit symbols / DB marker pairs / bracketing notes / station-token brackets / matchline hints) + a strict AUTO gate (AUTO only on source-bound termini + tight span; else REVIEW/ABSTAIN). NOT staging/demo/second-corpus/registry expansion. Detail: [[hot]] / [[current-sprint]] / [[log]] continued 61.

### Prior — continued 60 (2026-06-22): ODOT Phase 1 — FIRST real uploaded-corpus ENGINE adapter COMMITTED; renders a real job-local FINAL_REDLINE_PNG (REVIEW, not AUTO); deterministic 50/58 UNCHANGED
**continued 60 — the first real uploaded-corpus engine adapter is committed: it RUNS the engine on a job's own uploaded plan + reviewed bore-log (NOT a recognized-corpus replay) and publishes a real job-local redline.** Backend `feat/truelinev2` `1759a0b`→**`f3621c6`** (`product: add uploaded-corpus engine adapter phase 1`, 6 files/+589/−2, pushed). **`origin/main` UNTOUCHED `068a279`; render `c19b565`; deterministic frontier `50/58` UNCHANGED; P5 auth PAUSED; Vercel HELD.**
- **What shipped:** `truelinev2/contracts/uploaded_corpus_engine_handoff.py` (+ 2 `/v2/product` routes, behind the default-OFF `TL2_PRODUCT_PIPELINE_API_OPTIN`) — `evaluate` (candidate report: RUNNABLE or named blocker) + `render` (engine run → REVIEW/AUTO stroke → job-local `FINAL_REDLINE_PNG` bundle, `bundle_origin=UPLOADED_CORPUS_ENGINE`). Name-free: dialect chosen by PATTERN, zero customer/project/location/operator literals; engine + renderer reused READ-ONLY (no engine/render/fixture/coordinate change). Additive-only manifest enum value `UPLOADED_CORPUS_ENGINE`.
- **First real uploaded-corpus engine path:** it RUNS the real engine (`load_borelog → select_dialect → run_match → render_redline_stroke`) on the job's OWN uploaded files — NOT the recognized-corpus replay (which serves a committed render for a known fingerprint).
- **Proven on real data:** a gitignored proof ran the real ODOT plan + 71′ bore log end-to-end and published a **real job-local `FINAL_REDLINE_PNG`** (50,347 B, real sha256) — a red DASHED REVIEW stroke along the drawn directional bore on sheet 11.
- **Current result is REVIEW, not AUTO.** Named blocker/caveat = `DRAWN_EXTENT_COVERS_SPAN_NOT_TIGHT` (the plan draws a continuous dashed alignment with no per-bore termini — the banked M4 finding; REVIEW is the honest ceiling on this data).
- **Frontier:** the job-local uploaded-corpus bundle is a SEPARATE `1/1` and is **NOT counted into the deterministic `50/58`** (frontier lock asserts `50/58`, `DETERMINISTIC_AUTO=49`, unchanged).
- **Verify:** backend **147 passed / 1 skipped** (adapter + product API paths + naming guard + frontier lock + recognized-corpus + M2 render + ODOT units); name-leak grep on tracked files zero; pushed ahead/behind 0/0.

**Next ODOT targets (separately authorized; PLAN-FIRST; NOT started):** render the **118′** (sheets 10–11) and **88′** (sheets 11–12) two-sheet REVIEW candidates via the same adapter. The product still needs **AUTO / generalization** work: a deterministic per-bore AUTO needs a packet whose bore log spans a WHOLE drawn run (real termini) or a per-bore anchor (entry/exit pit symbols / bracketing "VIA DIRECTIONAL BORE" note pairs); the adapter generalizes to other dialects/corpora as the plan-dialect registry grows. Detail: [[hot]] / [[current-sprint]] / [[log]] continued 60.

### Prior — continued 59 (2026-06-22): continued-57/58 recognized-corpus STAGING work COMMITTED in BOTH repos + pivot to ODOT; deterministic 50/58 UNCHANGED
**continued 59 — the two previously-uncommitted staging sessions (57 + 58) are now COMMITTED and pushed; the next product lane pivots to ODOT.** Backend `feat/truelinev2` `c3715d4`→**`9fa92b8`** (`product: add recognized-corpus handoff harness`, pushed). Web (SEPARATE `trueline-web-experience`) `main` `e9d838d`→**`39c7958`** (`staging: harden intake recognized-corpus flow`, pushed). **`origin/main` (backend) UNTOUCHED `068a279`; render `c19b565`; deterministic frontier `50/58` UNCHANGED; P5 auth PAUSED; Vercel HELD.**
- **What shipped (now committed):** a NARROW, honest recognized-corpus auto-handoff HARNESS — a POSITIVELY-recognized uploaded plan (exact sha256, per a gitignored deployment registry) whose engine-ready reviewed bore-log maps to a DRAWN deterministic log publishes that log's EXISTING committed engine render (commit `c19b565`) as a job-local **`FINAL_REDLINE_PNG`** bundle (`bundle_origin=DETERMINISTIC_RECOGNIZED_CORPUS`). NOT human-clicked, NOT arbitrary upload support. Name-free: real corpus names live ONLY in the gitignored registry; only a generic `recognized-corpus-NNN` id + label cross the API/UI. The ONLY contract change is an additive OPTIONAL manifest enum value; new config env `TL2_RECOGNIZED_CORPUS_REGISTRY` (default None ⇒ nothing recognized).
- **Staging hardening (now committed):** staging runs a PRODUCTION `next build` + `next start` (NOT `next dev`) ⇒ CONTENT-HASHED immutable chunks (the real fix for stale Cloudflare-cached dev chunks); `force-dynamic` in the root layout makes the product build viable. **Obsolete uploaded-corpus panels (the generic engine-handoff-readiness card + 2 honest-disclaimer boxes) were DELETED from the normal `/intake`.** The recognized-corpus card **auto-re-fetches when uploads change (`refreshKey`)** — never a stale pre-upload "not recognized". Manual source-anchor capture demoted to INTERNAL-only (`NEXT_PUBLIC_TL2_INTERNAL_PROOF`, default hidden). **Public staging stays Cloudflare Access gated.**
- **Recognized packages can render `FINAL_REDLINE_PNG`; the arbitrary uploaded-corpus engine is STILL NOT SOLVED.** The recognized bridge replays the EXISTING deterministic render for a KNOWN corpus — it is an internal/product harness, NOT the uploaded-corpus product (no plan-dialect detection, no source-anchor extraction, no generalized geometry solver).
- **Verify (continued-59):** backend **132 passed / 1 skipped** (recognized-corpus + product API + naming guard + frontier lock `50/58`, `DETERMINISTIC_AUTO=49`); web `tsc --noEmit` clean + contract parity + 71 live-product-read checks + production `next build` succeeds (all routes server-rendered on demand). Both repos pushed, ahead/behind 0/0; `origin/main` untouched.

**Next recommended lane (separately authorized; PLAN-FIRST): `UPLOADED_CORPUS_ENGINE_ADAPTER_ODOT_PHASE_1`** — the FIRST real non-Brenham uploaded-corpus engine target, chosen because **we have ODOT bore logs**. Goal: ODOT plan files + ODOT bore logs → parsed reviewed rows → plan source-evidence inventory → row-to-plan candidate binding → render ONE real `FINAL_REDLINE_PNG` or abstain with a NAMED blocker (`NO_START_ANCHOR_FOUND` / `NO_END_ANCHOR_FOUND` / `MULTIPLE_STATION_MATCHES` / `NO_ROUTE_GEOMETRY_FOUND`). NO manual source-anchor clicking; NO fake nearest-line guessing; NO customer/project/location/operator hardcoding in tracked code/UI/API/tests; NO more Brenham recognized-replay as product proof. **Victoria is NOT the next target.** Detail: [[hot]] / [[current-sprint]] / [[log]] continued 59.

### Prior — continued 56 (2026-06-22): M2 human-confirmed source-anchor RENDER lane shipped (Slices 1–3, BOTH repos) — first real uploaded-job redline; deterministic 50/58 frontier UNCHANGED + isolated
**continued 56 — a job's uploaded plan + reviewed rows + human-confirmed control points now render a REAL dashed redline PNG into a `mock_example:false` bundle, fully isolated from the deterministic engine.** Backend `feat/truelinev2` `b74d7de`→`585c187` (M2 Slices 1/2/3, all pushed; `origin/feat/truelinev2`=`585c187`). Web (SEPARATE `trueline-web-experience`) `main` `9d73dd2`→`e9d838d` (M2 Slices 2/3, pushed; web `origin/main`=`e9d838d`). **`origin/main` (backend) UNTOUCHED `068a279`; render `c19b565`; deterministic frontier 50/58 — the human-confirmed render lane is SEPARATE and NEVER summed in; NO deterministic engine/render/fixture/anchor/coordinate/frontier-accounting change; P5 auth PAUSED; Vercel/deploy HELD.**
- **M2 Slice 1 — source-anchor record + API (backend `47c9c2b`):** durable `source_anchor` contract (ordered display-space `control_points`, `page_number`, `plan_upload_id`, `reviewed_bore_log_id`, coordinate-FREE start/end identity, provenance `HUMAN_CONFIRMED_CONTROL_POINTS`); routes `POST/GET /v2/product/jobs/{job}/source-anchors[/{id}]`; renderability validation (≥2 in-bounds points, PLAN_PDF resolvable, rbl engine-ready) → VALIDATED/REJECTED + named blockers. Read-only `PlanPdf.page_bounds_display` helper (fitz stays in `pdf.py`).
- **M2 Slice 2 — plan-page display + capture UI (backend `e715731` · web `40b2866`):** `GET …/plan-pages/{plan_upload_id}` (page metadata: display-space bounds + raster size) + `GET …/plan-pages/{plan_upload_id}/{page}/raster` (in-memory PNG of the plan AS-IS — no overlay, no disk); read-only `PlanPdf.render_page_png`. Web: `PlanPageViewer` (header-bearing blob→`<img>` + SVG click-to-mark, screen→display-space mapping) + `ProductSourceAnchorCapture` (select plan/page, click route, coordinate-free identity, submit → VALIDATED/REJECTED).
- **M2 Slice 3 — render from validated source anchor (backend `585c187` · web `e9d838d`):** `POST …/source-anchors/{id}/render` → re-validate (VALIDATED + renderable + rbl still engine-ready) → render ALL the job's validated anchors via `render_redline_stroke(status="REVIEW")` (dashed, human-adjustable) from the stored control points ONLY → publish a `mock_example:false` bundle via the EXISTING publisher/store/handoff → set the job's `redline_manifest` + `artifact_bundle` slots; existing `GET …/artifacts[/{path}]` lists + serves the real PNG. New render adapter `truelinev2/render/source_anchor_render.py` (imports the renderer + publisher; NO match/extract/manual_adjudication/solver). Web: a "Render dashed redline" action + inline artifact (header-bearing blob→`<img>`).
- **Manifest / provenance (honest, additive-only):** per-log `status=DRAWN_REDLINE`, `provenance=OWNER_CONFIRMED_HUMAN_ADJUSTABLE` (an EXISTING enum — never `DETERMINISTIC_AUTO`); the ONLY schema change is an additive OPTIONAL top-level `bundle_origin` (`HUMAN_CONFIRMED_SOURCE_ANCHOR`), so the committed Brenham 50/58 example + frontier tests are UNTOUCHED. `closure`/`coverage` null (no faked footage). Per-job manifest `frontier "N/N"` is job-local, never the deterministic 50/58. Content-keyed bundle ⇒ idempotent re-render.
- **Current app capability (real, end-to-end):** create job → upload PLAN_PDF + BORE_LOG → manually enter/review/group bore rows → reach `engine_ready:true` → display the uploaded plan page → click route control points → validate a source anchor → RENDER a real dashed redline PNG → publish a real bundle/artifact → view the artifact via the existing list/serve path (inline web proof). The continued-55 workflow (Intake / uploads / reviewed-bore-log gate / read-only engine-handoff readiness) remains.
- **Verify (continued-56, targeted only — full suite not re-run since c53 1686/4):** backend **154 passed / 1 skipped** (render contract + product API + source-anchor + the deterministic frontier-lock tests, which passed UNCHANGED) + naming guard (operator `NAME_TOKENS`) 3 ✓ + AST guard (render adapter imports no match/extract/manual_adjudication); web node check (incl. 6 new Slice 3 checks) ✓ + `tsc --noEmit` clean; both `git diff --check` clean; both pushed ahead/behind 0/0. Fresh smoke: real PNG (valid magic + real sha256 + bytes), `mock_example:false`, `bundle_origin=HUMAN_CONFIRMED_SOURCE_ANCHOR`, provenance `OWNER_CONFIRMED_HUMAN_ADJUSTABLE`, slots set, artifact listed + served; committed Brenham frontier still `50/58`, `DETERMINISTIC_AUTO=49` (untouched); no automatic engine path ran.
- **HARD BLOCKERS / DEFERRED:** OCR NOT implemented (and NOT the next critical path — reviewed rows are the trusted input); automatic uploaded-corpus engine NOT implemented; no automatic plan-dialect / source-anchor extraction; no station-to-plan geometry solver for arbitrary uploads; KMZ / Google-Earth export BLOCKED unless verified geospatial geometry exists (pixel/PDF-only → no coordinates, never faked); auth/P5 PAUSED; Vercel/deploy HELD unless explicitly authorized; the `/redlines` reviewer route is NOT product truth.
- **BOUNDARIES held:** no OCR / no fake OCR; no automatic engine / no geometry inference / no station solving / no nearest-length matching / no invented control points; no KMZ coordinates / fake export / generated export files; deterministic fixtures + frontier accounting + endpoint anchors UNTOUCHED; backend `origin/main` `068a279` untouched; v1 backend untouched; no mobile / no deploy.

**Next recommended lanes (separately authorized; NOT started; PLAN-FIRST):** (a) harden the real workflow with a browser smoke against a local backend+web (needs the held staging wiring); (b) `TRUELINE_PRODUCT_INTERNAL_DEPLOYMENT_PLAN` — internal staging/deploy ONLY when owner authorizes (stable tunnel / edge auth; the product API is unauthenticated while P5 is paused); (c) export/KMZ ONLY after geometry provenance is clear (verified geospatial geometry); OCR remains a later convenience, never a truth source; the automatic uploaded-corpus engine (M2+) remains the larger future effort. Detail: [[current-sprint]] / [[log]] continued 56.

### Prior — continued 55 (2026-06-22): real end-to-end product WORKFLOW shipped (Slices A+B+C committed + pushed in BOTH repos) — Intake → reviewed-bore-log gate → engine-handoff readiness; NO render-truth change; frontier 50/58
**continued 55 — the v2 product moved from a read-only seed viewer to a real start-to-finish INTERNAL workflow.** Backend `feat/truelinev2` `89cf5b5`→`b74d7de` (Slices A/B/C, all pushed; `origin/feat/truelinev2`=`b74d7de`). Web (SEPARATE `trueline-web-experience`) `main` `d9465b0`→`9d73dd2` (pushed; web `origin/main`=`9d73dd2`). **`origin/main` (backend) UNTOUCHED `068a279`; render `c19b565`; frontier 50/58; NO engine/render/fixture/anchor/coordinate change; P5 auth PAUSED; NO deploy / NO Vercel env change.**
- **Slice A — Intake upload workflow (backend `f743374` · web `b11e432`):** create project/job; upload PLAN_PDF / BORE_LOG / GIS_ROUTE(`.kmz`/`.kml`) / **PHOTO**(`.jpg`/`.jpeg`/`.png`/`.webp` — NEW kind); content-addressed storage; `GET /v2/product/jobs` tenant job list; stored upload inventory + honest status. Raw files are STORED, never parsed.
- **Slice B — Reviewed bore-log gate (backend `c8b5ab3` · web `8378803`):** manual structured rows (`MANUAL_ENTRY`, NOT OCR); per-row review (confirm/reject); segment grouping; derived `engine_ready:true/false` + honest blockers; `GET …/reviewed-bore-logs/{rbl}` full-record read.
- **Slice C — Engine-handoff readiness (backend `b74d7de` · web `9d73dd2`):** `GET /v2/product/jobs/{job}/engine-handoff` read-only — ALWAYS `BLOCKED`/`runnable:false` with the exact named blockers `ENGINE_HANDOFF_NOT_IMPLEMENTED_FOR_UPLOADED_CORPUS` + `NO_PLAN_DIALECT_FOR_UPLOADED_CONVENTION` + `NO_SOURCE_ANCHORS_FOR_UPLOADED_CORPUS` + `GEOMETRY_SOLVER_IS_CORPUS_SPECIFIC` (+ input blockers `NO_PLAN_PDF_UPLOAD` / `NO_ENGINE_READY_REVIEWED_BORE_LOG`). Renders nothing, runs no engine, creates no slots/artifacts/bundles, mutates no job (proven). Honest bridge before the deferred uploaded-corpus engine work. Adds a tenant/job-scoped `list_reviewed_bore_logs` helper.
- **Supporting (backend `c7ed45b` · web `4d33eff`/`383b178`):** product-staging local wiring runbook (`truelinev2/docs/runbooks/product_staging_local_wiring.md`); web manifest slot-shape FIX (`4d33eff` — dashboard shows real `1/1`, was `0/0`); web artifact gallery (`383b178` — real redline PNG blobs via header-bearing fetch→blob, `<img>` not next/image).
- **Current internal workflow capability (real, end-to-end):** Intake page → create project/job → upload PDFs/KMZ/KML/bore-log files/photos → stored upload inventory + status → manual reviewed structured bore-log rows → row review → segment grouping → derived `engine_ready` → read-only engine-handoff readiness check with exact named blockers for an uploaded corpus. **No fake outputs anywhere.**
- **Verify (continued-55, targeted only — full suite not re-run since c53 1686/4):** backend Slice A 76 ✓ · Slice B 95 ✓ · Slice C 93 + naming guard with operator `NAME_TOKENS` 3 ✓; direct name-grep of Slice C files = zero; web `check-live-product-read.mjs` 18/18 ✓ + `tsc --noEmit` clean; both repos `git diff --check` clean; both pushed, ahead/behind 0/0.
- **Staging proven, deploy HELD:** local backend+web smoke green; ngrok tunnel path PROVEN end-to-end (`X-TL-*` headers bypass the free interstitial; ACAO reflected for the Vercel origin). Owner DECISION: **HOLD Vercel** — no env change / no deploy; revisit with a stable tunnel + edge auth (the product API is unauthenticated while P5 is paused).
- **HARD BLOCKERS / DEFERRED:** OCR NOT implemented (raw uploads stored, never parsed; no fake OCR); uploaded-corpus engine/render NOT implemented; no generic plan-dialect / source-anchor / geometry solver for arbitrary uploads; KMZ / Google-Earth export remains BLOCKED unless verified geospatial geometry exists (pixel/PDF-only → no coordinates, never faked); NO auth (P5 PAUSED); Vercel HELD (no deploy); the `/redlines` reviewer route is NOT product truth (do not present it as the product).
- **BOUNDARIES held all session:** no OCR / no fake OCR; no engine run; no fake redlines / proof artifacts; no KMZ coordinates / fake export; no generated export files; uploaded raw files stored, never parsed; every "blocked" surface names the exact missing piece. Backend `origin/main` `068a279` untouched; render/fixtures/anchors/coordinates unchanged; v1 backend untouched; no mobile / no deploy.

**Next recommended lanes (separately authorized; NOT started; PLAN-FIRST):** either (a) `TRUELINE_PRODUCT_INTERNAL_DEPLOYMENT_PLAN` — internal deployment ONLY once the workflow is considered ready, using a stable tunnel / edge auth if needed (the product API is unauthenticated while P5 is paused); or (b) `TRUELINE_PRODUCT_UPLOADED_CORPUS_ENGINE_M2_PLAN` — the real uploaded-corpus engine: plan-dialect detection + source-anchor model + uploaded-PDF-corpus adapter + reviewed-rows→engine-input adapter + generalized geometry solver. Do NOT present a seed-only URL as the product goal. Detail: [[current-sprint]] / [[log]] continued 55 (vault save `77c49c8`).

### Prior — continued 54 (2026-06-21): WEB read-side cutover Slice 1 + generic fixture scrub landed (SEPARATE `trueline-web-experience` repo, main → `d9465b0`); backend UNCHANGED; NO render-truth change; frontier 50/58
**continued 54 — a WEB-REPO-ONLY session; `TrueLine_Beta` backend/engine/contracts/seed UNCHANGED (capability HEAD stays `89cf5b5`, `origin/main` `068a279`, frontier 50/58, render `c19b565`). The internal web app's read-side now reads the real `/v2/product` API in product mode; offline fixtures are name-genericized.** Two web commits on the continued-53 web base `16c7095`: `5a4fdea` (cutover Slice 1) → `d9465b0` (fixture scrub), both pushed to web `origin/main`.
- **Web read-side cutover Slice 1 (`5a4fdea` `web: add live v2 product read adapter`, 8 files / +559 / −89).** A new `liveV2ProductApi` is selected by the seam (`src/lib/api/index.ts`) when `NEXT_PUBLIC_TL2_PRODUCT_API=1` (+ `_API_BASE`/`_TENANT`/`_JOB_ID`); otherwise the offline fixture client. `fetchProduct` sends `X-TL-Tenant`/`X-TL-Session` DEV STAND-IN headers (NOT auth) and THROWS on failure (NO silent fallback to mock). `reviews.engineRedlineManifest()` composes `/v2/product/jobs/{job}/redline-manifest` (summary counts) + `/artifacts` into the manifest view (real totals/frontier/bundle/artifact-count; per-log body honestly EMPTY — never fabricated). A read-only v2 job-status strip reads `/closeout` + `/billing` + `/export-package` + `/kmz-export` as server truth (KMZ pixel-only → "unavailable"). All portfolio/field methods return honest-empty; `sync` = offline. Shared `/v2/reviewer` reads extracted to `reviewerReads.ts` (no duplicate reviewer logic). Verify: zero-dep node check 21/21; `tsc --noEmit` clean; lint clean except a PRE-EXISTING `SummaryRail.tsx` error (untouched file).
- **Generic fixture scrub (`d9465b0` `web: genericize offline fixture names`, 17 files incl. rename `mock/brenham.ts`→`mock/internalDemo.ts`).** Project IDs `p-cedar-ridge`/`p-fm-1842`/`p-oakdale` → `demo-project-001/002/003` (all refs); brenham fixture id → `demo-project-internal`; sheet titles, hero-map road labels, playback note, closeout packet names, dashboard/topbar/redlines titles, a reviewer role string, and an adapter comment genericized. Zero realistic-name tokens remain in `src/**/*.ts(x)`. Real-engine JSON fixtures left UNTOUCHED (they carry the real bundle `project_id` as checksummed data the adapter cross-checks). Product adapter/seam behavior unchanged (`liveV2Product.ts` not in the scrub changeset).
- **HONEST LIMITED VIEWS (deferred by decision):** the rich per-log manifest panel needs a backend full-manifest read route (D-2 deferred — NOT built); product artifact IMAGES are availability-only (header-bearing fetch→blob deferred) — the slice shows artifact metadata, not images. No fabrication.
- **BOUNDARIES:** web-repo only; backend `TrueLine_Beta` UNTOUCHED (`8f6cbbd`, `origin/main` `068a279`); no backend routes / auth / OCR / KMZ coordinates / export-file generation; no engine/render/fixture/anchor/coordinate change; no deploy; no dependency changes; P5 auth still PAUSED.

**Next recommended lane (separately authorized; NOT started; PLAN-FIRST):** either (a) `TRUELINE_PRODUCT_WEB_READ_SIDE_CUTOVER_SLICE_1B` — header-bearing artifact images (fetch→blob) + a small read-only backend full-manifest route to power the rich per-log panel; or (b) `TRUELINE_PRODUCT_STAGING_WIRING_PLAN` — run the backend with `TL2_PRODUCT_PIPELINE_API_OPTIN=1` + the seed `product_store` + the web origin in CORS so the web reads the real seed job end-to-end (NO production / `main`). Detail: [[current-sprint]] / [[log]] continued 54.

### Prior — continued 53 (2026-06-21): real product workflow SEED (Slice 1, log3) landed — proves the product spine end-to-end on ONE real drawn log; P5 auth PAUSED; active direction = finish workflow + retire mocks; NO render-truth change; frontier 50/58
**continued 53 — first REAL end-to-end product-store workflow seed; NO engine execution / renderer / fixture / anchor / coordinate / contract / API / config change; frontier UNCHANGED 50/58; render commit stays `c19b565`; latest capability/proof HEAD `89cf5b5`; v2 suite 1686 passed / 4 skipped.** A proof harness + 9 deterministic tests drive the full product spine on one real drawn log (log3) using only real v2 contracts — no new capability, no mock truth.
- **Real workflow seed LANDED (lane `TRUELINE_PRODUCT_REAL_WORKFLOW_SEED_SLICE_1_LOG3`, HEAD/origin `89cf5b5`).** Commit `89cf5b5` `proof: add real product workflow seed` (2 files / +620; staged explicitly, NO `git add -A`; pushed, HEAD = origin = `89cf5b5`, tree clean). Exactly 2 NEW files; **NO contract/API/config change**.
  - `truelinev2/proof/run_product_workflow_seed.py` — parameterized `seed_workflow()` + `main()` (real log3). Drives `customer_project → processing_job → upload → reviewed_bore_log (MANUAL untrusted rows, reviewed + grouped) → manifest_handoff finalize (a FRESH `mock_example:false` subset bundle published via the existing publisher/store/handoff, real sha256/bytes) → manifest-backed proof artifacts → kmz_export safety → closeout evaluate (NON-privileged) → billing compute (server cost rules) → export_package descriptor`. Writes ONLY under gitignored `data/outputs/truelinev2/product_store_seed`.
  - `truelinev2/tests/test_product_workflow_seed.py` — 9 deterministic tests (tmp_path; no httpx/TestClient): manual rows untrusted → gate opens after review+group; handoff REJECTS not-ready rbl / `mock_example:true` / tampered bundles; real subset bundle finalizes; slots VALIDATED; artifact reads manifest-backed + path-guarded; KMZ BLOCKED (pixel-only); closeout READY_FOR_APPROVAL (no privileged transition); billing COMPUTED from generic test-safe rules; export descriptor-only (no generated files); seed output under gitignored data/outputs; name-token guard.
  - **Real harness result:** upload queued → rbl `engine_ready=True` → handoff `SUCCEEDED` → bundle `seed-project-c19b565-abeaf35b1848` (two real log3 PNGs: sheet-2 + sheet-3 strokes, real sha256 + real bytes) → KMZ `BLOCKED[UNSUPPORTED_PIXEL_ONLY]` → closeout `READY_FOR_APPROVAL` → billing `COMPUTED` → export `READY` (descriptor/reference only).
  - **Verify:** targeted seed 9 ✓; product-spine group with operator `NAME_TOKENS` enforced 194 ✓ / 1 skip; direct name grep of both new files = zero; **full v2 suite 1686 passed / 4 skipped** (delta exactly +9 vs the committed 1677/4 baseline). Cleanliness gate: AST unused-import audit; full-diff review; only the 2 allowed files; no dead code / duplicate spine / broad utilities / out-of-scope edits; no runtime records committed; NO `git add -A`.

**TRUST FACTS (continued 53):** a REAL internal workflow seed — NOT the final all-50 unified bundle; does NOT claim all 50/58 are pipeline-produced; does NOT use the hand-authored `mock_example:true` example manifest as product truth; does NOT weaken anti-mock guards (proven: the handoff still rejects mock/tampered bundles); no fake OCR / no fake KMZ coordinates / no generated export files / no fake proof artifacts; runtime seed output is gitignored (`data/outputs/...`) and was not committed.

**DIRECTION CORRECTION (continued 53):** **P5 auth/role is PAUSED** — do NOT treat it as the active next milestone; no auth infrastructure, no privileged closeout transitions exposed. The active product direction is **finish the end-to-end product workflow + retire mock/demo/stub runtime behavior**. The v1 salvage audit remains blueprint/context only — never copy v1 code or revive v1 backend behavior; rebuild useful v1 concepts cleaner in v2.

**Next recommended lane (separately authorized; NOT started; PLAN-FIRST):** `TRUELINE_PRODUCT_WEB_READ_SIDE_CUTOVER_PLAN` — plan how the internal web app (`trueline-web-experience`) reads real v2 `/v2/product` API + product-store state instead of `mockApi` (single seam `src/lib/api/index.ts`); the seed job is the first real job it can read; no coding until approved. Alternate: `TRUELINE_PRODUCT_REAL_WORKFLOW_SEED_SLICE_2_PLAN` (expand/harden the seed). Detail: [[current-sprint]] / [[log]] continued 53.

### Prior — continued 52 (2026-06-21): backend/API Slice 4 Status/Closeout/Billing/Export landed — 4-slice backend/API wiring plan COMPLETE; NO render-truth change; frontier 50/58
**continued 52 — FOURTH (final) backend/API wiring slice (downstream status spine: KMZ safety / closeout / billing / export package); NO engine execution / renderer / fixture / anchor / corpus / census / parent-model / placement / contract change; frontier UNCHANGED 50/58; render commit stays `c19b565`; HEAD `eea8378`; v2 suite 1677 passed / 4 skipped.** Seven thin, default-OFF route adapters (server-derived reads + safe server actions) over the existing contracts — the contract spine is UNCHANGED. **This COMPLETES the 4-slice backend/API wiring plan.**
- **Backend/API Slice 4 LANDED (lane `TRUELINE_PRODUCT_BACKEND_API_SLICE_4_STATUS_CLOSEOUT_BILLING_EXPORT_READS`, Slice 4/final; HEAD/origin `eea8378`).** Commit `eea8378` `api: add product status closeout billing export routes` (3 files / +370 / −5; staged explicitly, NO `git add -A`; pushed, HEAD = origin = `eea8378`, tree clean). Parent docs save `a7c9289`. Default-OFF; nothing activates unless `TL2_PRODUCT_PIPELINE_API_OPTIN=1`.
  - `truelinev2/api/product_pipeline_routes.py` — +7 Slice 4 routes + 1 server cost-rule helper (`_server_cost_rule_set`); extended `_to_http` (adds 404 for Closeout/Billing/ExportPackage-NotFound + 409 for CloseoutStateError) + `_CONTRACT_ERRORS` (CloseoutReviewError/BillingSummaryError/ExportPackageError). Thin adapters over `kmz_export` + `closeout_review` + `billing_summary` + `export_package` (truth stays in the contracts). Identity = VERIFIED `X-TL-Tenant` slug → `customer_project_id == ctx.tenant.value`, NEVER from the URL path or request body. GET/POST only.
  - `truelinev2/config.py` — added `product_billing_cost_rules_path` (env `TL2_PRODUCT_BILLING_COST_RULES`, default None): the SERVER/deployment source for billing cost rules (rates are deployment data, NEVER baked in code and NEVER trusted from a client request).
  - `truelinev2/tests/test_product_pipeline_api.py` — +13 Slice 4 tests; updated `PRODUCT_PATHS` (16→23); updated `_settings` + the settings-default assertion.
  - **Routes (ONLY these 7, all under `/v2/product`):** `GET /jobs/{job_id}/kmz-export` (evaluate KMZ safety read-only; pixel/PDF-only → BLOCKED, never fakes coords); `POST /jobs/{job_id}/closeout/evaluate` (create+evaluate server-authoritative status; NON-privileged); `GET /jobs/{job_id}/closeout` (record + derived summary); `POST /jobs/{job_id}/billing/compute` (server-compute; server cost rules; no client input); `GET /jobs/{job_id}/billing` (record + derived view); `POST /jobs/{job_id}/export-package/assemble` (descriptor-of-references; no file generated); `GET /jobs/{job_id}/export-package` (record + derived view).
  - **Trust model:** the three POST routes take NO request body — everything is server-derived. Billing cost rules come ONLY from `TL2_PRODUCT_BILLING_COST_RULES` (deployment config); no client rates / no client itemized billing truth. NO privileged closeout routes (no lock/approve/close/reject/reopen/unlock); no verified-role system invented. KMZ is read/evaluate-only and abstains on pixel/PDF-only geometry (no coordinates). Export package is a DESCRIPTOR-of-references only — no PDF/HTML/ZIP/KMZ/DOCX/export file generated.
  - **Verify:** targeted product-pipeline API 60 ✓ (15 Slice 1 + 17 Slice 2 + 15 Slice 3 + 13 Slice 4); mounting-affected API group (reviewer + run-assembly + product) + naming guard 98 ✓ (no mounting regression); naming guard with operator `NAME_TOKENS` 3 ✓ + direct grep of all 3 changed files = zero real-name tokens; **full v2 suite 1677 passed / 4 skipped** (delta exactly +13 vs the committed 1664/4 baseline).

**SLICE 4 BOUNDARIES (what it does NOT do):** server-derived reads + safe server actions only; NO privileged closeout transitions / verified-role system / external auth; KMZ never fakes coordinates; billing rates only from server config (never client); export package never generates a file; NO web-UI / mobile / deploy / production-`main`; NO engine execution / renderer / fixture / anchor / coordinate change; **v1 backend behavior UNTOUCHED** (no v1 snapping, no v1 billing/export); the contract spine is UNCHANGED. Default-OFF. `origin/main` `068a279` untouched.

**ARCHITECTURE (clean-room + modular):** routes live under `truelinev2/api/...`; product contracts stay under `truelinev2/contracts/...`; route adapters are THIN; product truth + all validation stay INSIDE the contracts. **MILESTONE — 4-slice backend/API wiring plan COMPLETE:** Slice 1 Foundation (`986ae74`) → Slice 2 Inputs/Review-Gate (`bd55273`) → Slice 3 Manifest/Proof-Reads (`baf36e0`) → Slice 4 Status/Closeout/Billing/Export (`eea8378`). The API drives the complete contract spine: `upload_pipeline → reviewed_bore_log → manifest_handoff → kmz_export safety → closeout_review → billing_summary → export_package`. (V1 salvage-audit compliant — blueprint, not code: no v1 reuse, no client-side truth, no fake coords/files, no monolith coupling; useful v1 concepts rebuilt as cleaner v2 capabilities.)

**Next recommended milestone (separately authorized; NOT started; PLAN-FIRST):** `TRUELINE_PRODUCT_P5_FULL_IMPL_AUTH_ROLE_PLAN` — external auth / verified identity + the verified-role source for privileged closeout transitions (deferred through Slice 4) + a production-safe activation strategy (flag stays default-OFF until then) + the internal test-app wiring boundary; keep v2 separate from v1; preserve the salvage-audit blueprint without copying legacy code. Detail: [[current-sprint]] / [[log]] continued 52.

### Prior — continued 51 (2026-06-21): backend/API Slice 3 Manifest Handoff + Proof Reads landed (default-OFF /v2/product handoff record/finalize + manifest/artifact reads) — contract→routes wiring CONTINUED; NO render-truth change; frontier 50/58
**continued 51 — THIRD backend/API wiring slice (engine-output manifest handoff + read-only proof access); NO engine execution / renderer / fixture / anchor / corpus / census / parent-model / placement / contract change; frontier UNCHANGED 50/58; render commit stays `c19b565`; HEAD `baf36e0`; v2 suite 1664 passed / 4 skipped.** Five thin, default-OFF route adapters over the existing handoff/store/consumer contracts — the contract spine is UNCHANGED.
- **Backend/API Slice 3 LANDED (lane `TRUELINE_PRODUCT_BACKEND_API_SLICE_3_MANIFEST_PROOF_READS`, Slice 3; HEAD/origin `baf36e0`).** Commit `baf36e0` `api: add manifest proof read routes` (2 files / +437 / −9; staged explicitly, NO `git add -A`; pushed, HEAD = origin = `baf36e0`, tree clean). Parent docs save `a809ce8`. Default-OFF; nothing activates unless `TL2_PRODUCT_PIPELINE_API_OPTIN=1`.
  - `truelinev2/api/product_pipeline_routes.py` — +5 Slice 3 routes + 2 request models (`ManifestHandoffRecord`, `ManifestHandoffFinalize`) + 2 small route helpers (`_staged_bundle_root`, `_open_job_bundle`); extended `_to_http` (adds 404 for HandoffNotFound/BundleNotReadable/ArtifactNotServable + 409 for HandoffStateError) + `_CONTRACT_ERRORS` (ManifestHandoffError, ConsumerError). Thin adapters over `manifest_handoff` + `published_bundle_consumer` (truth + bundle validation stay in the contracts). Identity = VERIFIED `X-TL-Tenant` slug → `customer_project_id == ctx.tenant.value`, NEVER from the URL path or request body. GET/POST only.
  - `truelinev2/tests/test_product_pipeline_api.py` — +15 Slice 3 tests; updated `PRODUCT_PATHS` (11→16); extended the no-identity-in-path/body coverage to the 2 Slice 3 request models.
  - **Routes (ONLY these 5, all under `/v2/product`):** `POST /jobs/{job_id}/manifest-handoffs` (record ATTEMPT; does NOT run the engine/renderer); `POST /jobs/{job_id}/manifest-handoffs/{engine_run_id}/finalize` (validate + durably store + attach `redline_manifest`/`artifact_bundle` slots via the contract; returns SUCCEEDED/REJECTED/FAILED); `GET /jobs/{job_id}/redline-manifest` (validated manifest slot, descriptor/state ONLY); `GET /jobs/{job_id}/artifacts` (manifest-backed FINAL_REDLINE_PNG refs only); `GET /jobs/{job_id}/artifacts/{artifact_path:path}` (serve ONE proof artifact by manifest path).
  - **Trust model:** the engine bundle is resolved from a SAFE server-side staging ref (`<job_dir>/engine_outputs/<ref>`, single-segment regex → no traversal; NEVER a raw client filesystem path); bundle VALIDATION is the contract's (`store_bundle`); reads go through `StaticBundleConsumer(enable=True).open_bundle()` (re-enforces the website read contract: validate + checksum + `mock_example:false`); artifact serving via `resolve_artifact` (manifest allowlist + `is_safe_relative_path` + in-root containment). Handoff keyed by the contract's `engine_run_id`; the serve route uses `{artifact_path:path}` because manifest artifact paths contain slashes.
  - **Verify:** targeted product-pipeline API 47 ✓ (15 Slice 1 + 17 Slice 2 + 15 Slice 3); mounting-affected API group (reviewer + run-assembly + product) + naming guard 85 ✓ (no mounting regression); naming guard with operator `NAME_TOKENS` 3 ✓ + direct grep of both changed files = zero real-name tokens; **full v2 suite 1664 passed / 4 skipped** (delta exactly +15 vs the committed 1649/4 baseline).

**SLICE 3 BOUNDARIES (what it does NOT do):** does NOT run the engine or renderer (the placement engine's output bundle is a GIVEN input, validated by the contract); proof reads are manifest-backed only; NO fake artifacts generated (tests use tmp_path; nothing under `data/`/`outputs/` committed); NO KMZ / closeout / billing / export-package routes (Slice 4); NO `python-multipart`, NO `httpx`, NO `TestClient`, no new deps; no duplicate backend app; no broad utility layer; no dead routes; no engine/render/fixture/anchor/coordinate change; no web-UI / mobile / deploy / production-`main`; **v1 backend behavior UNTOUCHED** (no v1 snapping, no v1 billing/export); the contract spine is UNCHANGED. Default-OFF. `origin/main` `068a279` untouched.

**ARCHITECTURE (clean-room + modular):** routes live under `truelinev2/api/...`; product contracts stay under `truelinev2/contracts/...`; route adapters are THIN; product truth + bundle validation stay INSIDE the contracts. Backend/API wiring now has **Slice 1 Foundation (`986ae74`) + Slice 2 Inputs/Review-Gate (`bd55273`) + Slice 3 Manifest/Proof-Reads (`baf36e0`)**. The contract spine remains complete: `upload_pipeline → reviewed_bore_log → manifest_handoff → kmz_export safety → closeout_review → billing_summary → export_package`.

**Next recommended lane (separately authorized; NOT started):** `TRUELINE_PRODUCT_BACKEND_API_SLICE_4_STATUS_CLOSEOUT_BILLING_EXPORT_READS` — kmz_export safety read + closeout evaluate/read + billing compute/read + export-package assemble/read; safe server-derived status/actions only; privileged closeout transitions still DEFERRED until the verified-role source is decided; thin adapters over the contracts; same default-OFF flag. Detail: [[current-sprint]] / [[log]] continued 51.

### Prior — continued 50 (2026-06-21): backend/API Slice 2 Inputs + Review Gate landed (default-OFF /v2/product uploads + reviewed-bore-log review-gate routes) — contract→routes wiring CONTINUED; NO render-truth change; frontier 50/58
**continued 50 — SECOND backend/API wiring slice (inputs + the reviewed-bore-log review gate); NO engine/renderer/fixture/anchor/corpus/census/parent-model/placement/contract change; frontier UNCHANGED 50/58; render commit stays `c19b565`; HEAD `bd55273`; v2 suite 1649 passed / 4 skipped.** Seven thin, default-OFF route adapters over the existing contracts — the contract spine is UNCHANGED.
- **Backend/API Slice 2 LANDED (lane `TRUELINE_PRODUCT_BACKEND_API_SLICE_2_INPUTS_REVIEW_GATE`, Slice 2; HEAD/origin `bd55273`).** Commit `bd55273` `api: add product input review gate routes` (2 files / +474 / −10; staged explicitly, NO `git add -A`; pushed, HEAD = origin = `bd55273`, tree clean). Parent docs save `4c438ed`. Default-OFF; nothing activates unless `TL2_PRODUCT_PIPELINE_API_OPTIN=1`.
  - `truelinev2/api/product_pipeline_routes.py` — +7 Slice 2 routes + 7 Slice 2 request models; extended `_to_http` (adds 404 for ReviewedBoreLogNotFound/RowNotFound/GroupNotFound + 409 for UploadsClosed) + a shared `_CONTRACT_ERRORS` tuple used by all 7 routes. Thin adapters over `upload_pipeline` + `extracted_row` + `reviewed_bore_log` (truth stays in the contracts). Identity = VERIFIED `X-TL-Tenant` slug → `customer_project_id == ctx.tenant.value`, NEVER from the URL path or request body. GET/POST only; upload bytes via base64-JSON (NO `python-multipart`).
  - `truelinev2/tests/test_product_pipeline_api.py` — +17 Slice 2 tests; updated `PRODUCT_PATHS` (4→11); extended the no-identity-in-path/body coverage to all 7 Slice 2 request models.
  - **Routes (ONLY these 7, all under `/v2/product`):** `POST /jobs/{job_id}/uploads` (base64 JSON; stays `extraction_status:"queued"`, raw input UNTRUSTED); `POST /jobs/{job_id}/reviewed-bore-logs`; `POST …/reviewed-bore-logs/{reviewed_bore_log_id}/rows`; `POST …/rows/{row_id}/review`; `POST …/groups`; `POST …/groups/{group_id}/status`; `GET …/review-queue` (exposes the DERIVED `engine_eligible_row_ids` + `engine_ready`).
  - **Verify:** targeted product-pipeline API 32 ✓ (15 Slice 1 + 17 Slice 2); mounting-affected API group (reviewer + run-assembly + product) + naming guard 70 ✓ (no mounting regression); naming guard with operator `NAME_TOKENS` 3 ✓ + direct grep of both changed files = zero real-name tokens; **full v2 suite 1649 passed / 4 skipped** (delta exactly +17 vs the committed 1632/4 baseline).

**SLICE 2 BOUNDARIES (what it does NOT do):** raw uploads + imported/extracted rows stay UNTRUSTED (`extraction_status:"queued"`; rows start UNREVIEWED) — NO OCR/AI runs; engine eligibility is DERIVED by the `reviewed_bore_log` contract from review + grouping (never stored as mutable truth, never conferred by the API). NO manifest-handoff / proof-artifact / KMZ / closeout / billing / export-package routes (later slices); NO `python-multipart`, NO `httpx`, NO `TestClient`, no new deps; no duplicate backend app; no broad utility layer; no dead routes; no engine/render/fixture/anchor/coordinate change; no web-UI / mobile / deploy / production-`main`; **v1 backend behavior UNTOUCHED** (no v1 snapping, no v1 billing/export); the contract spine is UNCHANGED. Default-OFF. `origin/main` `068a279` untouched.

**ARCHITECTURE (clean-room + modular):** routes live under `truelinev2/api/...`; product contracts stay under `truelinev2/contracts/...`; route adapters are THIN (delegate per call; the `reviewed_bore_log` 409 pre-check mirrors the Slice 1 idiom rather than reimplementing contract logic); product truth stays INSIDE the contracts. Backend/API wiring now has **Slice 1 Foundation (`986ae74`) + Slice 2 Inputs/Review-Gate (`bd55273`)**. The contract spine remains complete: `upload_pipeline → reviewed_bore_log → manifest_handoff → kmz_export safety → closeout_review → billing_summary → export_package`.

**Next recommended lane (separately authorized; NOT started):** `TRUELINE_PRODUCT_BACKEND_API_SLICE_3_MANIFEST_PROOF_READS` — manifest-handoff record/finalize + redline-manifest read + artifact-bundle listing + proof-artifact read/access; everything manifest-backed + tenant-scoped; NO engine execution / renderer / fixture change / fake artifacts; thin adapters over the contracts; same default-OFF flag. Detail: [[current-sprint]] / [[log]] continued 50.

### Prior — continued 49 (2026-06-21): backend/API Slice 1 Foundation landed (default-OFF /v2/product project/job routes) — contract→routes wiring STARTED; NO render-truth change; frontier 50/58
**continued 49 — FIRST backend/API wiring slice (foundation routes only); NO engine/renderer/fixture/anchor/corpus/census/parent-model/placement/contract change; frontier UNCHANGED 50/58; render commit stays `c19b565`; HEAD `986ae74`; v2 suite 1632 passed / 4 skipped.** First crossing from contracts into routes — but the contract spine is UNCHANGED; this slice only ADDS thin, default-OFF route adapters.
- **Backend/API Slice 1 Foundation LANDED (lane `TRUELINE_PRODUCT_BACKEND_API_WIRING_PLAN`, Slice 1; HEAD/origin `986ae74`).** Commit `986ae74` `api: add product pipeline foundation routes` (4 files / +372; staged explicitly, NO `git add -A`; pushed, HEAD = origin = `986ae74`, tree clean). Default-OFF; nothing activates unless `TL2_PRODUCT_PIPELINE_API_OPTIN=1`.
  - `truelinev2/config.py` — added `product_pipeline_api_optin` (DEFAULT OFF, env `TL2_PRODUCT_PIPELINE_API_OPTIN`) + `product_store_root` (env `TL2_PRODUCT_STORE_ROOT`, default gitignored `data/outputs/truelinev2/product_store`).
  - `truelinev2/api/app.py` — mounts the product router ONLY when `settings.product_pipeline_api_optin` (mirrors the reviewer/run-assembly opt-in pattern; +4 lines).
  - `truelinev2/api/product_pipeline_routes.py` — NEW `/v2/product` foundation router (context-bearing). Routes: `POST/GET /project`, `POST /jobs`, `GET /jobs/{job_id}`, `POST /jobs/{job_id}/transition`. Thin adapters over the `customer_project` + `processing_job` contracts (truth stays in the contracts). Identity = VERIFIED `X-TL-Tenant` slug → `customer_project_id == ctx.tenant.value`, NEVER from the URL path or request body. GET/POST only; server-authoritative lifecycle (illegal transition → 409); errors mapped 404/409/400/403; `at`/`by` server-generated.
  - `truelinev2/tests/test_product_pipeline_api.py` — 15 tests, repo convention (NO httpx/TestClient): `app.routes` mount inspection (flag-off dormant / flag-on exact paths + GET/POST-only + context dep) + direct route-function calls over a tmp `product_store_root`; project/job create+get+transition happy paths; conflict 409, missing 404, invalid slug 400; **tenant isolation** (B cannot read/transition A's job/project → 404); **no customer_project id accepted from path or body**.
  - **Verify:** targeted 15 ✓; existing API tests (reviewer + run-assembly + product) 50 in one run ✓ (no mounting regression); naming guard with operator `NAME_TOKENS` ✓ zero leakage; **full v2 suite 1632 passed / 4 skipped** (delta exactly +15 vs the committed 1617/4 baseline).

**SLICE 1 BOUNDARIES (what it does NOT do):** NO upload / reviewed-bore-log / manifest-handoff / proof-artifact / KMZ / closeout / billing / export-package routes (later slices); NO `python-multipart`, NO `httpx`, no new deps; no duplicate backend app; no broad utility layer; no dead route surface; no engine/render/fixture/anchor/coordinate change; no web-UI / mobile / deploy / production-`main`; **v1 backend behavior UNTOUCHED** (no v1 snapping, no v1 billing/export); the contract spine is UNCHANGED. Default-OFF — nothing activates unless `TL2_PRODUCT_PIPELINE_API_OPTIN=1`. `origin/main` `068a279` untouched.

**ARCHITECTURE (clean-room + modular):** routes live under `truelinev2/api/...`; product contracts stay under `truelinev2/contracts/...`; product storage under the v2 `product_store_root`; route adapters are THIN; product truth stays INSIDE the contracts. The contract spine remains complete: `upload_pipeline → reviewed_bore_log → manifest_handoff → kmz_export safety → closeout_review → billing_summary → export_package`. Backend/API wiring has now STARTED (Slice 1 Foundation only).

**Next recommended lane (separately authorized; NOT started):** `TRUELINE_PRODUCT_BACKEND_API_SLICE_2_INPUTS_REVIEW_GATE` — uploads/input registration (untrusted/`queued`; base64-JSON, no `python-multipart`) + reviewed-bore-log create/rows/review/group/status + review-queue + eligibility-gate exposure; thin adapters over the contracts; same default-OFF flag. Detail: [[current-sprint]] / [[log]] continued 49.

### Prior — continued 48 (2026-06-21): export_package versioned closeout model landed — product spine now CONTRACT-COMPLETE end-to-end + all 3 job output slots produced; NO render-truth change; frontier 50/58
**continued 48 — the SEVENTH (final spine) permanent product-pipeline slice (versioned closeout export package); NO engine/renderer/fixture/anchor/corpus/census/parent-model/placement/flag change; frontier UNCHANGED 50/58; render commit stays `c19b565`; engine/contract HEAD `c5185d5`; v2 suite 1617 passed / 4 skipped.**
- **Export package versioned closeout LANDED (lane `TRUELINE_PRODUCT_EXPORT_PACKAGE_VERSIONED_CLOSEOUT`, HEAD/origin `c5185d5`).** Commit `c5185d5` `contracts: add export package versioned closeout model` (3 files / +984; staged explicitly, NO `git add -A`; pushed, HEAD = origin = `c5185d5`, tree clean). One GENERIC, contract-only, pure-stdlib module + its test + the +1-line guard extension. Fixes v1's defect (browser `document.write` print, blob URLs, no stored artifact / versioning / audit — salvage audit 6a/6b, DO-NOT-COPY 10) with a durable, content-addressed package DESCRIPTOR.
  - `truelinev2/contracts/export_package.py` — ONE durable `_export_package.json` per job (singleton) with append-only versioned `revisions[]`. Server-authoritative status `DRAFT · BLOCKED · ASSEMBLED · READY · FINAL` (two-tier per §8: READY at closeout ≥ READY_FOR_APPROVAL, FINAL at APPROVED). A DESCRIPTOR / manifest-of-references — NOT a rendered document (no PDF/HTML/binary generated). `assemble_export_package` resolves trusted sources into items (job summary; reviewed_bore_log if engine-eligible; redline manifest + manifest-backed artifacts from the VALIDATED sha256-verified durable bundle; KMZ only when `kmz_export` EXPORTABLE; closeout status; billing snapshot by reference). Reproducible `package_content_hash` over the canonical descriptor (regeneration appends a revision when trusted refs change — prior superseded, retained; identical reassemble revalidates idempotently). On success sets the job `export_package` output slot to a REFERENCE (after the record is written; no lifecycle transition, no completion/delivery, no closeout/billing override).
  - **Decisions baked in (A–E approved) + slot clarification:** A 5-state two-tier status; B missing/untrusted manifest = HARD blocker; C `assemble` sets the job `export_package` slot (reference-only, after the record); D descriptor-not-document; E one commit. KMZ included ONLY when `kmz_export` EXPORTABLE; pixel/PDF-only OMITS KMZ with a WARNING and NEVER blocks the rest of the package. Billing snapshot/reference ONLY — never recomputed by export. Closeout controls readiness/finality (no package-approve action). HARD (→ BLOCKED): JOB_NOT_IN_CLOSEOUT_RANGE / MISSING_MANIFEST / MISSING_ARTIFACT_BUNDLE / UNTRUSTED_OUTPUT / MANIFEST_NOT_RESOLVABLE / REVIEWED_BORE_LOG_NOT_READY. READINESS (→ ASSEMBLED): CLOSEOUT_NOT_READY / CLOSEOUT_REVIEW_MISSING. FINALIZATION (→ READY): CLOSEOUT_NOT_APPROVED. WARNINGS: KMZ_NOT_INCLUDED / BILLING_NOT_FINAL / BILLING_SUMMARY_MISSING / REVIEWED_BORE_LOG_NOT_RESOLVABLE / OPEN_ENGINE_BLOCKERS.
  - Tests: `test_export_package_versioned_closeout_contract.py` (24); the generic no-specific-names guard extended to all 9 pipeline modules (+1 line). Verify: targeted 24 ✓; sibling product-contract group 138 in one run; naming guard with operator `NAME_TOKENS` (real customer/location tokens) ✓ zero leakage; **full v2 suite 1617 passed / 4 skipped** (delta exactly +24 vs the committed 1593/4 baseline). NO artifacts committed (tmp_path; `data/`+`outputs/` gitignored); NO generated `.kmz`/`.kml`/`.png`/`.json`; NO rendered document / export file; NO deploy; NO mobile; NO engine/render/fixture/coordinate change; reads `closeout_review`/`billing_summary`/`kmz_export` but mutates none; never drives the job lifecycle; `origin/main` `068a279` untouched.

**EXPORT PACKAGE INVARIANTS:** ONE durable versioned `_export_package.json` per job; append-only revisions; server-authoritative status (no client/UI override survives a reassemble — proven); reproducible `package_content_hash` over trusted references; exact source refs + hashes where available; DESCRIPTOR-of-references only (no rendered document / fake export files); manifest-backed artifacts only after a validated handoff; KMZ included only when `kmz_export` EXPORTABLE; pixel/PDF-only OMITS KMZ with a warning + never blocks the rest; reviewed_bore_log included only when engine-eligible; closeout controls readiness/finality (no package-approve); billing included by snapshot/reference, never recomputed; hard blockers prevent READY/FINAL; warnings distinct; regeneration appends/supersedes on trusted-ref change; `assemble` sets the job `export_package` slot as a reference only (after the record; no lifecycle transition / no closeout-or-billing override).

**PERMANENT SPINE now CONTRACT-COMPLETE end-to-end:** `upload_pipeline → reviewed_bore_log → manifest_handoff → kmz_export safety → closeout_review → billing_summary → export_package`. **All 3 job output slots are now produced:** `redline_manifest` + `artifact_bundle` (by `manifest_handoff`), `export_package` (by this lane). Pipeline state: artifact serving DONE (served:true, continued-42) · upload/job `2193a0e` · reviewed bore-log gate `6048ef1` · manifest handoff `2013879` · KMZ geometry-safety `2f70e22` · closeout one-status `43a8501` · billing server-computed `42c6bab` · export package `c5185d5`; NO backend/web/UI/AI-OCR-provider/engine execution wired yet.

**Next recommended lane (separately authorized; NOT started; PLAN-FIRST):** `TRUELINE_PRODUCT_BACKEND_API_WIRING_PLAN` — plan generic, `customer_project`-scoped, external-auth-gated routes that DRIVE the now-complete contract spine (upload → project/job → review gate → manifest handoff → proof artifacts → kmz safety → closeout status → billing → export package descriptor); routes read/drive contracts, never recompute truth; cross-project = 403; no engine/render change; no deploy. Detail: [[current-sprint]] / [[log]] continued 48.

### Prior — continued 47 (2026-06-21): billing_summary server-computed model landed (one durable server-authoritative billing status per job; FINAL only when closeout APPROVED); NO render-truth change; frontier 50/58
**continued 47 — the SIXTH permanent product-pipeline slice (server-computed billing); NO engine/renderer/fixture/anchor/corpus/census/parent-model/placement/flag change; frontier UNCHANGED 50/58; render commit stays `c19b565`; engine/contract HEAD `42c6bab`; v2 suite 1593 passed / 4 skipped.**
- **Billing summary server-computed LANDED (lane `TRUELINE_PRODUCT_BILLING_SUMMARY_SERVER_COMPUTED`, HEAD/origin `42c6bab`).** Commit `42c6bab` `contracts: add billing summary server-computed model` (3 files / +1022; staged explicitly, NO `git add -A`; pushed, HEAD = origin = `42c6bab`, tree clean). One GENERIC, contract-only, pure-stdlib module + its test + the +1-line guard extension. Fixes v1's defect (billing computed in the client as React props; `billing.py` a placeholder — salvage audit 5a) with an authoritative server record.
  - `truelinev2/contracts/billing_summary.py` — ONE durable `_billing_summary.json` per job (singleton). Server-authoritative status `DRAFT · COMPUTED · BLOCKED · FINAL`. Totals computed SERVER-side from TRUSTED state + INJECTED VERSIONED cost rules (each amount = quantity × the rule's unit_cost; the client never supplies a dollar amount), money as `Decimal` serialized to canonical strings, `ROUND_HALF_UP` to the rule set's minor units (deterministic + reproducible). effective_footage = Σ `closure.drawn_ft` over DRAWN logs of the VALIDATED, sha256-verified durable manifest. Auditable calculation REVISIONS: regeneration appends a new revision (prior superseded, retained) when (inputs_hash, cost_rule_version) changes; identical recompute revalidates idempotently. No billing-approve action — billability follows closeout.
  - **Decisions baked in (A–E approved):** footage from validated-manifest `drawn_ft`; Decimal/HALF_UP money; `FINAL` derived purely from closeout (no billing-approve); closeout-not-approved = finalization blocker (allows COMPUTED); one commit. **Clarification:** `FINAL` requires closeout `APPROVED` AND zero billing hard blockers — closeout `BLOCKED` (open engine blockers) ⇒ billing stays `COMPUTED`; `OPEN_ENGINE_BLOCKERS` surfaces in billing as a WARNING only (closeout owns it as the hard blocker), so billing can NEVER downgrade it to reach FINAL.
  - HARD blockers (→ BLOCKED): JOB_NOT_IN_BILLING_RANGE / MISSING_MANIFEST / MISSING_ARTIFACT_BUNDLE / UNTRUSTED_OUTPUT / MANIFEST_NOT_RESOLVABLE / REVIEWED_BORE_LOG_NOT_READY. FINALIZATION blockers (allow COMPUTED, prevent FINAL): CLOSEOUT_NOT_APPROVED / CLOSEOUT_REVIEW_MISSING. WARNINGS: OPEN_ENGINE_BLOCKERS / ZERO_BILLABLE_TOTAL / EXCEPTION_WITHOUT_NOTE / MANIFEST_FOOTAGE_INCOMPLETE / REVIEWED_BORE_LOG_NOT_RESOLVABLE. RAISES (caller/config): InvalidCostRuleSetError, UntrustedItemizationError (raw/unreviewed itemization rejected — raw uploads can't become billing truth).
  - Tests: `test_billing_summary_server_computed_contract.py` (23); the generic no-specific-names guard extended to all 9 pipeline modules (+1 line). Verify: targeted 23 ✓; sibling product-contract group 114 in one run; naming guard with operator `NAME_TOKENS` (real customer/location tokens) ✓ zero leakage; **full v2 suite 1593 passed / 4 skipped** (delta exactly +23 vs the committed 1570/4 baseline). NO artifacts committed (tmp_path; `data/`+`outputs/` gitignored); NO generated `.kmz`/`.kml`/`.png`/`.json`; NO deploy; NO mobile; NO engine/render/fixture/coordinate change; reads `closeout_review` but never mutates it; never drives the job lifecycle; `origin/main` `068a279` untouched.

**BILLING INVARIANTS:** ONE durable server-authoritative `billing_summary` per job (no client flag / UI boolean overrides status or totals — proven: a tampered on-disk FINAL is re-derived away on recompute); totals deterministic from versioned INJECTED cost rules (no baked rates); `Decimal` money as canonical strings; auditable/reproducible revisions (append/supersede on input or rule change); raw uploads can't become billing truth unless trust-gated; manifest/bundle contributes only after a validated handoff; closeout controls billability and the UI cannot approve billing; `FINAL` only when closeout `APPROVED` + zero hard blockers; hard blockers prevent FINAL; warnings distinct from hard blockers; this lane reads but never mutates `closeout_review`, and touches no `export_package`/backend/web/UI/engine.

**PERMANENT SPINE now includes billing:** `upload_pipeline → reviewed_bore_log → manifest_handoff → kmz_export safety → closeout_review → billing_summary`. Pipeline state: artifact serving DONE (served:true, continued-42) · upload/job foundation `2193a0e` · reviewed bore-log gate `6048ef1` · manifest handoff `2013879` · KMZ geometry-safety `2f70e22` · closeout one-status `43a8501` · billing server-computed `42c6bab`; NO backend/web/UI/AI-OCR-provider/engine execution wired yet.

**Next recommended lane (separately authorized; NOT started):** `export_package` versioned/checksummed/reproducible closeout packet (§8; bundles only trusted outputs — job summary, reviewed-bore-log gate, validated manifest handoff, durable bundle refs, kmz_export safety, closeout status, billing snapshot — never inventing truth; gated on closeout readiness). Lane `TRUELINE_PRODUCT_EXPORT_PACKAGE_VERSIONED_CLOSEOUT`. Detail: [[current-sprint]] / [[log]] continued 47.

### Prior — continued 46 (2026-06-21): closeout_review ONE-status model landed (one durable server-authoritative status per job); NO render-truth change; frontier 50/58
**continued 46 — the FIFTH permanent product-pipeline slice (the ONE authoritative closeout status); NO engine/renderer/fixture/anchor/corpus/census/parent-model/placement/flag change; frontier UNCHANGED 50/58; render commit stays `c19b565`; engine/contract HEAD `43a8501`; v2 suite 1570 passed / 4 skipped.**
- **Closeout review one-status LANDED (lane `TRUELINE_PRODUCT_CLOSEOUT_REVIEW_ONE_STATUS`, HEAD/origin `43a8501`).** Commit `43a8501` `contracts: add closeout review one-status model` (3 files / +906; staged explicitly, NO `git add -A`; pushed, HEAD = origin = `43a8501`, tree clean). One GENERIC, contract-only, pure-stdlib module + its test + the +1-line guard extension. Fixes v1's core defect (a client `billingApprovalStatus` React state never persisted + a readiness checklist computed in 3 disagreeing places + an in-memory lock the lock endpoint never validated — salvage audit 4c/4d/5a).
  - `truelinev2/contracts/closeout_review.py` — ONE durable `_closeout_review.json` per job (singleton → structurally one status). Status set `OPEN · BLOCKED · READY_FOR_APPROVAL · LOCKED · APPROVED · REJECTED · CLOSED` (LOCKED kept DISTINCT, owner decision A). The gate is SERVER-evaluated from TRUSTED CONTRACTS ONLY (job lifecycle+slots, the sha256-verified durable manifest, the reviewed_bore_log resolved via the manifest-handoff chain for §6 freshness, read-only `kmz_export`) and STORED (a snapshot), not recomputed per render. `approve`/`lock` RE-EVALUATE the gate and REFUSE (leaving BLOCKED) if any hard blocker; LOCKED/APPROVED auto-invalidate → BLOCKED when trusted inputs break (§6). Privileged transitions (lock/unlock/approve/reopen/close) are permission-gated (`actor_role ∈ authorized_roles`, both injected — no baked roles); every transition audited; never touches `export_package`.
  - **Owner decisions baked in:** A = `LOCKED` is a distinct status. B(corrected) = `OPEN_ENGINE_BLOCKERS` is a HARD blocker (a closeout must never read "approved" while known engine/redline blockers remain open; approve-with-exceptions is a future SEPARATE audited waiver lane, deliberately NOT sneaked in here), while `KMZ_EXPORT_BLOCKED` from the current pixel-only manifests is a WARNING (PDF-first closeout does not require geospatial export). Other hard blockers: JOB_NOT_IN_CLOSEOUT_RANGE / MISSING_MANIFEST / MISSING_ARTIFACT_BUNDLE / UNTRUSTED_OUTPUT / MANIFEST_NOT_RESOLVABLE / REVIEWED_BORE_LOG_NOT_READY / UNRESOLVED_REVIEW_ITEMS / SOURCE_CONFLICT.
  - Tests: `test_closeout_review_one_status_contract.py` (20); the generic no-specific-names guard extended to all 8 pipeline modules (+1 line). Verify: targeted 20 ✓; sibling pipeline + naming guard 91 in one run; naming guard with operator `NAME_TOKENS` (real customer/location tokens) ✓ zero leakage; **full v2 suite 1570 passed / 4 skipped** (delta exactly +20 vs the committed 1550/4 baseline). NO artifacts committed (tmp_path; `data/`+`outputs/` gitignored); NO generated `.kmz`/`.kml`/`.png`/`.json`; NO deploy; NO mobile; NO engine/render/fixture/coordinate change; `origin/main` `068a279` untouched.

**CLOSEOUT INVARIANTS:** ONE durable server-authoritative status per job (no client flag / UI boolean may override it); the gate is server-evaluated from trusted contracts + STORED (not recomputed per render); OPEN_ENGINE_BLOCKERS / missing-or-untrusted-or-unresolvable outputs / reviewed_bore_log-not-ready / source-conflict are HARD blockers; KMZ pixel-only export-blocked is a WARNING; approve/lock re-evaluate + refuse on any hard blocker; approved/locked auto-invalidate to BLOCKED if trusted inputs break; LOCKED is a distinct durable state; transitions are permission-gated + audited; this lane does NOT touch `export_package`/billing/backend/web/UI/engine; this slice does NOT drive the job's `CLOSEOUT_REVIEW→CLOSED` transition (future integration).

**PERMANENT SPINE now includes closeout:** `upload_pipeline → reviewed_bore_log → manifest_handoff → kmz_export safety → closeout_review (one status)`. Pipeline state: artifact serving DONE (served:true, continued-42) · upload/job foundation `2193a0e` · reviewed bore-log gate `6048ef1` · manifest handoff `2013879` · KMZ geometry-safety `2f70e22` · closeout one-status `43a8501`; NO backend/web/UI/AI-OCR-provider/engine execution wired yet.

**Next recommended lane (separately authorized; NOT started):** `billing_summary` server-computed model (§6 — server-computed/persisted totals, client renders never overrides, PART OF the closeout status model, input change after approval invalidates approval). Alternate: the `export_package` stored/versioned/checksummed/reproducible packet (§8). Detail: [[current-sprint]] / [[log]] continued 46.

### Prior — continued 45 (2026-06-21): kmz_export geometry-safety contract landed (pixel-only manifests BLOCKED, never fake coords); NO render-truth change; frontier 50/58
**continued 45 — the FOURTH permanent product-pipeline slice (KMZ/KML geometry safety); NO engine/renderer/fixture/anchor/corpus/census/parent-model/placement/flag change; frontier UNCHANGED 50/58; render commit stays `c19b565`; engine HEAD `2f70e22`; v2 suite 1550 passed / 4 skipped.**
- **KMZ export geometry-safety LANDED (lane `TRUELINE_PRODUCT_KMZ_EXPORT_GEOMETRY_SAFETY`, HEAD/origin `2f70e22`).** Commit `2f70e22` `contracts: add kmz export geometry-safety contract` (3 files / +493; staged explicitly, NO `git add -A`; pushed, HEAD = origin = `2f70e22`, tree clean). One GENERIC, contract-only, pure-stdlib module + its test + the +1-line guard extension. **Decisive finding:** the v2 redline manifest has NO geospatial basis (only `source_sheets`/`span` stations/`closure` footage/PNG `artifacts`; schema `additionalProperties:false`), so every REAL manifest classifies `UNSUPPORTED_PIXEL_ONLY` and the export is BLOCKED — the system abstains rather than fake coordinates.
  - `truelinev2/contracts/kmz_export.py` — geometry-basis classifier (`GEOSPATIAL_COORDINATES`/`GEOREFERENCED_SHEET`/`UNSUPPORTED_PIXEL_ONLY`; NEVER treats stations/sheets/pixels as geospatial), a named blocker taxonomy (10 codes incl. `MISSING_MANIFEST_SLOT`/`UNTRUSTED_HANDOFF`/`MANIFEST_NOT_RESOLVABLE`/`UNSUPPORTED_PIXEL_ONLY`/`AMBIGUOUS_CRS`/`COORDINATE_UNCERTAINTY`/`SOURCE_CONFLICT`), a deterministic red-styled KML builder (text + traceability ExtendedData; KMZ = interface descriptor only, no binary), and `evaluate_export` — READ-ONLY over the job's trusted `redline_manifest` + `artifact_bundle` slots (resolves + sha256-verifies the durably-stored manifest), returns an export record (EXPORTABLE/BLOCKED). Persists nothing; never touches `export_package`; never reads `job["uploads"]`.
  - Tests: `test_kmz_export_geometry_safety_contract.py` (10); the generic no-specific-names guard extended to all 7 pipeline modules (+1 line). Verify: targeted 10 ✓; prior pipeline + published-bundle contracts + repo-wide guards ✓ (96 in one run); **full v2 suite 1550 passed / 4 skipped** (delta exactly +10 vs the committed 1540/4 baseline). NO artifacts committed (tmp_path; `data/`+`outputs/` gitignored); NO generated `.kmz`/`.kml`; NO deploy; NO mobile; NO engine/render/fixture/coordinate change; `origin/main` `068a279` untouched.

**KMZ SAFETY INVARIANTS:** current real v2 manifests have NO geospatial basis → classify `UNSUPPORTED_PIXEL_ONLY` → export BLOCKED with named reasons (never fake coords); uploaded GIS_ROUTE/KMZ/KML is NEVER treated as approved output (and `evaluate_export` never reads uploads); NO route-snap / nearest-line / station-guess / PDF-bounds / KMZ-bounds as truth; `evaluate_export` is read-only over the two trusted slots; `export_package` untouched; synthetic geospatial geometry exists ONLY in tests/future-path fixtures (proves the path for when the engine emits verified coords — the manifest schema is NOT modified here).

**PERMANENT SPINE now includes export safety:** `upload_pipeline → reviewed_bore_log → manifest_handoff → kmz_export safety evaluation`. Pipeline state: artifact serving DONE (served:true, continued-42) · upload/job foundation `2193a0e` · reviewed bore-log gate `6048ef1` · manifest handoff `2013879` · KMZ geometry-safety `2f70e22`; NO backend/web/UI/AI-OCR-provider/engine execution wired yet. RULE (memory `generic-naming-reusable-code`): reusable identifiers stay generic; real names only runtime data / historical audit docs.

**Next recommended lane (separately authorized; NOT started):** `closeout_review` ONE-status model (§5 — a single persisted `closeout_review.status` per job, server-authoritative + audited; all readiness UI renders it; no client flag overrides; a server-side gate checklist evaluated once + stored; LOCKED/APPROVED permission-gated; durable lock). Alternates: `billing_summary` server-computed (§6) or the `export_package` stored/versioned/reproducible packet (§8). Detail: [[current-sprint]] / [[log]] continued 45.

### Prior — continued 44 (2026-06-21): manifest_handoff engine-output attachment landed (validate→durably store→attach to job slots); NO render-truth change; frontier 50/58
**continued 44 — the THIRD permanent product-pipeline slice (engine-output handoff); NO engine/renderer/fixture/anchor/corpus/census/parent-model/placement/flag change; frontier UNCHANGED 50/58; render commit stays `c19b565`; engine HEAD `2013879`; v2 suite 1540 passed / 4 skipped.**
- **Manifest handoff LANDED (lane `TRUELINE_PRODUCT_MANIFEST_HANDOFF`, HEAD/origin `2013879`).** Commit `2013879` `contracts: add manifest handoff engine-output attachment` (3 files / +467; staged explicitly, NO `git add -A`; pushed, HEAD = origin = `2013879`, tree clean). One GENERIC, contract-only, pure-stdlib module + its test + the +1-line guard extension. NOT engine execution / render / export — the engine output bundle is a GIVEN input; ALL validation + durable storage REUSES existing machinery (no new validator):
  - `truelinev2/contracts/manifest_handoff.py` — records an engine-output handoff and, gated, validates + durably stores the engine output bundle and attaches content-addressed refs to the job's output slots. State machine `ATTEMPTED → SUCCEEDED | FAILED | REJECTED` (all terminal; re-finalize raises `HandoffStateError`). `record_handoff_attempt` (verifies job + reviewed_bore_log exist/in-scope; opaque `engine_run_status`; rejects duplicate `engine_run_id`). `finalize_handoff` = gate (`reviewed_bore_log.is_engine_ready`) → REJECTED on fail; else `store_bundle` into the job-scoped `bundle_store/` (validates via `admission_errors` + content-keys + copies) → FAILED on `BundleRejectedError`; else build `redline_manifest` + `artifact_bundle` attachments (bundle_id / manifest_sha256 / counts / `validation_status:"VALIDATED"`) + `set_output_slot` (reused as-is). Stored at `…/processing_jobs/<job>/handoffs/<engine_run_id>/_manifest_handoff.json`; durable bundle at `…/bundle_store/bundles/<bundle_id>/`.
  - Tests: `test_manifest_handoff_contract.py` (12); the generic no-specific-names guard extended to all 6 pipeline modules (+1 line). Verify: targeted 12 ✓; Slice 1 + reviewed-bore-log gate + published-bundle contracts + repo-wide guards ✓ (84 in one run); **full v2 suite 1540 passed / 4 skipped** (delta exactly +12 vs the committed 1528/4 baseline). NO artifacts committed (tmp_path; `data/`+`outputs/` gitignored); NO deploy; NO mobile; NO engine/render/fixture/coordinate change; `origin/main` `068a279` untouched.

**HANDOFF INVARIANTS:** reviewed_bore_log must be engine-ready before finalize; the engine output bundle must validate through the existing published-bundle machinery; the validated bundle is durably stored via `store_bundle`; output slots are attached ONLY after validation/storage succeeds; `REJECTED`/`FAILED` leave slots UNTOUCHED; success attaches ONLY `redline_manifest` + `artifact_bundle` (`export_package` untouched); terminal handoffs are immutable; the term "engine" = the TrueLine placement engine only (never AI/OCR/extraction). No engine execution / render / KMZ / backend / UI in this lane.

**PERMANENT CORE SPINE now exists:** `upload_pipeline → reviewed_bore_log → manifest_handoff → redline_manifest/artifact_bundle slots`. Pipeline state: artifact serving DONE (served:true, continued-42) · upload/job foundation `2193a0e` · reviewed bore-log gate `6048ef1` · manifest handoff `2013879`; NO backend/web/UI/AI-OCR-provider/engine execution wired yet. RULE (memory `generic-naming-reusable-code`): reusable identifiers stay generic; real names only runtime data / historical audit docs.

**Next recommended lane (separately authorized; NOT started):** `kmz_export` geometry-safety contract (approved-manifest geometry only, CRS header `EPSG:4326`/WGS84, per-coordinate `source`+`confidence`, abstain-not-fake, export is a checksummed `artifact_bundle` member; contract §7). Alternates: `closeout_review` ONE-status model (§5) or the `export_package` stored/versioned/reproducible packet (§8). Detail: [[current-sprint]] / [[log]] continued 44.

### Prior — continued 43 (2026-06-21): reviewed_bore_log engine-eligibility GATE landed (extracted_row + reviewed_bore_log); NO render-truth change; frontier 50/58
**continued 43 — the SECOND permanent product-pipeline slice (the EXTRACTING→PLACING review gate); NO engine/renderer/fixture/anchor/corpus/census/parent-model/placement/flag change; frontier UNCHANGED 50/58; render commit stays `c19b565`; engine HEAD `6048ef1`; v2 suite 1528 passed / 4 skipped.**
- **Reviewed bore-log gate LANDED (lane `TRUELINE_PRODUCT_REVIEWED_BORE_LOG_GATE`, HEAD/origin `6048ef1`).** Commit `6048ef1` `contracts: add reviewed bore-log engine-eligibility gate` (5 files / +783; staged explicitly, NO `git add -A`; pushed, HEAD = origin = `6048ef1`, tree clean). Two GENERIC, contract-only, pure-stdlib modules + the mandatory EXTRACTING→PLACING gate (contract §3):
  - `truelinev2/contracts/extracted_row.py` — the UNTRUSTED `extracted_row` + per-row review state (UNREVIEWED/CONFIRMED/CORRECTED/REJECTED/NEEDS_CLARIFICATION; CORRECTED needs corrected_values; REJECTED/NEEDS_CLARIFICATION need a reason; re-review allowed + audited). Row-level helpers are REVIEW-only (`row_review_passes`/`row_review_blocks_engine`) — a row ALONE is never engine-eligible. Generic extraction metadata: `extraction_method` ∈ {OCR, TEXT_PARSE, TABLE_IMPORT, MANUAL_ENTRY} + opaque `extractor_name` (no `engine` field; "engine" = the placement engine).
  - `truelinev2/contracts/reviewed_bore_log.py` — the aggregate gate attached to a `processing_job` + a BORE_LOG upload: extracted_rows + `segment_group`s (relation SEPARATE_BORE/SAME_RUN_SEGMENTS/AMBIGUOUS; grouping_status PENDING/CONFIRMED/SOURCE_CONFLICT), durable JSON under the job scope, aggregate + per-row + per-group audit, a pure `review_queue` view, and the DERIVED eligibility gate. `row_engine_eligible(rbl, row_id)` = review passed AND the row resolves into EXACTLY ONE confirmed, non-ambiguous, non-conflicting group; `is_engine_ready(rbl)` = ≥1 row AND every non-rejected row eligible (empty → never ready).
  - Tests: `test_extracted_row_review_contract.py` (10) + `test_reviewed_bore_log_gate_contract.py` (13); the generic no-specific-names guard extended to all 5 pipeline modules (+2 lines). Verify: targeted 23 ✓; Slice 1 + repo-wide guards ✓; **full v2 suite 1528 passed / 4 skipped** (delta exactly +23 vs the committed 1505/4 baseline). NO artifacts committed (tmp_path; `data/`+`outputs/` gitignored); NO deploy; NO mobile; NO engine/render/fixture/coordinate change; `origin/main` `068a279` untouched.

**SAFETY INVARIANTS (the gate's contract):** raw AI/OCR/table/manual rows are UNTRUSTED; no raw row is engine-eligible by default; row review alone is NOT enough (grouping required); grouping must be RESOLVED before eligibility; eligibility is DERIVED ONLY (never stored as mutable truth — no `engine_eligible`/`engine_ready` in persisted JSON); rejected / ambiguous / source-conflict / unreviewed / ungrouped / multi-group-drift rows all block placement; extraction metadata uses generic extractor fields (never placement-engine terminology).

**Pipeline state:** artifact serving DONE (served:true, continued-42) · upload/job foundation `2193a0e` · reviewed bore-log gate `6048ef1`; NO backend/web/UI/AI-OCR-provider/engine execution wired yet. RULE (memory `generic-naming-reusable-code`): reusable identifiers stay generic; real names only runtime data / historical audit docs.

**Next recommended lane (separately authorized; NOT started):** plan the next permanent product slice — candidate `manifest_handoff` (the v2 engine `redline_manifest` → durable, checksummed `artifact_bundle` handoff producer; downstream read-only, resolve by path+sha256, never infer status from filenames; contract §4), or alternate `kmz_export` geometry-safety contract (approved-manifest geometry only, CRS header, per-coordinate source+confidence, abstain-not-fake; contract §7). Detail: [[current-sprint]] / [[log]] continued 43.

### Prior — continued 42 (2026-06-20): Fable artifact serving ACTIVATED (served:true) + Product pipeline Slice 1 LANDED (customer_project + processing_job + upload_pipeline contract); NO render-truth change; frontier 50/58
**continued 42 — owner ACTIVATED Fable artifact serving (served:true) + the FIRST permanent product-pipeline implementation slice; NO engine/renderer/fixture/anchor/corpus/census/parent-model/placement/flag change; frontier UNCHANGED 50/58; render commit stays `c19b565`; engine HEAD `2193a0e`; v2 suite 1505 passed / 4 skipped.** Two threads:
- **Artifact serving ACTIVATED (Fable web `main` `16c7095`).** The continued-41 artifact-hosting CODE is now LIVE-ACTIVE: owner uploaded the 48 MB GitHub Release asset + set Vercel `TL2_REDLINE_BUNDLE_URL` (present, len=169) + `NEXT_PUBLIC_TL2_REDLINE_MANIFEST_SERVED=1` + redeployed WITHOUT build cache. Verified build log: `served=true`; `archive 50268315 bytes; validating entries`; `OK: 83/83 FINAL_REDLINE_PNG verified -> public/redline-bundle/.../artifacts/...`. Verified live `/redlines`: `Served · lazy`, `83 FINAL_REDLINE_PNG · served: true`, bundle served from the durable artifact path. **The `…_ARTIFACT_HOSTING_VERIFY` gate is CLOSED (served:true).** (The continued-40/41 START_HERE/hot `served:false` was the pre-activation snapshot — now superseded.)
- **Product pipeline Slice 1 LANDED (engine repo `feat/truelinev2`, HEAD/origin synced `2193a0e`).** Lane `TRUELINE_PRODUCT_UPLOAD_PIPELINE_PROCESSING_JOB` — the FIRST permanent v2 product-foundation implementation. Commit `2193a0e` `contracts: add product upload pipeline job foundation` (7 files / +787; staged explicitly, NO `git add -A`; pushed, HEAD = origin = `2193a0e`, tree clean). Three GENERIC, contract-only, filesystem-shaped + adapter-neutral modules (pure stdlib; no engine/render/web/backend/AI-OCR/KMZ):
  - `truelinev2/contracts/customer_project.py` — the ISOLATION ROOT: safe-id validation, `customer_projects/<id>/` path-scoping + realpath containment guard, `assert_same_project`/`CrossProjectAccessError` (in-process 403 analog); `display_name` is opaque runtime data.
  - `truelinev2/contracts/processing_job.py` — durable JSON job record + the contract state machine (`CREATED→UPLOADING→EXTRACTING→AWAITING_REVIEW→PLACING→PLACED→CLOSEOUT_REVIEW→CLOSED`; any non-terminal→`FAILED`, reason-required); server-authoritative AUDITED transitions (from/to/at/by/reason); ONE authoritative status; durable across reload; 3 typed OUTPUT SLOTS (redline_manifest/artifact_bundle/export_package) — interface only, producers are later lanes.
  - `truelinev2/contracts/upload_pipeline.py` — accepted-kind inventory (PLAN_PDF/BORE_LOG/GIS_ROUTE + ext allowlist), `accept_upload` (kind/size validation, scoped storage, sha256, content-idempotent, intake closed after EXTRACTING). **NO extraction runs — every upload is `extraction_status:"queued"`** (AI/OCR untrusted, gated to a later lane).
  - Four targeted tests (`truelinev2/tests/`): `test_customer_project_isolation_contract.py`, `test_processing_job_lifecycle_contract.py`, `test_upload_pipeline_contract.py`, `test_no_specific_names_in_pipeline_contract.py` (NAME-FREE naming guard = operator `NAME_TOKENS` env denylist + AST identity-no-default structural invariant; proven via a runtime real-token run + a `queued` negative control + whole-word grep).
  - Verification: targeted 26 passed; repo-wide guards + bundle contracts 35 passed; **full v2 suite 1505 passed / 4 skipped** (proven delta = exactly +26 vs 1479/4 without the new files; the long-carried "1392/2-skip" was stale doc drift). NO artifacts committed (tmp_path; `data/`+`outputs/` gitignored); NO deploy; NO mobile; NO engine/render/fixture/coordinate change; `origin/main` `068a279` untouched.

**NAMING/PRODUCT RULE (hard, now memory-pinned `generic-naming-reusable-code`):** reusable code/routes/env/schema/test/component/docs-heading names must stay GENERIC — no customer/person/project/location/demo names in reusable infrastructure; real names live ONLY as runtime data or historical audit docs. Name-free naming guards (`NAME_TOKENS` env + AST identity-no-default).

**Next recommended lane (separately authorized; NOT started): `reviewed_bore_log` / extraction-review gate** — uploaded or AI/OCR-extracted bore-log rows stay UNTRUSTED until reviewed/confirmed; support multi-segment + unrelated-bore grouping BEFORE engine placement (contract §3, the mandatory `EXTRACTING → PLACING` gate that consumes only `review.status == approved` segments). Detail: [[current-sprint]] / [[log]] continued 42.

### Prior — continued 41 (2026-06-20): Fable staging LIVE as a read-only v2 proof-viewer; artifact-hosting built (owner activation pending); v1 salvage audit + v2 pipeline contract; NO render-truth change; frontier 50/58
**continued 41 — docs/audit (engine repo) + Fable-web product work (SEPARATE repo); NO engine/renderer/fixture/anchor/corpus/census/parent-model/placement/flag change; frontier UNCHANGED 50/58; render commit stays `c19b565`; engine HEAD `a4bf2a5`; v2 suite 1392/2-skip.** Three threads:
- **Artifact hosting (Fable `main` 51dcbf7→`16c7095`; engine docs `09fc469`/`e886de2`/`2a559bd`).** Built `scripts/fetch-redline-bundle.mjs` (Fable `3ab0c80`): downloads the 48 MB bundle from `TL2_REDLINE_BUNDLE_URL`, extracts to gitignored `public/redline-bundle/<id>/`, **sha256-verifies all 83 PNGs** vs the committed manifest; NO PNGs in git; archive sha256 `864c657c…`. Env-debug (`16c7095`): build = **`node scripts/fetch-redline-bundle.mjs && next build`** (prebuild lifecycle removed — wasn't firing on Vercel) + an always-on no-secret `env:` diagnostic. Locally proven (tsc/contracts/build PASS, served build 83/83, tamper fails). **No env-var mismatch** (fetch + UI both `NEXT_PUBLIC_TL2_REDLINE_MANIFEST_SERVED==='1'`).
- **Staging honesty (Fable `85682bb`→`f753b1a`; engine docs `b9fd9cf`/`377536d`/`5211102`).** URL recorded (`https://trueline-web-experience.vercel.app/`, project `trueline-web-experience`). Cedar Ridge→**`Brenham PH5 — v2 staging`**; then **mock-portfolio shell → real read-only v2 proof-viewer**: dashboard = single Brenham summary from the REAL manifest (58/50/1/7, 83 artifacts, bundle `brenham-c19b565-ddfffff7cbe7`, render `c19b565`); global banner `Read-only v2 staging · no upload/live render yet`; `/redlines` leads with the real `RedlineManifestPanel`, mock review queue collapsed+labeled `Mock UI demo queue — not engine data`.
- **v1 salvage audit (engine `a4bf2a5`, `docs/`).** `product_v1_workflow_salvage_audit.md` (7-capability read-only audit + salvage-class table + DO-NOT-COPY list) + `product_v2_permanent_pipeline_contract.md` (generic pipeline: upload → reviewed bore-log → engine placement → redline_manifest/artifact_bundle → closeout_review → billing_summary → kmz/kml export → export_package) + `probe_v1_inventory.sh` (read-only). Doctrine: engine owns placement truth; geometry from a reviewed manifest/approved override only; NO fake KMZ when coords unavailable; OCR untrusted until reviewed; multi-bore grouping before placement; reproducible exports; ONE status model; `customer_project` isolation. Generic names only (contract+probe = 0 customer tokens). Audited read-only via 3 parallel Explore agents — NO v1 behavior change.

**VERIFY status:** staging is LIVE + honest (Brenham labels, 50/58 panel correct) but **`served:false`** — artifact activation is pending the OWNER step (upload the Release asset → set Vercel `TL2_REDLINE_BUNDLE_URL` + `NEXT_PUBLIC_TL2_REDLINE_MANIFEST_SERVED=1` → **redeploy WITHOUT build cache**). `origin/main` `068a279` + `osp-redlining` + mobile (`c61b2c3`) untouched; agent ran no `vercel`/deploy. **Next: owner activates → re-run `…_ARTIFACT_HOSTING_VERIFY` for `served:true`; then (separately authorized) a v2-pipeline implementation lane (strongest first = `upload_pipeline` + `processing_job` isolation, or the `kmz_export` geometry-safety contract).** Detail: [[current-sprint]] / [[log]] continued 41.

### Prior — continued 40 (2026-06-19): Fable Vercel staging STANDUP succeeded (manifest/status-only on `main @ 51dcbf7`); NO render-truth change; frontier 50/58
**continued 40 — docs only (records a successful owner-run Vercel standup); NO engine/renderer/fixture/anchor/corpus/census/parent-model/placement/flag change; frontier UNCHANGED 50/58; render commit stays `c19b565`; engine work HEAD `a4b8590` (this docs save sits one commit above it).**
- **Fable Vercel staging STANDUP SUCCEEDED (owner-run; agent ran no `vercel`/deploy).** Fable web loads on Vercel from `patrickverardi45/trueline-web-experience`, branch **`main`**, commit **`51dcbf7`**; `/redlines` renders with the Fable UI intact + the **v2 redline manifest (durable bundle)** panel: **50/58**, bundle **`brenham-c19b565-ddfffff7cbe7`**, render/source **`c19b565`**, **83 FINAL_REDLINE_PNG**, **`served: false`** (manifest/status-only — PNG serving off + PNGs gitignored/local-only). NO live render, NO upload flow, NO client data, NO production swap; `osp-redlining` project/domain + Render/domain/env untouched.
- **Result recorded (`a4b8590`, docs-only path-scoped, pushed).** `wiki/trueline_v2_fable_vercel_staging_plan.md` gained a "Standup result — DONE" section (every observed value VERIFIED read-only against the committed fixtures `redline_manifest.v1.json` + `redline_store_index.v1.json` that a `main @ 51dcbf7` build serves) + a "Next unresolved work" list.
- **Staging URL: `https://trueline-web-experience.vercel.app/` · Vercel project: `trueline-web-experience`** (owner-provided 2026-06-19; the actual project reuses the repo name rather than the earlier-recommended slug `trueline-web-staging`).
- **Fable web repo state:** default branch **`main @ 51dcbf7`**; feature branch `feat/2k-static-bundle-adapter @ 51dcbf7` PRESERVED; tag `fable-v2-ui-bones-2026-06-19 → 7e3b392` intact.

Frontier UNCHANGED **50/58**; render commit `c19b565`; v2 suite **1392 passed / 2 skipped**. **Next lane: `TRUELINE_V2_FABLE_VERCEL_STAGING_ARTIFACT_HOSTING` (planning-first)** — host the redline PNGs WITHOUT committing the ~50 MB pile to GitHub (object/static storage, a Vercel prebuild fetch/copy, or backend static serving later), then flip `NEXT_PUBLIC_TL2_REDLINE_MANIFEST_SERVED=1` so strokes render. No production/domain change. Detail: [[current-sprint]] / [[log]] continued 40.

### Prior — continued 39 (2026-06-19): P4 staging-plan persisted + Fable branch hygiene (clean `main` is the GitHub default); NO render-truth change; frontier 50/58
**continued 39 — docs + git-hygiene only; NO engine/renderer/fixture/anchor/corpus/census/parent-model/placement/flag change; frontier UNCHANGED 50/58; render commit stays `c19b565`; engine work HEAD `16295d4` (this docs save sits one commit above it).** Two safe prep lanes after the continued-38 Fable remote-init:
- **P4 staging plan PERSISTED (`16295d4`, path-scoped, pushed → `origin/feat/truelinev2`).** `wiki/trueline_v2_fable_vercel_staging_plan.md`: recommended Vercel slug `trueline-web-staging`, repo `github.com/patrickverardi45/trueline-web-experience`, branch strategy (+ clean `main`), Next.js/npm/`next build` + default Vercel Next output + Node 20.x/22.x, env (`NEXT_PUBLIC_TL2_REDLINE_MANIFEST=1`, `…_SERVED` OFF), manifest/status-only artifact plan (no 50 MB PNGs; object-store + prebuild later), first staging objective (read-only shell, `/redlines` 50/58 manifest, no render/upload/data/domain-swap), risks, and the future execution checklist. **PLANNING ONLY — nothing on Vercel.**
- **Fable web branch hygiene DONE (separate `trueline-web-experience` repo).** Clean **`main`** created from `51dcbf7` and pushed (`* [new branch] main -> main`, upstream tracking); the **GitHub default branch is now `main`** (owner flipped it in the UI; verified live `ls-remote --symref` → `refs/heads/main`). `feat/2k-static-bundle-adapter` PRESERVED @ `51dcbf7` (not deleted / not force-pushed); tag `fable-v2-ui-bones-2026-06-19 → 7e3b392` intact; no old branches (`master`, `codex/*`) pushed; no code change. **`.vercel/` absent — not linked; no deploy/Vercel/domain/env change; `osp-redlining` / Render / `origin/main` `068a279` untouched.**

Frontier UNCHANGED **50/58**; render commit `c19b565`; v2 suite **1392 passed / 2 skipped**. **Next lane: `TRUELINE_V2_FABLE_VERCEL_STAGING_STANDUP`** — execute the staging-plan checklist (Vercel project creation is an owner dashboard action; the agent does not run `vercel`); point it at `main`. Detail: [[current-sprint]] / [[log]] continued 39.

### Prior — continued 38 (2026-06-19): bootstrap drift fixed → HEAD `e35cd26`; Phase 2J static bundle consumer + Fable UI preserve/retire + repo-architecture plan + Fable remote-init P1 DONE; NO render-truth change; frontier 50/58
**continued 38 — bootstrap re-canonicalized + website-READ-side / UI-base / architecture arc; NO engine/renderer/fixture/anchor/corpus/census/parent-model/placement/flag change; frontier UNCHANGED 50/58; render commit stays `c19b565`; HEAD `e35cd26`.** The continued-37 save left START_HERE + `hot.md` pinned at `81f3cd3` while four real commits landed after it; this save bumps the snapshot to git truth (`feat/truelinev2 @ e35cd26`, pushed; `origin/main` `068a279` untouched; clean tree) and records the arc. Lineage on the continued-37 save `0c0ff20`:
- **`841960f` — Phase 2J: read-only static bundle CONSUMER (website READ side).** `truelinev2/contracts/published_bundle_consumer.py` + proof `run_redline_manifest_static_consumer_proof.py` + contract test (+620 LOC). Consumes the durable store's `latest_valid` bundle as a pure static read (checksum + in-root path-safety verified); NO live render, NO backend, NO write path.
- **`0fd3228` — Fable UI preserve / contract mock-UI retire.** The Fable v2 repos (`trueline-web-experience` web, `trueline-field-mobile` mobile) are the AUTHORITATIVE v2 UI/design/function base; the temporary `truelinev2/contracts/mock_ui/` is SUPERSEDED → historical contract fixture only (`_DEPRECATED.md`; its fidelity test still guards manifest↔fixture). Future web integration ADAPTS Fable to the durable manifest contract — never rebuilds a new UI. Canonical: `wiki/ui/fable_v2_ui_bones.md`.
- **`eecf2ef` — legacy-extraction & repo-architecture plan (canonical).** `wiki/trueline_v2_legacy_extraction_and_repo_architecture_plan.md`: v1 = legacy prototype / reference spec / algorithm parts-bin (NOT sacred); v2 product = v2 engine (`truelinev2/`) + `redline_manifest` durable-bundle contract + Fable UI; PDF-first before KMZ; v1 auth REFERENCE-ONLY (external provider replaces it — only the tenant-isolation requirement survives); P0–P8 migration phases; the one hard caution = `truelinev2/` lives INSIDE `TrueLine_Beta` (`osp-redlining`), never wipe / `git add -A` until the intentional P7 split.
- **`e35cd26` — Fable remote-init P1 DONE (save).** `TRUELINE_V2_FABLE_REMOTE_INIT` complete: Fable web repo `origin = https://github.com/patrickverardi45/trueline-web-experience`; pushed branch `feat/2k-static-bundle-adapter @ 51dcbf7` (tracking) + tag `fable-v2-ui-bones-2026-06-19 → 7e3b392`; old local branches (`master`, `codex/*`) intentionally NOT pushed. Phase 2K (static-bundle adapter on `/redlines`, default-OFF gate `NEXT_PUBLIC_TL2_REDLINE_MANIFEST`) visually ACCEPTED + git-bundle backed up. **No deploy, no Vercel, no domain change; `osp-redlining` / Render / `origin/main` untouched.**

Frontier UNCHANGED **50/58** (log14 COVERED by log10; 7 owner/source-gated: log5/31/38/43 owner-locked abstain, log15/16 source-gap, log57 `.FS`); render commit `c19b565`; v2 suite **1392 passed / 2 skipped**. **Next lane: `TRUELINE_V2_FABLE_VERCEL_STAGING_PLAN` (P4) — PLANNING ONLY** (a NEW Fable Vercel/staging project on a fresh slug, mock/read-only; NOT a production swap, no domain move). Detail: [[current-sprint]] / [[log]] continued 38.

### Prior — continued 37 (2026-06-19): redline-manifest engine→website CONTRACT pipeline COMPLETE (Phases 2A–2I); NO render-truth change; frontier 50/58
**continued 37 — 12-commit proof/CONTRACT arc; NO engine/renderer/fixture/anchor/corpus/census/parent-model/flag change;
frontier UNCHANGED 50/58; render commit stays `c19b565`; HEAD `81f3cd3`.** Built + proved the entire engine→website
redline-manifest pipeline, all generated artifacts GITIGNORED under `data/outputs/` (NONE committed): schema-pinned
`truelinev2/contracts/redline_manifest.schema.json` + a 50/58 example (2A `a0a490f`) → static manifest-driven mock UI
(`505a9a2`) → artifact **publisher** (real sha256/bytes, `mock_example:false`; `bccfdc8`) → existing-artifact inspection
(2A.5 `4c11722`) → **2B STOP** (`f7a616b`: a unified all-50 render is impossible without a solver change — the callout
sweep hardcodes the ALREADY_DRAWN skip; refused partial-37-as-50) → **2C** canonical render registry re-renders the 13
ALREADY_DRAWN through their existing lanes (`c0498d4`; resolves 2B WITHOUT a solver change; 13/13, log50 incl, log7 PARTIAL)
→ **2D** first REAL all-50 manifest (`06c734a`; 83 artifacts/50.5 MB, 58/50/1/7) → **2E** published-bundle contract
(`f7988ab`; static-serving safe, checksum-verified, bundle index) → **2F** one-command pipeline runner (`cbeb9be`; + fixed
a latent zero-bucket reconciliation false-rejection) → **2G/2H** render benchmarks (`aaa8952`/`9ed6c98`: 13=52.2 s,
37=299.6 s; full refresh ~5.9 min, render-bound) → **2I** adapter-neutral durable bundle store (`81f3cd3`; immutable
content-keyed `bundles/<id>/` + `store_index.json` `latest_valid` + retention + `WEBSITE_READ_CONTRACT`; real bundle stored
`brenham-c19b565-ddfffff7cbe7`, store VALID). ~63 targeted contract tests (61 pass + 2 jsonschema-optional skips).
`B-DATA-LOG48-ADJ-1` unchanged. The full local contract+storage chain is complete + benchmarked; the next step crosses into
website/backend wiring (gated). Detail: [[current-sprint]] / [[log]] continued 37.

### Prior — continued 36 (2026-06-19): repo-hygiene arc COMPLETE (101→0 untracked); NO render; frontier 50/58
**continued 36 — pure repo-hygiene + provenance; NO engine/render/census change; frontier UNCHANGED 50/58; render commit
stays `c19b565`; HEAD `e3df509`.** The continued-35 inventory found 101 untracked files; this arc drove it to **0** without
losing anything. Feat lineage (on the continued-35 save `69dd876`): **`f5dbed1`** committed the evidence trail (7 `gac/*.md`
source-adjudication packets + `run_review_candidate_reasoning_sweep.py`); **`c0e6680`** committed 4 Group-A evidence slices
(KMZ↔PDF georeference ×2, the ambiguity-resolution render primitive, the `gac/log44`-cited owner-source packet); **`e3df509`**
relocated the token-reduction doctrine to `wiki/doctrine/`. A pushed **ARCHIVE** branch+tag `archive-v2-continued-35-superseded-scratch`
= **`d8508b9`** preserves the superseded `backend/tl_core/**` (35) + 14 ambiguous proof slices, which were then removed from the
working tree (step 4C); 26 scratch probes + `probe_err.txt` were deleted (step 5); the old-app `RECOVERED_BASELINE_98d108a.md`
note was deleted (owner decision; recoverable via tag `recovered-pdf-first-overlay-98d108a` + branch `backup-live-lp-chain-6eaade3`).
**`backend/tl_core` is now ARCHIVE-ONLY** (a superseded reuse-by-import wrapper, never imported by v2); the `git add -A` landmine is
DEFUSED; **repo hygiene is COMPLETE (untracked = 0)**. NO code/renderer/fixture/census/flag change. Detail: [[current-sprint]] / [[log]] continued 36.

### Prior — continued 35 (2026-06-19): accountability ledger + website-readiness audit + repo-hygiene (NO render; frontier 50/58)
**continued 35 — docs + repo-hygiene checkpoint; NO engine/render/census change; frontier UNCHANGED 50/58; render commit
stays `c19b565`.** Four commits on `feat/truelinev2` (HEAD **`8ea66bc`**): **`b083b76`** added
`wiki/trueline_v2_50_of_58_accountability_table.md` — the 58-log ledger (**50 DRAWN / 1 COVERED log14←log10 / 4
OWNER_LOCKED_ABSTAIN log5·31·38·43 / 2 SOURCE_GAP log15·16 / 1 MISSING_SOURCE_SHEET log57**; drawn set = sweep
`ALREADY_DRAWN`∪`NEW_TARGETS`; `placement_status` proven STALE → never the drawn census). **`15e00f7`** added
`wiki/trueline_v2_engine_website_readiness_audit.md`: the engine is **accountability-complete but NOT 58/58
drawn-complete and NOT website-ready** — the gap is a CONTRACT boundary (no machine-readable `redline_manifest.json`;
no clean parameterized runner — proof-script-driven + Brenham-hardcoded, seam exemplar-only log53/64/71, API default-OFF
review-card transport; artifacts gitignored/on-demand; runtime unbenchmarked; stale `placement_status`; proof≠final),
with 5 status + 6 provenance enums (**log3 preserved OWNER_CONFIRMED_HUMAN_ADJUSTABLE, not AUTO**) + the two-truth-axes
warning. **`6f2e4a5`** (repo-hygiene fix 1) tracked the load-bearing
`truelinev2/proof/run_station_corridor_route_solver_slice.py` (imported by TRACKED `run_log15_log16_run_group_review_slice.py:37`
+ `test_log15_log16_run_group_review.py:79` → clone/CI fix; targeted test 8 passed). **`8ea66bc`** (repo-hygiene fix 2)
added `.gitignore` rules `.agents/` + `skills-lock.json` (untracked 100→86). NO code/renderer/fixture/census/flag change.
Safe website work NOW = contract-first mock UI vs the manifest schema; no live wiring. Detail: [[current-sprint]] /
[[log]] continued 35.

### Prior — continued 34 (2026-06-19): log3 WIRED + DRAWN (49→50)
**continued 34 — log3 owner-confirmed / HUMAN-ADJUSTABLE GEOMETRY render.** Frontier **49 → 50/58** (log3 = 50th
drawn). Lineage `069e70d` (continued-33 save) → **`683825c`** (log3 owner-control ingest PROOF, 16/16, read-only)
→ **`c19b565`** (log3 sweep WIRING, DRAWN). The s3 `12+66→15+13` conduit is too FRAGMENTED to auto-trace
(`DESIGN_PATH_NOT_CONNECTED`); the owner confirmed the route + dotted the TOP path → 11 owner control points
DIFF-ingested → the s3 leg is the STRAIGHT segment between two source-bound endpoints (matchline crossing @ owner
top-y 296.5 → `15+13 NEXTLINK HH`), control-point-verified (maxdev 1.3pt), closing 247.7'. Renders 2 red strokes
(s2 `12+63 FLOWER POT` stub 2.8' + s3 247') via gated opt-ins `printed_run_callout_chain_route` +
`owner_confirmed_geometry`; new content = upstream **250'**, downstream `15+13→21+63` (650') COVERED by drawn log4
(gated `covered_by_drawn_children` parent-gate exception), 0 overlap. **FIRST owner-GEOMETRY render** — Patrick
classified it the **HUMAN-ADJUSTABLE lane** (NOT deterministic AUTO), reconciling with "never invented
coordinates / manual placement is not the [AUTO] product". All **64 prior PNGs BYTE-IDENTICAL** (md5 stash-baseline
diff); census FROZEN (`doc`); `parent_source_model`/fixtures UNTOUCHED; v2 1392/2-skip; e2e PASS. Doctrine:
fragmented conduit + parallel tracks → owner picks the track + confirms straightness → straight segment between
BOUND endpoints (minimal, not freehand); DIFF-vs-baseline ingests owner packets (only explicit marks). Detail:
[[current-sprint]] / [[log]] continued 34.

### Prior — continued 33 (2026-06-18, READ-ONLY adjudication; no render)
**continued 33 — log3/log14 owner-adjudication (READ-ONLY).** No render; frontier UNCHANGED **49/58**. **log14 =
confirmed DUPLICATE of drawn log10** — its only bindable s7 route is log10's first leg (`0+58=0+00 / 0+00→4+16`);
end `4+18` unprintable; `solve_log` BLOCKED → covered_by_existing_redline, NOT a missing redline (effective
denominator ~57). **log3 = RECLASSIFIED distinct longer bore** (not a duplicate): `12+63` is printed on s2 as a
DRIVEWAY boundary (non-structure → unbindable start); `12+66` is the s2/s3 matchline; log3 shares log4's
`15+13→21+63` downstream half; nearest real origin = the 9+75 AP-106 8-port HH (288' upstream/outside span) →
moved from "owner-adjudication" to "owner span-correction + N-leg solver" blocker. Evidence packet
`gac/log3_log14_owner_adjudication.md` (committed). NO engine/render/census/fixture mutation. Doctrine: a corpus
"start" can be a driveway/matchline cut — trace the continuous-frame chain across matchlines before ruling
duplicate/dead-end. Detail: [[current-sprint]] / [[log]] continued 33.

### Prior — continued 32 (2026-06-18): log44 render (48→49)
ONE render this arc — frontier **48 → 49/58** (one actual drawn red stroke). Lineage `62ee0da` → `44597ff`
(continued-31 save) → **`7039c48`** (pushed → `origin/feat/truelinev2` = `7039c48`). Census FROZEN; both new
primitives are GATED per-log opt-ins → all **62 prior render PNGs BYTE-IDENTICAL** (md5-verified); global
`BASE_CONDUIT` / `MAX_DASH_GAP` / `parent_source_model.json` untouched. Only two files changed:
`truelinev2/proof/run_callout_route_assembly_sweep.py` + its test.
- **`7039c48` — log44 (48→49), owner-corrected Woodson run + footage-tick evidence.** bore_log17 Segment B.
  Owner SOURCE-VERIFIED the corpus "print 18" as a sheet mis-map onto the real **WOODSON LN drop on sheets
  10+13**. Cross-sheet 2-leg (the log70 shape): **STA 43+36 INSTALLER HH** (s10, = local 0+00) → down Woodson
  167' → **MATCHLINE 1+67/1+66 SEE SHEET 13** → past **AP-158 TERMINAL 8 PORT HH (STA 2+45, INTERMEDIATE)** →
  **STA 3+23 FLOWER POT** (s13); drawn 318.2' closes 323' (corpus 325'). Binds on `endpoint_anchors` alone; the
  bundled 1+67/1+66 matchline is unique by chain-reach (the parallel 1+66 RIGHT sibling excluded). TWO new
  GATED opt-ins: (1) `owner_corrected_parent_sheet_context` flips the parent gate's stale corpus sheet
  [18]→[10,13] ONLY after span-closure + anti-sibling-mixup already pass (fixture untouched); (2)
  `footage_tick_ladder_route_evidence` — NEW reusable primitive: the printed **2'/5'/7' footage-tick LADDERS**
  corroborate each leg's direction + length (abstains if a leg lacks one; NEVER sets endpoints; band 22pt <
  half the ~50' ROW spacing → belongs to THIS bore, not a parallel sibling). Overlap clean: only the shared
  3+23 FLOWER POT junction with the consecutive log47 (0' coincident; distinct parents).
- Prior arc (continued 31, `a8b2d31`/`f75c5c6`/`62ee0da`): log30 (Ledbetter parallel), log4 (FIRST fiber-MAIN),
  log42 (owner-corrected AP-105 terminal). Detail: [[current-sprint]] / [[log]] continued 31.
- Invariants this commit: census FROZEN (flag-OFF 31/6/1/17/3, flag-ON 22/1/4); `TRUELINE_MANUAL_ADJUDICATIONS`
  default-OFF; no corpus/`parent_source_model`/fixture mutation; NO census rebaseline; NO new production flag; red strokes.

## Current redline frontier
**50/58 drawn** (+log3 this arc). **log3 RESOLVED — WIRED + DRAWN** (continued-34, `c19b565`): owner-confirmed /
HUMAN-ADJUSTABLE GEOMETRY render — s2 `12+63 FLOWER POT` stub (2.8') + s3 247' STRAIGHT top-path between two bound
endpoints (matchline crossing @ owner top-y → `15+13 NEXTLINK HH`, 11-control-point-verified); downstream
`15+13→21+63` (650') covered by drawn log4. **log14** remains a confirmed DUPLICATE of drawn log10
(covered_by_existing_redline — the 8th non-drawn, NOT a missing redline). Remaining genuinely-open **7**:
log5, log15, log16, log31, log38, log43, log57 — all owner/source-gated:
- **Owner-locked ABSTAIN:** log5, log31, log38, log43 (`must_remain_abstained`).
- **Source-gap:** log15/log16 (unprinted ruler-cuts → sheet-5+ head-end), log57 (`.FS` drive sheet, absent).
- (RESOLVED continued 34: log3 WIRED + DRAWN — owner-confirmed/human-adjustable GEOMETRY, the FIRST owner-geometry
  render; gated per-log opt-ins, 64 prior PNGs byte-identical, census frozen, fixtures untouched. `c19b565`.)
- (RESOLVED continued 33: log14 = confirmed DUPLICATE of drawn log10; log3 reclassified (then wired in 34) —
  packet `gac/log3_log14_owner_adjudication.md`.)
- (RESOLVED continued 32: log44 rendered `7039c48` — owner source-verified the corpus print-18 mis-map onto the
  Woodson s10+13 run; AP-158/2+45 intermediate, STA 3+23 FLOWER POT end. The source-location conflict is closed.)

## Current next gates (each separately authorized; NONE started)
1. **`TRUELINE_PRODUCT_P5_FULL_IMPL_AUTH_ROLE_PLAN` ← recommended next (separately authorized; NOT started; PLAN-FIRST).**
   The 4-slice backend/API wiring plan is COMPLETE (Slices 1-4 below). Plan the next phase: external auth / verified
   identity source; the verified-role source for privileged closeout transitions (lock/approve/close/reject/reopen/unlock —
   deliberately deferred through Slice 4); a production-safe API activation strategy (the flag stays default-OFF until then);
   and the internal test-app wiring boundary. Keep v2 separate from v1; preserve the v1 salvage-audit blueprint without
   copying legacy code. Later (each separately gated): P5 full IMPL → P6 parity → P7 engine split → P8 retire v1.
   - **`TRUELINE_PRODUCT_BACKEND_API_SLICE_4_STATUS_CLOSEOUT_BILLING_EXPORT_READS` — ✅ DONE (continued-52, `eea8378`).** 7 thin
     default-OFF `/v2/product` routes: kmz-export safety read (pixel/PDF-only → BLOCKED, never fakes coords), closeout
     evaluate + read (server-authoritative; NON-privileged), billing compute + read (server cost rules from
     `TL2_PRODUCT_BILLING_COST_RULES`; no client billing truth), export-package assemble + read (descriptor-of-references; no
     file generated). The 3 POST routes take no body; NO privileged closeout transitions / verified-role system; +13 tests.
     **4-slice backend/API wiring plan COMPLETE.** See Latest above.
   - **`TRUELINE_PRODUCT_BACKEND_API_SLICE_3_MANIFEST_PROOF_READS` — ✅ DONE (continued-51, `baf36e0`).** 5 thin default-OFF
     `/v2/product` routes: manifest-handoff record + finalize (validate/store/attach slots via the contract → SUCCEEDED/
     REJECTED/FAILED), redline-manifest read (descriptor/state only), artifact listing (manifest-backed FINAL_REDLINE_PNG
     only), and artifact serve by manifest path (consumer allowlist + traversal-safety + checksum). Engine bundle resolved
     from a safe server-side staging ref (no raw client path); validation owned by the contracts; +15 tests. See Latest above.
   - **`TRUELINE_PRODUCT_BACKEND_API_SLICE_2_INPUTS_REVIEW_GATE` — ✅ DONE (continued-50, `bd55273`).** 7 thin default-OFF
     `/v2/product` routes: `POST /jobs/{job_id}/uploads` (base64; stays `extraction_status:"queued"`), reviewed-bore-log
     create, rows add, row review, segment-group define, grouping status, and `GET …/review-queue` (derived
     `engine_eligible_row_ids` + `engine_ready`). Raw uploads + imported rows stay UNTRUSTED; eligibility is DERIVED by the
     contract (never stored / never conferred by the API); +17 tests (no httpx/TestClient/python-multipart). See Latest above.
   - **`TRUELINE_PRODUCT_BACKEND_API_WIRING_PLAN` — ✅ APPROVED + Slice 1 DONE (continued-49, `986ae74`).** 4-slice split behind
     one default-OFF flag. **Slice 1 Foundation** landed: `product_pipeline_api_optin` (`TL2_PRODUCT_PIPELINE_API_OPTIN`) +
     `product_store_root` (`TL2_PRODUCT_STORE_ROOT`) in `config.py`; opt-in mount in `api/app.py`; `api/product_pipeline_routes.py`
     (`/v2/product` project create/get + jobs create/get + transition; context-bearing, `customer_project_id == ctx.tenant.value`
     never from path/body; GET/POST only; tenant-isolated; illegal transition 409); 15 tests (no httpx/TestClient). NO upload/
     review/handoff/proof/kmz/closeout/billing/export routes; NO `python-multipart`/`httpx`; nothing activates unless the flag is on.
   - **`TRUELINE_PRODUCT_EXPORT_PACKAGE_VERSIONED_CLOSEOUT` — ✅ DONE (continued-48, `c5185d5`).** one durable versioned
     `export_package` per job (DRAFT/BLOCKED/ASSEMBLED/READY/FINAL); descriptor / manifest-of-references (NO rendered document); KMZ included only when `kmz_export` EXPORTABLE (pixel-only omits w/ warning, never blocks); closeout-gated readiness/finality; billing snapshot by reference (never recomputed); sets the job `export_package` slot (reference-only, after the record); +24 tests. See Latest above.
   - **`TRUELINE_PRODUCT_BILLING_SUMMARY_SERVER_COMPUTED` — ✅ DONE (continued-47, `42c6bab`).** one durable server-authoritative
     `billing_summary` per job (DRAFT/COMPUTED/BLOCKED/FINAL); Decimal money / versioned injected cost rules / auditable revisions; FINAL only when closeout APPROVED (no billing-approve action; OPEN_ENGINE_BLOCKERS warning-only); +23 tests. See Latest above.
   - **`TRUELINE_PRODUCT_CLOSEOUT_REVIEW_ONE_STATUS` — ✅ DONE (continued-46, `43a8501`).** one durable server-authoritative
     `closeout_review` status per job; OPEN_ENGINE_BLOCKERS hard, KMZ pixel-only = warning; approve/lock re-evaluate + refuse on hard blockers; +20 tests. See Latest above.
   - **`TRUELINE_PRODUCT_KMZ_EXPORT_GEOMETRY_SAFETY` — ✅ DONE (continued-45, `2f70e22`).** `kmz_export`: pixel-only manifests
     BLOCKED with named reasons (never fake coords); read-only over trusted slots; `export_package` untouched; +10 tests. See Latest above.
   - **`TRUELINE_PRODUCT_MANIFEST_HANDOFF` — ✅ DONE (continued-44, `2013879`).** gated validate→durably store→attach slots; +12 tests.
   - **`TRUELINE_PRODUCT_REVIEWED_BORE_LOG_GATE` — ✅ DONE (continued-43, `6048ef1`).** extracted_row + reviewed_bore_log; +23 tests.
   - **`TRUELINE_PRODUCT_UPLOAD_PIPELINE_PROCESSING_JOB` — ✅ Slice 1 DONE (continued-42, `2193a0e`).**
   - **`TRUELINE_V2_FABLE_STAGING_ARTIFACT_HOSTING_VERIFY` — ✅ DONE (continued-42, `served:true`).** Fable web `main` `16c7095`.
   Later (each separately gated): billing / `export_package` → P5 v2 backend/API with EXTERNAL auth → P6 parity → P7 engine split → P8 retire v1.
2. **`TRUELINE_V2_REDLINEMANIFEST_SCHEMA_AND_RUNNER_CONTRACT`** — ✅ DONE (continued-37, Phases 2A–2I, `a0a490f`→`81f3cd3`):
   schema + 50/58 example + mock UI + publisher + unified render registry + real all-50 manifest + published-bundle contract +
   one-command pipeline runner + render-cost benchmark (full refresh ~5.9 min, render-bound) + adapter-neutral durable bundle
   store. All generated artifacts gitignored under `data/outputs/`; ~63 contract tests. Optional follow-on: a warm-engine /
   single-process unified runner to cut the ~6 min refresh.
3. **Repo hygiene** — ✅ COMPLETE (continued-36): untracked 101→0; evidence trail + Group-A committed (`f5dbed1`/`c0e6680`),
   superseded set archived at `d8508b9`, doctrine relocated (`e3df509`). The `git add -A` landmine is defused.
4. **Accountability table** — ✅ DONE (continued-35, `b083b76`); website-readiness audited (`15e00f7`).
5. **Owner-locked abstains** log5/31/38/43 + **source-gap** log15/16 + log57 — unchanged (owner/source input needed).

## Current known blockers (each a NAMED missing piece, NOT a generic solver limitation)
- **log14** — RESOLVED continued-33: its only bindable s7 route IS drawn log10's first leg (reset `0+58=0+00`,
  run `0+00→4+16`; end `4+18` unprintable, `solve_log` BLOCKED) → confirmed DUPLICATE / covered_by_existing_redline.
  Not a missing redline.
- **log3** — RESOLVED continued-34: WIRED + DRAWN (`c19b565`) as an owner-confirmed / HUMAN-ADJUSTABLE GEOMETRY
  render. The fragmented s3 conduit couldn't auto-trace (`DESIGN_PATH_NOT_CONNECTED`), so the owner confirmed the
  straight TOP path (11 control points); s2 `12+63 FLOWER POT` stub + s3 247' straight segment between bound
  endpoints; downstream `15+13→21+63` covered by drawn log4. No longer a blocker. (Was reclassified in continued-33.)
- **Owner-locked abstains** log5/31/38/43 (`must_remain_abstained`); **source-gap** log15/16 (unprinted cuts) + log57 (`.FS`).
- **Stored-anchor debt** — log48 (corrupted `5+14`) + log70 (superseded `1+45`) render via the override, but the
  stored fixture values are still wrong; repair under a census re-baseline. (B-DATA-LOG48-ADJ-1.)
- (RESOLVED continued 32: log44 rendered `7039c48` — owner source-verified the corpus print-18 mis-map onto the
  Woodson s10+13 run via two gated opt-ins (`owner_corrected_parent_sheet_context` + the reusable
  `footage_tick_ladder_route_evidence`); the source-location conflict across sheets 18/13/10 is closed.)
- (RESOLVED continued 31: log30/log4/log42 rendered `a8b2d31`/`f75c5c6`/`62ee0da`; first fiber-MAIN render path
  proven; sibling-shared-trunk gate added; log42 end re-identified as the AP-105 terminal, not a pothole.)

## Forbidden areas (this and every wiki/session-hygiene session)
Do NOT touch: engine code, renderer, fixtures / anchors / coordinates, backend, web, product runtime,
`origin/main`, or deploy. No new production flag; no fixture mutation; no invented coordinates; no owner
naming where the solver can bind from source. All changes surgical, reversible, minimal-blast-radius.

## Where archived detail lives (load ON DEMAND only — never as default bootstrap)
- `C:/Nova/knowledge/TrueLine-Wiki/wiki/hot.md` — current-state arc (read TOP ~35 lines only for latest).
- `…/wiki/current-sprint.md` — per-session rollups (detailed saves).
- `…/wiki/log.md` — full chronological archive.
- `…/wiki/bugs/current-bugs.md` — open bugs by ID (file:line cited).
- `…/wiki/index.md` — section map + on-demand doc index.
- `gac/*.md` (repo) — per-target source-adjudication packets.
- `wiki/active-context.md` (repo) — deeper historical engine context (pre-continued-23; see its banner).

## Bootstrap rule
Read this file first. Read the TOP section of `hot.md` only if you need the very latest. **Do NOT
full-read `log.md`, `current-sprint.md`, or `hot.md`** unless explicitly asked or tracing a specific
historical decision. Trust this snapshot + the latest commit as current truth; verify against `git` if
in doubt. When you `/save-session`, also bump the snapshot block above so this file stays canonical.
