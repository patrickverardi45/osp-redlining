# START HERE — TrueLine v2 / FieldRoute Bootstrap (tight)

> Single source of current working truth. Last save-session 2026-07-07 (continued-127): **the structure-datum REVIEW-candidate reasoner is BUILT (standalone, NOT wired) and a read-only refusal CENSUS decided the next direction** (backend proof/harness only; NOT auth; no engine/render/AUTO/store/staging/wiring change; NOT deployed). Backend HEAD `feat/truelinev2` **`7c40496`** (census v2, LOCAL/unpushed) atop PUSHED **`ad663ef`** (census v1) + **`63ba257`** (reasoner); `origin/feat/truelinev2` `ad663ef`; `origin/main` `068a279` UNTOUCHED. **(1) Reasoner `63ba257`:** standalone read-only harness `structure_datum_reasoning.py` places a bore via a source-supported structure/station DATUM + the logged tally when the exact-box match fails; REVIEW-only (9 conditions; named refusals `NO_PRINTED_DATUM_WITNESS`/`AMBIGUOUS_STRUCTURE_DATUM`/`UNSUPPORTED_FAR_ENDPOINT` + Slice-3 pass-throughs); composes ONLY shipped `extract/` observers (`plan_view_anchor_resolver` + `matchline_join`); draws/places/promotes nothing; NOT wired into product (like `endpoint_binding`/`route_verification` were before composition). 11 tests; v2 2381→2392. **(2) Real proof:** previously-abstaining `bore_log43`/`bore_log5` → honest `AMBIGUOUS_STRUCTURE_DATUM` (parallel runs + unprinted far endpoint); no wrong redline. **(3) Census `ad663ef`+`7c40496`:** read-only `proof/run_structure_datum_census.py` classifies all 58 bores into a mutually-exclusive matrix (JSON under `data/outputs/`; reasoner byte-identical). v2 reconciles the 27 "multi-sheet pass-throughs" against the AUTHORITATIVE closure/render ledger (`final_engine_truth_table`+adjudication via `build_ledger`; frontier a function of the LEDGER only, never run_match). Decision-grade: **pick-card / symbol-class / ranking each convert 0 bores**; the 27 → **10 already-render-frontier-placed + 7 owner-promotion-hold + 2 owner-locked + 6 terminus/parent-child evidence-blocker + 1 OCR + 1 TRUE cross-sheet frame-join gap (`log48`)**; `render_frontier_placed=36` vs old `run_match=25`. v2 2392→2398→**2401 pass / 4 skip**. **Direction DECIDED:** the single-sheet datum-selection arc is STOPPED (0 conversions); "build cross-sheet" is WRONG framing (assembly already converts 10 of 27). Next: (a) owner-promote the 7 held-back — no code; (b) terminus/parent-child evidence modeling for the 6 — largest coded lever; (c) `log48` frame-join — the ONLY true cross-sheet gap (owner-gated + corrupted adjudication). Staging UNCHANGED (still `6231dcf`, PID 33912). `DETERMINISTIC_AUTO=49`/50-58/matrix 11/11 UNCHANGED. **Prior (continued-126):** Print-Reference audit Slices 2+3 (`dd5397e`/`6231dcf`) closed the print ref → engineering sheet → resolved PDF page → station span → REVIEW candidate chain — pushed + live. NOT auth. **Slice 2 (`dd5397e`):** the strict engine reader already carried the bore log's plan-sheet reference (`Bore.sheet_refs`/`print_raw`); the GENERIC/readiness span extractor dropped it. Additive — NEW `_sheet_refs_from_print` (strict reader's EXACT ordered-dedup digit rule, test-locked to `sheets_from_print`; defined LOCALLY, no named-dialect import) + `_PRINT_COLS` synonyms (Print #, Sheet, Plan Sheet, …) + two additive `SpanRow` fields (`print_raw`/`sheet_refs`), detected in both table shapes and copied through `borelog_rows._generic_span_rows` into the `extracted_row` raw. No column → absent (never guessed; "page" is NOT a synonym). +7 tests; v2 2366 → 2373. **Slice 3 (`6231dcf`):** the readiness/REVIEW-candidate lane was print-ref-BLIND (`plan_sheet` default 1 → RAW page index). `plan_sheet` is now OPTIONAL: OMITTED, the bridge derives the sheet from the Slice-2 refs and resolves engineering sheet → PDF page via the title-block index (Slice 1'/C2 rule); binder + verifier evaluate AND the overlay rasterizes on PDF page = `sheet + sheet_offset` (`RouteVerificationReport.sheet_offset` additive, default 0 byte-identical). Both axes SEPARATE in a new `sheet_context`. No refs → prior sheet-1 default; ONE unresolvable ref (or untitled/unreadable plan) → NAMED refusal **`SHEET_REF_UNRESOLVED`** (never page-1 fallback); MULTIPLE distinct refs → NAMED refusal **`MULTI_SHEET_REFS_UNSUPPORTED`** (single-sheet spine); EXPLICIT `plan_sheet` → raw semantics verbatim. +8 tests (incl. a dimensional overlay-page proof); v2 2373 → **2381 pass / 4 skip**. `DETERMINISTIC_AUTO=49`/50-58/matrix 11/11 UNCHANGED; renderer-truth/thresholds/AUTO/recognized-replay/manual-anchor/uploaded-corpus lane/`select_dialect`/`_cap_review`/named-detectors untouched; classifier vocabulary untouched (refusals are bridge-level statuses). **DEPLOYED via TWO backend-only bounces** (web + cloudflared preserved — supervisor `web OK`/`tunnel OK`, NOT restarted): Slice 2 PID 45280 → 8396 on `dd5397e`, Slice 3 PID 8396 → **33912 on `6231dcf`**, web PID 5084 + cloudflared PID 37336 UNCHANGED → 200/200/302; delete 403-blocked + audit `blocked:true`; `starter-project` (generic seed) is the ONLY served job; pre-bounce store snapshots (18 files each). `TL2_ENABLE_DESTRUCTIVE_PRODUCT_ROUTES` UNSET + `TL2_UPLOADED_CORPUS_AUTO_OPTIN` UNSET. **Prior (continued-125):** matchline page-coherence fast-follow `f84b690` — pushed + live. NOT auth.
> Full session history: vault `wiki/log.md` + this file's own git history (pre-compression append-log version at commit `9c4ab33`).
> Verify against `git` before trusting any snapshot. Bump this file at `/save-session` — keep it TIGHT: update sections in place, do not re-grow an append-log.

## Doctrine

- **v2-forward.** v1 = legacy/reference only (including its auth). The product is the `truelinev2/` engine → `redline_manifest` bundle → FieldRoute web (`trueline-web-experience`) + field mobile. PDF redlines are source truth before KMZ.
- **Evidence-seeking.** A completed package CONTAINS the info. Abstain = an unmodeled relationship — an interim safety state and a named engineering target, never a manual fallback, never "done". Zero-false beats coverage.
- **Product lanes.** The engine need not auto-place 100%: abstains classify into product lanes (pick-card suggestions, human-adjustable REVIEW redlines). Banked human grades are never overridden.
- **REVIEW ≠ AUTO ≠ final.** Drawing-capable REVIEW candidates gate STRICTLY on `READY_FOR_REVIEW_REDLINE`. G-e / final AUTO is owner-gated and NOT started.
- **Improve, don't mirror.** Reproduce v1's correct behavior, never its bad design; if parity demands bad design, stop and propose v2-native.
- **Red Stroke Law.** Every drawn overlay is red (canonical `REDLINE_STROKE_RGB`, test-locked). Never recolor source PDF evidence.
- **Generic naming + no demo language.** No customer/project/person/demo/location names in reusable code, routes, env, schema, tests, docs headings — real names are runtime data only. Never show "demo" (or raw `demo-*` ids outside Diagnostics) in customer-facing UI.

## Repo states (verified 2026-07-07)

| Repo | Branch | HEAD | vs remote | Tree |
|---|---|---|---|---|
| `C:\Nova\projects\TrueLine\TrueLine_Beta` (backend/engine) | `feat/truelinev2` | **`7c40496`** (census v2 reconciliation, LOCAL) atop PUSHED `ad663ef` (census v1) + `63ba257` (structure-datum reasoner) | `63ba257`+`ad663ef` **PUSHED** to `origin/feat/truelinev2` (`ad663ef`, FF 0/0); **`7c40496` LOCAL/unpushed + this continued-127 doc bump pending (push owner-gated)**; `origin/main` `068a279` UNTOUCHED | clean (untracked proof PNGs/scratchpad only) |
| `C:\Nova\projects\trueline-web-experience` (FieldRoute web) | `feat/source-print-review-precision` @ **`2ca3046`** (page-identity label; LOCAL/unpushed, from continued-119/120) atop `342d804` (deployed) atop `4f6047f` (= `origin/main`) — **UNCHANGED this session** | web `origin/main` **`4f6047f`**; `2ca3046` local-only | clean |
| `C:\Nova\projects\trueline-field-mobile` (field app) | `master` | `4ac00a8` | **NO remote at all** — local-only (owner-gated), UNCHANGED | clean |
| `C:\Nova\knowledge\TrueLine-Wiki` (vault) | — | continued-127 save | no remote; large pre-existing drift | stage ONLY touched files, never `git add -A` |

Engine truth: `DETERMINISTIC_AUTO=49`, deterministic **50/58**, cold matrix **11/11** — unchanged (CI-asserted in-suite through the continued-127 structure-datum reasoner + census; the reasoner is byte-identical through the census); full v2 suite **2401 pass / 4 skip** (continued-127 reasoner +11 / census +9, atop continued-126 Slices 2+3 +15). **Field-evidence photo thumbnails are now LIVE on staging and owner VISUAL-CONFIRMED** (continued-114, 2026-07-02): backend `9baa98a` (MERGED to web `main` via PR **#7**, merge commit `0f85d85`) adds a tenant-scoped PHOTO read `GET /jobs/{id}/uploads/{upload_id}/photo` (mounted only under `TL2_FIELD_EVIDENCE_API_OPTIN`; PHOTO-kind allowlist + fixed image content-type + path containment; wrong-tenant/missing/non-PHOTO/unsafe → uniform 404; **pure read**, no lifecycle/slot/review/closeout/placement/AUTO change, snapshot-locked). Web thumbnails render behind **`NEXT_PUBLIC_TL2_FIELD_EVIDENCE_THUMBS`** (null uploadId never fetches; failed fetch → honest "Photo unavailable."; no mock) — **now baked ON in the staging build**. **Activation (surgical, reversible):** staging backend bounced onto working tree `7984e27` (photo route now in live OpenAPI) + staging web rebuilt from `0f85d85` with the thumbs flag ON (BUILD_ID `jKX7Gx63Jz4qlWusomVf0`; rollback snapshot `.next.prev-prethumbs` = pre-thumbs `G54G1ONBF9H4OZfZCmBz2`); cloudflared tunnel never restarted. Prior arc on web `main` via PR **#6** (`52b46c5`; PR **#5** auto-resolved/superseded). Backend `origin/main` `068a279` + mobile UNTOUCHED.

**Production-ops baseline (continued-115, PUSHED; NOT auth).** First ops-safety slice — all **default-off** seams, no engine/renderer/`_cap_review`/placement/closeout change; both repos were CI-greenfield. **Backend `466cc02`→`e708c1d`** (pushed `origin/feat/truelinev2` 0/0; `e708c1d` = CI dep-pin of the `truelinev2/requirements.txt` web/server stack → backend-checks GREEN): first backend CI (`.github/workflows/backend-checks.yml`, targeted 276-test set — the deterministic render/proof corpus is excluded, large gitignored fixtures + slow, verified locally); **default-off observability** (`api/observability.py` — `FIELDROUTE_SENTRY_DSN`|`SENTRY_DSN`, optional `sentry-sdk`, never crashes unconfigured, privacy-safe `send_default_pii=False`/`max_request_body_size="never"`); **default-off in-process rate-limit guardrail** (`api/rate_limit.py` — mounted only under `TL2_RATE_LIMIT_OPTIN`, `/v2/health` exempt, inner-of-CORS, sits behind Access; a single-instance fallback, NOT the production limiter); doc `truelinev2/docs/production-ops-baseline.md`. **Web `df4b101`→`1c2986d`** (**PR #8 MERGED** → web `origin/main` `9ce4de9`; `1c2986d` = Node-24 CI fix; local `main` realigned to `9ce4de9`; branch kept): web CI (`typecheck`/lint/pure checks/build, no deploy); **default-off observability seam** (`src/lib/observability.ts` + `src/instrumentation.ts` — server-only `FIELDROUTE_OBSERVABILITY_DSN`, never logs tenant/session/bodies); `docs/production-ops.md`; `package.json` `typecheck`. **Managed auth / production rate-limit provider / observability DSN / hosting stay owner-vendor-gated** — this is not the auth slice. **PR #8 is MERGED — the baseline is fully landed; no open decision remains from this arc.**

**Arbitrary-upload bore-log extraction (continued-117, PUSHED + backend-LIVE; NOT auth).** The customer extract path is now two-tier: the strict engine reader (`load_borelog`) runs FIRST and unchanged (recognized files byte-identical), and ONLY after it refuses does the SHIPPED generic name-free span extractor (the readiness spine's reader — read-only reuse; `select_dialect` never weakened) run — turning a simple structured CSV/XLSX/text-PDF span source into UNTRUSTED `TABLE_IMPORT`/UNREVIEWED `extracted_row`s carrying source provenance (grammar/citation/source-kind+page/footage-source) + the extractor's OWN source-derived confidence band (never fabricated). Nothing is invented: no source-confirmed span → ONE honest `BoreLogExtractionError` naming `BORE_LOG_FORMAT_UNRECOGNIZED` + the specific refusal reason (`d0bf3c0` sanitized the detail so NO absolute-server / `product_store` / payload path leaks). The human review/grouping/eligibility gate is UNCHANGED — `engine_ready` stays False, no AUTO, no placement, no closeout. **Backend `ac1979e`→`d0bf3c0`** PUSHED `origin/feat/truelinev2` 0/0 (full v2 **2310 pass / 4 skip**; CI set 278; changed-file 126). **Backend-LIVE on staging** via a backend-only refresh (kill the `:8100` listener + supervisor `-Once`; web/cloudflared untouched). **Isolated temp-job smoke PASSED** (`tmp-upload-readiness-smoke`: generic CSV → 1 UNREVIEWED row + `engine_ready` False + job CREATED; junk file → 400 `BORE_LOG_FORMAT_UNRECOGNIZED`, no path leak) → temp job **DELETED** → store back to the exact 3 demos, all truths intact. **Web `feat/upload-review-readiness` `b7e3096`** (surfaces the backend refusal `detail`, bounded 400 chars, best-effort) — **continued-118: MERGED via PR #9** (merge `4f6047f`) into web `main`/`origin/main`; local `main` realigned; branch kept. Staging web still runs the pre-PR-9 build (BUILD_ID `VWf0jrT1-iCg59G6xpkDd`), so the merged display is in `main` but NOT yet served — activation (clean rebuild from `main` `4f6047f` + web-only bounce; shared-`.next` caution) is the current open decision.

## Recent commits (backend `7c40496` LOCAL / `ad663ef` PUSHED; web precision branch local-only; mobile local-only)

- **Backend `feat/truelinev2` (HEAD `7c40496`; PUSHED through `ad663ef`):** …**`dd5397e`** *Slice 2* (generic extractor preserves `print_raw`/`sheet_refs`) → **`6231dcf`** *Slice 3* (readiness derives plan sheet from the print ref) → `a3671a0` continued-126 doc bump → **`63ba257`** *structure-datum reasoner* (`feat(reason)`: standalone read-only harness; datum + tally → REVIEW candidate when the exact-box match fails; NOT wired; +11 tests; v2 →2392) → **`ad663ef`** *refusal census v1* (`proof(census)`: 58-bore mutually-exclusive matrix; +6 tests; →2398) → **`7c40496`** *census v2 reconciliation* (LOCAL; reconciles the 27 MULTI_REF vs the authoritative render-frontier ledger → 1 true gap `log48`; +3 tests; →2401) → this continued-127 doc bump. Prior in-window: Slice 1′ `6c6b78d`, matchline `f84b690`.
- **Web (prior arc on `main` `52b46c5` via PR #6):** `f16d88c` readiness panel → … → `24aff38` field-evidence display → `97421db` nav → `536fb26` candidate primacy. PR #7 (`0f85d85`) opt-in photo thumbnails MERGED. **PR #8 MERGED (`9ce4de9`, prod-ops `df4b101`→`1c2986d`):** web CI (`.github/workflows/web-checks.yml`) + default-off observability seam (`src/lib/observability.ts` + `src/instrumentation.ts`, server-only `FIELDROUTE_OBSERVABILITY_DSN`) + `docs/production-ops.md` + `package.json typecheck`; existing middleware/components/routes UNTOUCHED. Branches kept: `feat/production-ops-baseline` (`1c2986d`), `feat/review-readiness-panel` (PR #5). **`feat/upload-review-readiness` MERGED via PR #9 (merge `4f6047f`; branch kept `b7e3096`):** `productWrites.ts` appends the backend refusal `detail` (bounded 400 chars, best-effort, cannot crash) to a failed write so the bore-log gate shows the honest `BORE_LOG_FORMAT_UNRECOGNIZED` blocker instead of a bare HTTP 400 — now in web `main` `4f6047f`, NOT yet served on staging (pre-PR-9 build still live).
- **Mobile `master` (local-only):** `bb1e6d4`→`d0f1e83`→`a2d9d77` (branding/live-read/segment-evidence) → **`4ac00a8`** field-evidence WRITE client (`fieldEvidenceWrite.ts`, env-gated default-OFF, kebab→snake maps, plain-English refusals, `npm run evidence:live-write`).

## Staging state (Cloudflare, LIVE)

- Zone `fieldroute.io`; tunnel `fieldroute-api-staging` (`47f42c57…`); `staging.fieldroute.io` + `api-staging.fieldroute.io`; Access team `morning-river-d67d`, One-time-PIN, verified blocking both. Same-origin path-split.
- **Backend live on `6231dcf`** (**continued-127: STAGING UNCHANGED — nothing deployed** [the structure-datum reasoner + census are a standalone capability + read-only proof, NOT wired, NOT deployed]; still the continued-126 Slices-2+3 backend, **PID 33912 on `6231dcf`**; web PID 5084 + cloudflared PID 37336 unchanged; 200/200/302). Live flags: `TL2_PRODUCT_PIPELINE_API_OPTIN=1`, `TL2_PRODUCT_READINESS_API_OPTIN=1`, `TL2_FIELD_EVIDENCE_API_OPTIN=1` (durable); **`TL2_ENABLE_DESTRUCTIVE_PRODUCT_ROUTES` UNSET → job-delete 403-blocked**; **`TL2_UPLOADED_CORPUS_AUTO_OPTIN` UNSET → AUTO cap ACTIVE**. Bounce = kill dead `:8100` proc + start via the supervisor's `Start-Backend` env (NEVER `-Restart` — it also kills web+cloudflared; env durable in the gitignored `data/outputs/truelinev2/staging_smoke/ops/staging-supervisor.ps1`). **⚠️ Host-sleep reaps recur** (backend+web down, cloudflared alive) → heal via supervisor `Start-Backend`/`Start-Web` (existing `.next`, NO rebuild). Backend smoke green (health 200; jobs load; delete → 403 + audit `blocked:true`; `starter-project` survives). Pre-activation + pre-bounce store snapshots under `staging_smoke/product_store_snapshots/`. **Store = ONLY a generic seeded `starter-project`** (the real `test` job was deleted mid-session, UNATTRIBUTED; curated demos remain in the non-served `council_audit/product_store`).
- **Web serving the continued-120 Slice A build** (BUILD_ID `r0U_Grt_qxp6CCr0QBj2I`, rebuilt from web `342d804`; rollback `.next.prev-preslicea` = the PR#9 build `6wXLfCDQ…`) — **UNCHANGED this session** (continued-121 was a BACKEND-ONLY bounce; no web rebuild/restart, no `.next` touch). **CAUTION:** the staging web `next start` serves from the web checkout's `.next` — **never run `npm run build` in the web checkout while it backs live staging** (build in isolation, or restore from `main` + bounce). The web precision-label branch `2ca3046` (page-identity) is LOCAL/unpushed and NOT served. Field-evidence photo thumbnails + the PR #9 refusal-detail display are live in the served build (owner-confirmed).
- **`generic-ready-demo`:** readiness `READY_FOR_REVIEW_REDLINE` + `REVIEW_CANDIDATE_READY` → the source-backed candidate is the **primary review surface**; the strict `/workflow/redline` abstains honestly (`BORE_LOG_FORMAT_UNRECOGNIZED`, HTTP 200, controlled — no 500, no closeout claim, review-only). Job stays `CREATED`.
- **`demo-general-upload`:** `REVIEW_ACCEPTED`, closeout **READY / pkg-2**, artifact route 200 (healed bundle `…fe3a80381e45`), pricing 150 ft × $15 = $2,250.00 — unchanged. Backups: `staging_smoke/ops/backups/demo-general-upload-*-2026-07-01/`.

## Mobile state (FieldRoute Capture — temp name)

- Expo SDK 56, managed workflow, **runs in Expo Go** (no native modules). Mock-first; env-gated READ-ONLY live client (`EXPO_PUBLIC_TL2_PRODUCT_API/_API_BASE/_TENANT[/_SESSION]`, default OFF; live REPLACES mock — never mixed; plain-English `statusCopy`, raw engine codes never render; REVIEW never AUTO/final).
- Field rules (`src/lib/fieldEvidence.ts` + `npm run evidence:check`, 21 checks): required START/END station photos are the only default-required evidence; per-problem photos demanded before completion; `BoreReading` with `offsetFt` (~50 ft NOMINAL, advisory) as the future digital-redline plot axis.
- Runtime smoke PASSED (Expo web + Playwright: 0 console errors on every screen; live env → honest-error card with zero mock rows; 5 screenshots).
- **Backend write side (`9351476`):** `contracts/field_evidence.py` (record `trueline-field-evidence-1` per segment; DRAFT→SUBMITTED_FOR_REVIEW then LOCKED; photos count only when bound to REAL job PHOTO uploads — evidence never invented; refusal `BLOCKED_MISSING_REQUIRED_EVIDENCE`) + `api/field_evidence_routes.py` (tenant-scoped, fail-closed) behind `TL2_FIELD_EVIDENCE_API_OPTIN` — **LIVE on staging**. `review_support_only=True`; submit changes no job status/slot.
- **Mobile write client EXISTS (`4ac00a8`):** `src/lib/fieldEvidenceWrite.ts` mirrors the read client — env-gated default-OFF, needs an explicit `EXPO_PUBLIC_TL2_WRITE_JOB` for the ticket submit; kebab→snake problem/method/photo maps (unknowns throw); plain-English refusals; PHOTO-upload/save/submit ops; `npm run evidence:live-write` (static + live smoke). Web office-review display (`24aff38`) consumes the same records.
- **NOT yet:** on-device write (needs a LAN-reachable backend — `:8100` is loopback-only); camera/GPS (native modules gated); photo thumbnails (no upload byte-serving route yet).

## Apple / TestFlight caution

Owner context: Patrick **HAS an active Apple Developer account** and **TestFlight installed on his iPhone**; he does **NOT have an App Store Connect API key** and does not yet know how to create one. Hard rules: **never automate Apple Developer / App Store Connect website login; never retry Apple ID/2FA — if Apple auth fails once, STOP and report.** When EAS/TestFlight time arrives, guide Patrick manually, step by step, through creating the ASC API key. Do **NOT** start EAS Build / TestFlight until (1) the mobile app is runtime-stable, (2) the final app name + bundle id are owner-approved ("FieldRoute Capture" is temp; "FieldRoute Field" REJECTED), and (3) Patrick explicitly approves. Today's distribution needs only Expo Go (QR scan); the repo also still has no remote.

## Current next choices (all owner-gated; none started)

**PRINT-REFERENCE PAGE-RESOLUTION AUDIT — COMPLETE + LIVE** (Slice 1′ `6c6b78d` + matchline `f84b690` + Slice 2 `dd5397e` + Slice 3 `6231dcf`, all pushed + live). **STRUCTURE-DATUM REASONER — BUILT (standalone, NOT wired) + census-vetted (continued-127).** The refusal census PROVED the single-sheet datum-selection line converts 0 real bores (pick-card / symbol-class / ranking all 0) and reconciled the 27 multi-sheet pass-throughs to a SINGLE true cross-sheet gap. **Next candidates (owner-gated, NOT started), ascending code cost — chosen from the census, NOT invented:** (a) **owner promotion** of the 7 `OWNER_PROMOTION_HOLD` bores (log11/47/52/58/67/69/70) — review action, NO code; (b) **terminus / parent-child evidence modeling** for the 6 `UNMODELED_TERMINUS`/parent-child bores (log6/41/46/54/63/68) — the largest coded lever; (c) **`log48` cross-sheet frame-join** — the ONLY true cross-sheet capability gap (owner-gated + entangled with its corrupted adjudication, see `bugs/current-bugs` log48 note). Also pending: push of the LOCAL census-v2 commit `7c40496`; staging web build refresh (SEPARATE repo). **Do NOT build a pick-card / symbol discriminator / ranking — the census falsified all three.**

0. **C2 + UPLOADED-CORPUS AUTO CAP — SHIPPED, PUSHED, LIVE + SMOKED (continued-121); no open required work.** Backend `d96c324` pushed to `origin/feat/truelinev2` + deployed to staging (backend-only bounce) + isolated staging smoke 14/14. Optional follow-ups (owner-gated, NOT started): (a) push the pending **START_HERE doc bump** (this commit); (b) a small **web-copy nicety** for the capped candidate — friendly copy for the new `UPLOADED_CORPUS_AUTO_OWNER_GATED` blocker + soften the generic "not tight enough evidence" line for a policy-capped exact match — plus Option C (dashed stroke / manifest `raw_engine_tier` vs `product_review_tier`); doctrine holds without either; (c) cleanup of the pre-existing staging `test` job. **Web is a SEPARATE repo — any web-copy change ships there, never in this backend repo.**
1. **STAGING PHOTO ACTIVATION — DONE + tidied** (continued-114, owner visual-confirmed): backend bounced onto `7984e27` (photo route live) + web rebuilt on `0f85d85` with `NEXT_PUBLIC_TL2_FIELD_EVIDENCE_THUMBS=1`; temp-job smoke PASSED (right-tenant 200 image/png / wrong-tenant 404 / PLAN_PDF 404 / submit `SUBMITTED_FOR_REVIEW`); owner saw the thumbnails; temp jobs deleted; store back to exactly the 3 demos with all truths intact. PR #6 (`52b46c5`) + PR #7 (`0f85d85`) MERGED to web `main`; PR #5 auto-resolved/superseded. **Merged-branch cleanup DONE:** `feat/hector-preflight` (was `536fb26`) + `feat/field-evidence-photo-thumbnails` (was `3927071`) deleted local + origin (safe `-d`, ancestry-verified); `feat/review-readiness-panel` kept. **This arc has NO remaining required work.**
2. **Mobile remote** — create later (owner decides repo name/visibility; blocked on the FINAL app name — "FieldRoute Capture" is temp, "FieldRoute Field" REJECTED). Until then mobile is local-only.
3. **On-device mobile write** — needs an owner-approved LAN bind of the staging backend (`:8100` is loopback-only); the Access-gated public edge is NOT a mobile write path (native fetch can't do OTP; a bundled token would leak).
4. **Camera/GPS** native modules; bore-reading plotting on the proof surface. (Photo byte-serving route + opt-in thumbnails = DONE + LIVE on staging.)
5. **Source-backed candidate → closeout** is deliberately NOT wired (candidate stays review-only); wiring it is a separate owner-gated slice.
6. **Cold-package EVIDENCE COLLECTION** (not code): 32 packages need a bore-log span source; cold-011 needs the owner-gated B2 off-route-label binder. G-e final/AUTO owner-gated, NOT started; Hector v1-parity parked.

## Guardrails

- **Never touch:** `select_dialect`, named detectors, `_cap_review`, renderer truths. AUTO/G-e BLOCKED. **Uploaded-corpus AUTO is owner-gated by `TL2_UPLOADED_CORPUS_AUTO_OPTIN`** (default OFF → a raw engine `AUTO_SELECT` on a customer upload is capped to a REVIEW candidate; the engine's raw verdict is preserved as metadata; the rendered bundle is unchanged — the cap is a product-tier/acceptance gate only, no renderer change; closeout/export blocked until human acceptance). All prior proof/diagnostic renders stay byte-identical (caption default True).
- **Locked constants:** `DETERMINISTIC_AUTO=49`, deterministic 50/58, cold matrix 11/11, v2 suite green (**2401/4**) — re-verify after any engine-adjacent change.
- **`origin/main` untouched** in backend AND web unless the owner says merge.
- **New API surface ships behind default-OFF flags**, enabled nowhere until owner opt-in.
- **Tests:** repo-root venv (Python 3.11.9) + `PYTHONPATH` for backend tests; `backend\venv` is a broken trap.
- **Wiki vault:** stage only files you touched; never `git add -A`/push (no remote).
- **LAMA** mobile (`C:\dev\lama\lama-mobile`) is a separate product — never mix into TrueLine sessions.
- `truelinev2/` still lives INSIDE `TrueLine_Beta` — don't wipe the old tree until the engine split.
- Commit/push only when the owner asks; verify git truth before trusting any stale summary (including this file).

## Archived detail (load ON DEMAND only)

Vault (`C:/Nova/knowledge/TrueLine-Wiki/wiki/`): `hot.md` (top ~35 lines for latest), `current-sprint.md`, `log.md`, `bugs/current-bugs.md`, `index.md`. Repo: `gac/*.md` (adjudication packets), `wiki/active-context.md` (pre-continued-23 engine history). **Do NOT full-read the logs** unless tracing a specific historical decision.
